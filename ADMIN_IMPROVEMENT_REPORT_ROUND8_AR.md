# تقرير تحسينات نظام الرصيد والأكواد — DZ HOOF IPTV (الجولة الثامنة)

**التاريخ:** 2026-08-24 (جولة ثامنة)
**النطاق:** نظام رصيد الأكواد للمحلات (بوابة الموزعين + لوحة الأدمن + الباكند)
**الحالة:** ✅ مُنفَّذ ومرفوع على GitHub (`c2c1ddc`) + منشور على الإنتاج

---

## 1) المشكلة

فكرة الرصيد والأكواد كانت ناقصة في 4 نقاط جوهرية:
1. **الأكواد التي يولّدها الموزع لا تنتهي أبدًا** (`codeExpiresAt: null`) — الرصيد يُستهلك بأكواد قد تبقى معلقة للأبد بلا تفعيل.
2. **لا يوجد استرجاع للرصيد** — لا عند انتهاء الصلاحية ولا يدويًا من الأدمن؛ الكود الملغي/المنتهي يضيع رصيده.
3. **لا يوجد سجل حركات** (منح / توليد / استرجاع) — لا شفافية للموزع ولا للأدمن.
4. **الموزع لا يرى سعر الجملة ولا يغيّر كلمة مروره بنفسه** (كانت تُضبط من الأدمن فقط).

## 2) ما نُفّذ

### أ) سجل حركات الرصيد (Credit Ledger) — الشفافية الكاملة
- نموذج جديد `CreditTransaction`: `{resellerId, planId, type, quantity (موقّعة), balanceAfter, note, createdBy}`.
- أنواع الحركات: `GRANT` (منح/خصم من الأدمن) · `CONSUME` (توليد أكواد) · `RETURN` (استرجاع يدوي) · `EXPIRE_RETURN` (استرجاع تلقائي بعد انتهاء الصلاحية).
- يُسجَّل تلقائيًا: عند إنشاء/تعديل رصيد المحل من الأدمن (بالفروقات)، وعند كل توليد، وعند كل استرجاع.
- **واجهة الأدمن**: زر ⏱️ "سجل الرصيد" لكل محل → نافذة تعرض كل الحركات (النوع، الباقة، الكمية، الرصيد بعد، الملاحظة).
- **بوابة الموزع**: قسم "سجل حركات الرصيد" يرى الموزع كل حركاته.

### ب) استرجاع الرصيد يدويًا (Admin)
- زر "استرجاع الأكواد غير المستخدمة" في نافذة السجل: يُلغي كل أكواد المحل غير المستخدمة (`REVOKED`) ويعيد رصيدها فورًا، مع تسجيل حركة `RETURN`.
- يدعم التحديد حسب الدفعة أو الخطة (API).

### ج) انتهاء صلاحية الأكواد + استرجاع الرصيد تلقائيًا
- **الأكواد الجديدة التي يولّدها الموزع تنتهي خلال فترة قابلة للضبط** (إعداد `code_expiry_days`، الافتراضي 30 يومًا) — لا أكواد معلقة للأبد.
- **مهمة مجدولة جديدة `code-expiry-check`** (يوميًا، ظاهرة في صفحة جدول المهام): تعلّم الأكواد المنتهية `EXPIRED` وتعيد رصيدها للموزع تلقائيًا مع تسجيل `EXPIRE_RETURN`.

### د) بوابة الموزع — استقلالية أكبر
- **تغيير كلمة المرور ذاتيًا**: `POST /reseller/auth/change-password` (يتحقق من كلمة المرور الحالية).
- **عرض أسعار الجملة** (`/reseller/me` → `prices`) — الموزع يرى سعر كل باقة.

## 3) الاختبارات

- **الباكند: 34 مجموعة / 247 اختبارًا ناجحًا** (كان 240 → **+7**): `credit-ledger.test.ts` يغطي تسجيل GRANT، الزيادة عبر addResellerCredit، فترة الصلاحية الافتراضية/المعدلة، الاسترجاع اليدوي (إلغاء + إعادة رصيد + حركة RETURN)، عدم وجود أكواد للاسترجاع، انتهاء الصلاحية مع إعادة الرصيد (EXPIRE_RETURN)، وحالة عدم وجود أكواد منتهية.
- tsc نظيف (باكند + واجهة) + ESLint بلا أخطاء.

## 4) النشر

- `git push` → GitHub `main` = `c2c1ddc` (10 ملفات، +1018 سطرًا).
- المزامنة إلى `/opt/dzhoot/server` (تحقق checksums 10/10) ثم `deploy-production.sh --apply`.

## 5) ملفات تغيّرت

```
backend/src/models/CreditTransaction.ts                       (جديد — سجل الحركات)
backend/src/services/subscription-service.ts                  (getCodeExpiryDays, recordCreditTx, addResellerCredit, returnUnusedCreditForReseller, expireStaleCodesAndReturnCredit)
backend/src/services/task-registry.ts                         (مهمة code-expiry-check)
backend/src/routes/reseller.js                                (توليد بفترة صلاحية + سجل CONSUME + /ledger + أسعار في /me)
backend/src/routes/reseller-auth.js                           (POST /change-password)
backend/src/routes/admin-resellers.js                         (سجل GRANT عند الإنشاء/التعديل + POST /:id/credit/return + GET /:id/ledger)
backend/src/__tests__/credit-ledger.test.ts                   (جديد — 7 اختبارات)
frontend/src/app/(dashboard)/admin/resellers/page.tsx         (زر + نافذة سجل الرصيد + استرجاع)
frontend/src/app/reseller/page.tsx                            (سجل + أسعار الجملة + تغيير كلمة المرور)
frontend/src/components/locale-provider.tsx                   (مفاتيح عربي/إنجليزي/فرنسي)
```

## 6) ملاحظات

- **الأكواد القديمة غير المستخدمة** (قبل هذه الجولة، بلا `codeExpiresAt`) لن تنتهي تلقائيًا — تُدار يدويًا بزر الاسترجاع.
- فترة الصلاحية الافتراضية 30 يومًا؛ يمكن ضبطها عبر AppSetting `code_expiry_days` (أو env `CODE_EXPIRY_DAYS`).
- التحقق الحي (بعد النشر): إنشاء محل مؤقت → منح رصيد → توليد أكواد (تظهر فترة الصلاحية) → سجل الحركات → استرجاع → تنظيف.
