# تقرير تحسين لوحتي الأدمن والموزع على الهاتف — DZ HOOF IPTV (الجولة الثانية عشرة)

**التاريخ:** 2026-08-24 (جولة 12)
**النطاق:** تجربة الجوال للوحة الأدمن + بوابة الموزع
**الحالة:** ✅ مُنفَّذ ومرفوع على GitHub (`2061747`) + منشور على الإنتاج (`v1.0.1-20260824T185930Z`)

---

## 1) التشخيص

فحصتُ اللوحتين بمنظور هاتف (< 1024px — فاصل الجوال `lg`):
- **التنقل**: سليم — القائمة الجانبية تنزلق (hamburger + overlay)، المستند بلا تمرير أفقي.
- **بوابة الموزع**: سليمة — الجداول داخل `overflow-x-auto` والشبكات متجاوبة.
- **المشكلة الحقيقية**: جداول لوحة الأدمن كانت تعرض **كل الأعمدة** على الجوال (7 أعمدة في صفحة الأكواد مثلًا) — غير قابلة للاستخدام على شاشة 390px. إطار `DataTable` يدعم إخفاء الأعمدة (`mobileHidden`) لكن **6 صفحات لم تستخدمه** للأعمدة الثانوية. رأسا صفحتي الأكواد والمستخدمين لا يلتفّان على الجوال.

## 2) ما نُفّذ

### أ) إخفاء الأعمدة الثانوية على الجوال (12 عمودًا في 6 صفحات)
| الصفحة | الأعمدة المخفية على الجوال |
|---|---|
| الأكواد | المحل، الدفعة، انتهاء الكود (تُرك: الكود، الباقة، الحالة، المُفعّل، الإجراءات) |
| المحلات | المدينة، الهاتف |
| الباقات | الأجهزة، المتزامنة |
| سجل النشاط | المورد، عنوان IP |
| المستخدمون | البريد، قنوات المستخدم، رمز القناة |
| الدفعات | سعر الجملة، تاريخ الاستلام |

مع **إصلاح خطأ قديم**: عمود "المحل" في صفحة الأكواد كان يحمل `mobileHidden` مكررًا (خطأ نسخ) — أزلنا المكرر.

### ب) رأسا صفحتي الأكواد والمستخدمين يلتفّان على الجوال
`flex-col sm:flex-row` — العنوان والأزرار (تصدير/إنشاء) تتراص عموديًا على الشاشات الضيقة بدل الضغط.

## 3) التحقق الحي (بمنظور 780px — تحت فاصل الجوال)

- **صفحة الأكواد**: الصف يعرض فقط [مربع] الكود، الباقة، الحالة، الإجراءات — المحل/الدفعة/الانتهاء مخفية ✓. الصفوف تلتف كبطاقات.
- **صفحة المحلات**: كل محل بطاقة مدمجة (الاسم، الحالة، الإحصائيات، الرصيد، القيمة، الإجراءات) — المدينة/الهاتف مخفيان ✓.
- المستند بلا تمرير أفقي، والرأس الجانبي يعمل ✓.

## 4) الاختبارات

- tsc نظيف + ESLint بلا أخطاء (6 صفحات معدلة). لا تغييرات على الباكند.

## 5) النشر

- GitHub `main` = `2061747` (6 ملفات) → مزامنة إلى الخادم (checksums 6/6) → النشر الرسمي `v1.0.1-20260824T185930Z`.

## 6) ملفات تغيّرت (الواجهة فقط)

```
frontend/src/app/(dashboard)/admin/codes/page.tsx          (mobileHidden + رأس يلتف + إصلاح مكرر)
frontend/src/app/(dashboard)/admin/resellers/page.tsx      (إخفاء المدينة/الهاتف)
frontend/src/app/(dashboard)/admin/plans/page.tsx          (إخفاء الأجهزة/المتزامنة)
frontend/src/app/(dashboard)/admin/activity/page.tsx       (إخفاء المورد/IP)
frontend/src/app/(dashboard)/admin/users/page.tsx          (إخفاء 3 أعمدة + رأس يلتف)
frontend/src/app/(dashboard)/admin/code-batches/page.tsx   (إخفاء الجملة/الاستلام)
```

## 7) ملاحظات

- قاعدة `DataTable`: أي عمود ثانوي جديد في المستقبل يُفضَّل أن يحمل `mobileHidden: true`.
- البوابة (الموزع) كانت سليمة أصلًا — لم تتطلب تعديلًا.
- للتحقق من أي صفحة على الهاتف: افتحها وعرض النافذة < 1024px — الأعمدة الثانوية تختفي تلقائيًا.

---

## 📱 ملحق — فحص بوابة الموزعين على الجوال (نفس الجولة 12)

### ما تحقق (اختبار حي بمنظور هاتف 780px بموزع تجريبي حقيقي)
| عنصر البوابة | الحالة |
|---|---|
| الدخول (نموذج تسجيل) | ✅ يعمل ويظهر جيدًا |
| بطاقة رصيد الأكواد + زر "إنشاء أكواد" | ✅ تظهر لكل باقة (رصيد + متبقي + زر توليد) |
| توليد الأكواد من الجوال | ✅ نافذة الكمية + زر توليد تعمل (2 كود → CONSUME في السجل) |
| سجل حركات الرصيد | ✅ جدول داخل overflow-x-auto |
| جدول الدفعات (دفعة/مدة/استلام/مفعّل/متبقي) | ✅ داخل overflow-x-auto |
| نافذة أكواد الدفعة (نسخ كود / نسخ الكل) | ✅ بتنسيق بطاقات (كود + حالة + نسخ) |
| تغيير كلمة المرور | ✅ |
| تمرير أفقي على مستوى الصفحة | ✅ لا يوجد (docOverflow = false) |

### خطأان اكتشفهما الفحص الحي وأُصلحا (commit `3e2c655`)
1. **مفتاح ترجمة ناقص**: `portal.activated` كان يظهر كنص خام ("portal.activated 5 0") في بطاقة الإحصائيات — أُضيفت الترجمة (مفعّل/Activated/Activé). فحص شامل لكل مفاتيح `t()` في كل الصفحات: لا مفاتيح ناقصة أخرى.
2. **أكواد دفعات الأدمن لا تنتهي**: `admin-code-batches.js` لم يمرر `codeExpiresInDays` → أكواد الدفعات المُسلَّمة من الأدمن `codeExpiresAt: null` (لا تنتهي أبدًا) بينما أكواد بوابة الموزع تنتهي بعد `code_expiry_days` (45). هذا يكسر نظام "انتهاء الأكواد وإرجاع الرصيد". الآن يقرأ المسار `getCodeExpiryDays()` ويمرره — سلوك موحّد.

### الاختبارات
253/253 ناجحة (+2 جديدة تغطي سلوك codeExpiresAt في الاتجاهين).

### تنظيف
حُذف المتجر التجريبي وأكواده (7) ودفعاته (2) وحركاته (2) من قاعدة الإنتاج — بقي محلا "بن عكنون" فقط.
