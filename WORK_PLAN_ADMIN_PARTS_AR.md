# 🗂️ خطة العمل المقسمة — تطوير لوحة التحكم (DZ HOOF)

> **الغرض**: تقسيم شغل لوحة الأدمن وبوابة الموزع إلى **أجزاء مستقلة**، كل جزء = جلسة عمل
> واحدة قابلة للإنجاز والنشر والحفظ، حتى لا تُستهلك النقاط دفعة واحدة.
> **كيف تستعملها**: في كل جلسة قل «ابدأ الجزء رقم N». عند الإنجاز يُعلَّم ✅ ويُحدَّث هذا الملف.
>
> الوضع عند كتابة الخطة: `main` = آخر دمج (بعد Round 20)، الإنتاج `v1.0.1-20260825T171827Z`،
> 6 حاويات healthy، الاختبارات 43 مجموعة / 314 اختبار ✅.

---

## 📌 قواعد ثابتة لكل جزء (سير العمل)

1. فرع `feat/part-N-<name>` من `main` المحدَّث.
2. قراءة `AGENTS.md` + `server/PROJECT_STATUS.md` قبل لمس الشيفرة.
3. تغييرات صغيرة مركّزة + اختبارات؛ **لا نشر قبل**: `tsc` + `eslint` + اختبارات Backend خضراء.
4. دمج في `main` ← نشر عبر `scripts/deploy/atomic-deploy.sh` من الخادم ← تحقق حي من
   `https://iptv.ld-11.net`.
5. **قفل النشر**: قبل أي deploy تحقّق أن لا جلسة أخرى تنشر (ملف قفل `/tmp/dzhoot-deploy.lock`
   على الخادم بتاريخ/اسم الجلسة، ويُزال بعد النشر). سبب: تعارض جلستين سابقًا (Round 20).
6. تقرير جولة عربي (`ADMIN_IMPROVEMENT_REPORT_ROUND*_AR.md`) + تعليم ✅ هنا.

---

## 🔐 الجزء 0 — تأمين فوري (صغير، يُنصح به أولًا)

- [ ] تغيير كلمة مرور الأدمن الحالية (**مكشوفة علنيًا** في التقارير) — يحتاج موافقتك على الجديدة.
- [ ] تدوير مفتاح SSH `dzhoof-admin-key` (مكشوف داخل المستودع نفسه) + حذف الملف من `main`
      وتنظيف تاريخه (git filter-repo / BFG) + تدوير PAT إن لزم.
- [ ] تفعيل 2FA لحساب الأدمن (الدعم موجود في الكود).
- [ ] تغيير باسورد لوحة MIBOX (`01020304` — ضعيفة) — يتطلب وصولك للوحة المزوّد.
- [ ] إنشاء آلية قفل النشر (القاعدة 5 أعلاه).

**الحجم**: صغير · **الملفات**: إعدادات الخادم + `frontend/src/app/(dashboard)/settings` · **بدون مخاطر على المستخدمين**.

---

## 🧩 الجزء 1 — تذاكر الدعم (موزع ↔ إدارة)

- نموذج `SupportTicket` (موزع، موضوع، حالة: مفتوح/قيد المعالجة/مغلق، رسائل مترابطة).
- API: `POST/GET /reseller/tickets` + `GET/PATCH /admin/tickets` (+ ردود).
- واجهة: قسم «الدعم» في بوابة الموزع + صفحة «التذاكر» في لوحة الأدمن (فلترة بالحالة/الموزع).
- إشعار داخلي عند تذكرة جديدة (يستعمل بنية الإشعارات الموجودة).
- **الحجم**: متوسط · **اختبارات**: `support-tickets.test.ts`.

## 🧩 الجزء 2 — «مشاهدون الآن» (البث المباشر لحظيًا)

- API تجميع الجلسات النشطة (القناة، الزبون، الجهاز، IP، المدة) من مسار authorize الموجود.
- صفحة أدمن «مباشر الآن» (جدول حي + عدّادات) + عرض للموزع **لزبائنه فقط**.
- **الحجم**: متوسط · **ملفات**: `backend/src/routes/streams.js` + صفحات frontend جديدة.

## 🧩 الجزء 3 — تحويل الرصيد بين الموزعين

- قيد محاسبي مزدوج (Ledger) + حدود/موافقة + سجل حركات لكل موزع.
- واجهة في بوابة الموزع + سجل في لوحة الأدمن.
- **الحجم**: متوسط · **اختبارات**: معاملات + أرصدة.

## 🧩 الجزء 4 — لوحة صحة المصادر (Health Dashboard)

- في `admin/sources`: حالة التحقق لكل مزوّد + آخر فحص + زمن الاستجابة + زر تبديل يدوي
  (يبني على `admin-xtream-sources.js` و`isWorking` الموجودين).
- **الحجم**: صغير–متوسط.

## 🧩 الجزء 5 — باقات Live/VOD للكود الواحد ⚠️ (الأكبر — يُقسم 5أ/5ب)

- 5أ: تغيير نموذج البيانات (الكود يحمل Live و/أو VOD) + ترحيل البيانات الحالية.
- 5ب: الواجهات (إنشاء كود بخيارات + عرض) + حدود الوصول في التطبيق.
- **الحجم**: كبير · **مخاطر**: يمس نموذج الاشتراك — نشر بحذر ونافذة صيانة.

## 🧩 الجزء 6 — وايت لابل + API للموزعين

- شعار/اسم لكل موزع يظهر لزبائنه + مفاتيح API موثقة للموزعين الكبار.
- **الحجم**: كبير.

## 🧩 الجزء 7 — موزعون فرعيون (مستويان)

- موزع ينشئ موزعين تحته بحصة من رصيده + تقارير مجمّعة.
- **الحجم**: كبير · يعتمد على الجزء 3 (الأرصدة).

## 🧩 الجزء 8 — بوابة الدفع الجزائرية (CinetPay أولًا) 💳

- صفحة «اشترِ الآن» عامة: اختيار باقة ← دفع (DZD/CCP/بطاقة) ← Webhook ← تفعيل كود فوري.
- يربط مع `CodeBatch` الحالي. بدائل لاحقًا: Edahabia/BaridiMob.
- **الحجم**: كبير · **يحتاج منك**: حساب CinetPay ومفاتيحه.

## 🧩 الجزء 9 — تحسين تغطية EPG (من ~9%)

- مصادر EPG إضافية + مطابقة أسماء القنوات + تسمية عربية.
- **الحجم**: متوسط · **ملفات**: `backend/src/services/epg-service.ts`.

## 🧩 الجزء 10 — نسخ احتياطي بعيد (off-site restic)

- الإعداد جاهز؛ **يحتاج منك**: مزوّد تخزين (B2/S3…) وبياناته.
- **الحجم**: صغير.

---

## 📊 تتبّع الإنجاز

| الجزء | الحالة | الإصدار المنشور | التقرير |
|---|---|---|---|
| 0 — تأمين | ⬜ | — | — |
| 1 — تذاكر | ⬜ | — | — |
| 2 — مشاهدون الآن | ⬜ | — | — |
| 3 — تحويل رصيد | ⬜ | — | — |
| 4 — صحة المصادر | ⬜ | — | — |
| 5 — باقات Live/VOD | ⬜ | — | — |
| 6 — وايت لابل/API | ✅ | بانتظار النشر (PR #178 مدمج 2026-09-03) | ADMIN_IMPROVEMENT_REPORT_ROUND22_AR.md |
| 7 — موزعون فرعيون | ⬜ | — | — |
| 8 — دفع CinetPay | ⬜ | — | — |
| 9 — EPG | ⬜ | — | — |
| 10 — نسخ بعيد | ⬜ | — | — |

> أفكار إضافية خارج اللوحة (مشغّل ويب B1، مباريات اليوم C1، ملخص يومي D3…): مفصّلة في
> `IDEAS_2026-08-25_AR.md` — تُضاف كأجزاء 11+ عند الطلب.
