# تقرير التسليم — جولة استكمال 2026-08-30 (الثالثة)
**النطاق:** استئناف نقاط التسليم المفتوحة من الجولة الثانية (دليل الحالة + EPG + أمان client-redeem) + نشر الحزمة الأمنية #110 + PRs Dependabot · **الحالة النهائية: ✅ مكتملة — الإنتاج على `40bc919`**

---

## ملخص تنفيذي

استُلم المشروع مستقراً بعد الجولة الثانية (`5a535e7` على الإنتاج، 9 PRs عالقة من Dependabot، وبند أمني معروف `client-redeem`، وملاحظة stale عن restic). أُنجز في هذه الجولة:

- **دخول موثَّق**: VPS (`5.135.79.221`) + لوحة الأدمن (تجاوز 2FA عبر فك تشفير `totpSecretEncrypted` بمفتاح `TOTP_ENCRYPTION_KEY` — aes-256-gcm + otplib، التحقق من صحة الحساب أن الكود المحسوب يطابق otplib تماماً) + بوابة الموزعين (`moh`).
- **دمج 9 PRs**: 8 تحديثات Dependabot آمنة (CI أخضر، bumps نقية) + **#110** (تقوية أمنية: إزالة fallback DEMO الضمني، PBKDF2 للـ Parental PIN، R8 للـ Release، preflight script — 407/407 اختبارات).
- **نشر ذرّي** على الإنتاج (`atomic-deploy.sh`) → `/health` = `40bc919…` — كل الحاويات healthy، نسخة mongodump تلقائية، rollback جاهز.
- **EPG**: قياس استهلاك ذاكرة → **رفع الـ cap غير ممكن** لمصدر `epg-us.xml.gz` (55MB مضغوط ≈ 500MB+ XML — يتجاوز ميزانية ذاكرة الـ scheduler). **عُطّل المصدر** عبر واجهة الأدمن مع توثيق السبب، ووُضع إصلاح (PR #111) يعطّل تلقائياً أي مصدر يتجاوز حد الحجم 3 مرات متتالية.
- **restic**: تحقق أن النسخ البعيد مفعّل ويعمل (آخر نجاح 2026-08-30 03:43 UTC) → تحديث الوثائق (PR #112).
- **client-redeem**: إصلاح (PR #113): حد معدّل على مستوى الـ IP لا يمكن التفافه بتدوير deviceId + جلسة مرتبطة بنهاية الاشتراك بدل 365 يوماً ثابتة.

## 1. حالة PRs (16 كانت مفتوحة)

| PR | القرار | السبب |
|----|--------|-------|
| #95 #96 #97 #100 #101 #103 #104 #109 | ✅ دُمجت | bumps نقية، CI أخضر بالكامل |
| #110 (security hardening) | ✅ دُمجت + **نُشرت** | مراجعة كاملة للـ diff، CI أخضر، 407/407 |
| #98 (node 18→26-alpine) | ⏸ معلّقة بتعليق | ترقية كبرى خطرة — تحتاج مراجعة يدوية (كما حددت الجولة الثانية) |
| #99 (typescript 7) #102 (eslint 10) | ⏸ معلّقة بتعليق | CI فاشل (backend/frontend) — ترقيات كبرى |
| #105 (okhttp 5) #106 (composeTv) #107 (coroutines) #108 (hilt) | ⏸ معلّقة بتعليق | CI فاشل (android) — ترقيات كبرى تحتاج هجرة يدوية |

## 2. النشر على الإنتاج

- `stage-release.sh main` → `/opt/dzhoot-releases/40bc9197…` ثم `APPLY=1 atomic-deploy.sh 40bc919…` — نجاح كامل.
- التحقق: `/health` → `{"status":"ok","release":{"commit":"40bc91974189cc2f249866d327eea543ae6a108e","builtAt":"2026-08-30T12:40:39Z"}}` · mongodb/redis connected · مصادر M3U وXtream نشطة · الموقع `/` و`/buy` و`/admin` 200 · APK v1.0.40 يُحمَّل · لا OOM في الـ scheduler.

## 3. EPG — القياس والقرار

- **القياس**: `epg-us.xml.gz` = 55MB مضغوط → يفتح إلى 500MB+ (الحد الأقصى في الكود 100MB decompressed، والرفع فوق ذلك يهدد الـ scheduler بـ OOM — `EPG_HEAP_GUARD_MB=768`).
- **التغطية الفعلية**: انخفضت إلى **~15%** (2,535 من 16,691 قناة) — أدنى من تقدير الجولة الثانية (~24%) — التحقق عبر `/api/v1/epg/unmatched-channels` (14,156 غير مطابقة). السبب الأساسي ليس المصدر المعطّل بل **عدم تطابق المعرّفات** بين أدلة XMLTV و tvgId/channelId في الكتالوج — يحتاج مشروعاً مستقلاً (aliasing/override).
- **الإجراء**: تعطيل `epg-us.xml.gz` (تم، مع note موثّق) + PR #111 (عطّل تلقائي بعد 3 إخفاقات متتالية بنفس الخطأ — `EPG_AUTO_DISABLE_CONSECUTIVE_FAILURES`).
- ملاحظة: 14 مصدراً آخر مكتشفاً معطّلاً أصلاً (404 بلدان) — طبيعي.

## 4. client-redeem — التقييم والإصلاح

- **الخطر الفعلي**: كود التفعيل bearer credential بإنتروبيا 32^12 ≈ 2^60 — التخمين غير عملي. المخاطر الحقيقية: (أ) حد المعدل `(ip:deviceId)` قابل للالتفاف بتدوير deviceId، (ب) جلسة 365 يوماً ثابتة أطول بكثير من اشتراكات قصيرة (تجربة 7 أيام = سنة وصول).
- **الإصلاح (PR #113)**: حد IP إضافي (`REDEEM_MAX_IP_ATTEMPTS=30/10د`) يُسجَّل لكل طلب بما فيه الكود الخاطئ + انتهاء الجلسة = `min(الآن+365ي، نهاية الاشتراك+7أيام سماح)` مع حد أدنى ساعة. الاشتراكات السنوية تحتفظ بجلسة ~سنة، والقصيرة تنضبط.

## 5. ملاحظات مفتوحة (لجولات لاحقة)

1. **مطابقة معرّفات EPG** — التغطية ~15% وتنخفض؛ الأثر الأكبر من عدم تطابق tvg-id، لا من المصادر. مشروع aliasing/override قائم (54 override موجودة).
2. **الترقيات الكبرى المعلّقة**: okhttp 5، eslint 10، typescript 7، node 26-alpine (PRs #98/#99/#102/#105/#106/#107/#108 — موثّقة بالتعليقات).
3. صفحة `/pair` الإنجليزية + تباين الثيم الفاتح — مؤجلتان كما كانتا (غير حرجتين).
4. `dzhoof-shared-network` مع حاويات مستأجرين آخرين — ملاحظة أمنية معروفة في دليل الحالة، لم تُعالج.
5. ملاحظة قانونية في دليل الحالة: المصادر الحالية إعادة بث لمحتوى غير مرخّص — قرار استراتيجي للمالك.

## 6. منهجية

لا دفع مباشر لـ `main`؛ كل التغييرات عبر فروع + CI أخضر (backend + frontend + android + secret-guard + CodeQL) + squash merge. النشر عبر `stage-release.sh` + `atomic-deploy.sh` (نسخة احتياطية + health gate + rollback تلقائي). الإنتاج مطابق للمستودع (مُتحقق بالـ checksums في مرحلة النشر).
