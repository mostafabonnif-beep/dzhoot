# تقرير تحسين لوحة التحكم — DZ HOOF IPTV (الجولة 13: الإتقان)

**التاريخ:** 2026-08-24 (جولة 13)
**النطاق:** إصلاح أخطاء + ميزات جديدة في لوحتي الأدمن (والباك-إند المرافق)
**الحالة:** ✅ مُنفَّذ ومرفوع على GitHub + منشور على الإنتاج

---

## 1) التشخيص

أُجري فحص شامل حي (تسجيل دخول فعلي للوحة) + مراجعة كود لكل صفحات الأدمن
الـ 24. اللوحة مكتملة الميزات بعد 12 جولة، لكن وُجدت:

### أخطاء مؤكدة
| الخطأ | الملف | الأثر |
|---|---|---|
| أسهم التنقل (السابق/التالي) **معكوسة في العربية RTL** في كل الصفحات المترقمة | `components/ui/pagination.tsx` | اتجاه سهم خاطئ بصريًا في كل الجداول |
| خطأ regex مزدوج الإفلات في حقلي 2FA: `/\\D/g` يطابق الحرفين `\D` ولا يمسح الحروف | `admin/settings/page.tsx` (مرتان) | لصق "abc12345" يبقي الحروف → خطأ "رمز 2FA غير صحيح" |
| إحصائيات صفحة الأجهزة تُحسب من **صفحة العرض فقط** (50 جهازًا) | `admin/devices/page.tsx` | أرقام ناقصة (النشطون/المنصات/الطلبات) عند تجاوز 50 جهازًا |
| إشعار مجدول يظهر كـ"مسودة" ويمكن إرساله يدويًا مرتين | `admin/notifications/page.tsx` | الـ backend يدعم `scheduledAt` لكنه يفرض DRAFT ولا يُرسل أبدًا |

### ثغرات ميزات
| الثغرة | الملفات | الوصف |
|---|---|---|
| لا يمكن **تعديل بيانات فيلم/مسلسل** (عنوان، تصنيف، بوستر، سنة...) | `admin-vod.js` + `vod-admin-shell.tsx` | الحل الوحيد كان حذف وإعادة استيراد |
| لا يوجد **بحث شامل** في اللوحة | جديد `global-search.tsx` + `header.tsx` | البحث في 24 صفحة منفصلة فقط |

---

## 2) ما نُفّذ

### أ) إصلاح أسهم الترقيم في RTL (جميع الصفحات)
`Pagination` يبدّل الأيقونتين عندما تكون اللغة عربية: السابق ← سهم يمين، التالي ← سهم
يسار. إصلاح سطرين يلمس كل جدول مترقّم في اللوحة.

### ب) إصلاح regex حقلي 2FA
`/\\D/g` → `/\D/g` في موضعين (تعطيل 2FA + التحقق) — الآن يُمسح كل ما ليس رقمًا.

### ج) إحصائيات الأجهزة الصحيحة + بحث/فلترة
- **الباك-إند** (`admin.js`): `GET /admin/devices` يردّ الآن `stats` محسوبة على كامل
  المجموعة: `active7d` (أجهزة نشطة آخر 7 أيام) + `platforms` (عدد المنصات المميزة) +
  `pendingPairings` (طلبات الاقتران المعلقة).
- **الواجهة**: البطاقات الأربع تقرأ من `stats` بدل الصفحة المرئية. أُضيف **بحث** (debounce)
  و**فلترة بالحالة** (الكل/النشطون/خامل) — الباك-إند كان يدعمها أصلًا ولم تكن مستخدمة.

### د) جدولة الإشعارات (الميزة)
- **الباك-إند** (`admin-notifications.js`): `scheduledAt` في المستقبل → `status: 'SCHEDULED'`
  (كان يفرض DRAFT دائمًا).
- **مهمة مجدولة جديدة** `notification-dispatcher` في `task-registry.ts` (كل دقيقة):
  تلتقط إشعارات SCHEDULED المستحقة (`scheduledAt <= الآن`) وترسلها عبر FCM وتعلّمها
  SENT؛ أي فشل نظامي يبقيها SCHEDULED لإعادة المحاولة.
- **الواجهة**: حقل `datetime-local` في نموذج الإنشاء ("جدولة الإرسال") + زر يتبدل
  نصه بين "جدولة الإرسال" و"إنشاء مسودة" + شارة "مجدول" زرقاء ووقت الجدولة في
  القائمة + فلترة بالحالة + زر "إرسال" للمسودات فقط.

### هـ) تعديل بيانات الأفلام والمسلسلات (الميزة)
- **الباك-إند** (`admin-vod.js`): `PATCH /movies/:id` و`/series/:id` يقبلان الآن حقل
  `isActive` كما كان **أو** حقول بيانات اختيارية مُتحقق منها (عنوان، تصنيف، بوستر،
  خلفية، وصف/قصة، سنة، مدة، تقييم، فريق العمل...) — تُرسَل فقط الحقول المقدَّمة،
  و`null` لمسح سنة/مدة/تقييم.
- **الواجهة** (`vod-admin-shell.tsx`): زر "تعديل" (قلم) في عرضَي الشبكة والقائمة يفتح
  نافذة تحرير بحقول حسب النوع (فيلم/مسلسل) مع تحقق محلي ورسائل عربية.

### و) البحث الشامل ⌘K / Ctrl+K (الميزة)
- مكوّن جديد `global-search.tsx`: نافذة بحث فوق كل الصفحات تبحث **بالتوازي** في
  المستخدمين + أكواد التفعيل + القنوات (3 طلبات مع `Promise.allSettled`)، النتائج
  مجمّعة بترويسات، تنقّل بلوحة المفاتيح (↑/↓ + Enter + Esc)، وصول للنقر، ترويسات
  عربية، وإخفاء تلقائي للأكواد.
- زر بحث في الهيدر مع تلميح `⌘K`، والاختصار يعمل من أي صفحة.

---

## 3) الاختبارات

| الفحص | النتيجة |
|---|---|
| `npm run typecheck` (shared + backend tsc) | ✅ نظيف |
| `npx tsc --noEmit` (frontend) | ✅ نظيف |
| `npx eslint` على كل الملفات المعدلة (7 ملفات) | ✅ 0 أخطاء |
| اختبارات الواجهة (jest) | ✅ 6/6 |
| اختبارات الباك-إند (jest) — تشمل `admin-vod` (10) | ✅ 253/253 |

---

## 4) الملفات المتغيّرة (11)

```
server/backend/src/routes/admin-notifications.js   (جدولة: status SCHEDULED)
server/backend/src/routes/admin-vod.js             (تحرير بيانات VOD)
server/backend/src/routes/admin.js                 (stats الأجهزة)
server/backend/src/services/task-registry.ts       (مهمة notification-dispatcher)
server/frontend/src/app/(dashboard)/admin/devices/page.tsx    (stats + بحث/فلترة)
server/frontend/src/app/(dashboard)/admin/notifications/page.tsx (جدولة + شارة + فلترة)
server/frontend/src/app/(dashboard)/admin/settings/page.tsx   (إصلاح regex 2FA)
server/frontend/src/components/layout/header.tsx              (زر البحث الشامل)
server/frontend/src/components/ui/pagination.tsx              (أسهم RTL)
server/frontend/src/components/vod-admin-shell.tsx            (نافذة تعديل VOD)
server/frontend/src/components/global-search.tsx              (جديد — البحث الشامل)
```

## 5) ملاحظات

- **منطقة زمنية**: حقل الجدولة `datetime-local` يرسل وقت المتصفح المحلي ويُفسَّر
  بخادم الإنتاج (UTC+1) — فرق ساعة محتمل؛ يُنصح لاحقًا بتحويل UTC صريح.
- **FCM غير مُهيّأ** حاليًا (لا مفاتيح) — الإشعارات المجدولة ستُرسل فقط بعد وضع
  مفاتيح Firebase في `/etc/dzhoot/.env.production` (خطوة من الإعدادات). المهمة
  الجديدة تسجّل `configured: false` في سجل جدول المهام.
- **الأسعار والتطبيق**: لم تُلمس أسعار الباقات ولا إصدارات التطبيق.
