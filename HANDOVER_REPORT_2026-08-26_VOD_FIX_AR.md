# تقرير التسليم — DZ HOOF IPTV (جلسة 2026-08-26 — إصلاح الأفلام والمسلسلات)

> **بعد `HANDOVER_REPORT_2026-08-26_VERIFICATION_SESSION_AR.md`.** جلسة إصلاح VOD:
> السببان الجذريان لـ«الأفلام والمسلسلات لا تشتغل» + نشر `v1.0.1-20260826T020928Z`.

---

## 1. الملخص التنفيذي

| الجبهة | قبل | بعد |
|---|---|---|
| **المسلسلات** | 20,391 مسلسل، **0 موسم، 0 حلقة** في القاعدة — لا شيء يشتغل | ✅ حلقات تُجلب lazy من NEO عند أول فتح (مُختبر حيًا: 8 حلقات + 10 حلقات لمسلسلين) وتُبث فعليًا (MKV 206) |
| **الأفلام** | 16,922 فيلمًا **ميتًا** (مصدر ottstreambox المحذوف) يظهر للزبائن ويفشل | ✅ عُطّلت (`isActive:false`) — الكتالوج النشط = 66,908 فيلمًا = عدّاد NEO بالضبط |
| **المسلسلات اليتيمة** | 3,571 مسلسلًا من المصدر المحذوف | ✅ عُطّلت — النشط = 16,819 = عدّاد NEO |
| **الاختبارات** | 329 | ✅ **341/341** (6 اختبارات regression جديدة) + tsc + eslint 0 أخطاء |

## 2. الأسباب الجذرية (ثلاثة)

### أ) parser الحلقات يقرأ صيغة غير موجودة — `xtream-service.ts`
Xtream `get_series_info` يرجع الحلقات في **قاموس `episodes` مفهرس برقم الموسم**
(`{"1": [...]}`) و`seasons[]` يحمل metadata فقط. الكود القديم قرأ `seasons[].episodes`
(صيغة مدمجة نادرة) ⇒ **صفر حلقات مستوردة منذ بداية المشروع**. الإصلاح: union أرقام
المواسم من المكانين + تفضيل القاموس + دعم الصيغة المدمجة + دعم شكل NEO
(`seasons: []` مع قاموس حلقات ممتلئ).

### ب) `duration` النصي يقتل استيراد المسلسل كاملًا
NEO يرسل `info.duration` نصًا حرًا («45 min», «N/A») ⇒ `Number()` = NaN ⇒
Mongoose CastError ⇒ **الاستيراد يموت بعد إنشاء الموسم وقبل أي حلقة** (شوهد حيًا
في سجل الإنتاج: `Cast to Number failed for value "NaN" at path "duration"`).
الإصلاح: استخراج الرقم القائد بالـ regex + try/catch لكل حلقة (حلقة فاسدة لا تقتل الباقي).

### ج) التحميل الكسول كان **admin-only** وفي مسار لا يستخدمه التطبيق
- `series.js` قيّد الجلب بـ `role === 'Admin'` ⇒ الزبائن لا يُشغّلون الجلب أبدًا.
- التطبيق يستخدم مسارات **`/api/v1/catalog/...`** التي لم يكن فيها جلب كسول أصلًا.
الإصلاح: الجلب للجميع في كلا المسارين (`catalog.js` + `series.js`)، محكومًا بختم
`episodesFetchedAt` (نافذة 24 ساعة): مسلسل بلا حلقات فعلًا لا يُعاد جلبه إلا بعد
24س، والفشل العابر **لا يُختم** (يُعاد عند الفتح التالي).

## 3. الملفات المعدّلة

- `backend/src/services/xtream-service.ts` — parser ثلاثي الأشكال + duration آمن + try/catch لكل حلقة + الختم عند النجاح فقط.
- `backend/src/models/Series.ts` — حقل `episodesFetchedAt` جديد.
- `backend/src/routes/series.js` — الجلب الكسول للجميع + حارس الـ24 ساعة (كلا المسارين).
- `backend/src/routes/catalog.js` — **جلب كسول جديد** لمساري seasons/episodes (مسار التطبيق).
- `backend/src/__tests__/series-episodes-sync.test.ts` — 6 اختبارات (الأشكال الثلاثة + الختم + duration الفاسد + عدم الختم عند الفشل).

## 4. عمليات القاعدة (بلا كود)

```js
// تعطيل محتوى ottstreambox الميت (روابطه 403 نهائيًا)
db.movies.updateMany({sourceId: ObjectId("6a8dc2ba4dcee65c98394ebd")}, {$set: {isActive: false}})  // 16,922
db.series.updateMany({sourceId: ObjectId("6a8dc2ba4dcee65c98394ebd")}, {$set: {isActive: false}}) // 3,571
// تنظيف ختم قديم من المحاولة الفاشلة قبل إصلاح duration
db.series.updateMany({episodesFetchedAt: {$ne: null}}, {$set: {episodesFetchedAt: null}})         // 1
```

## 5. التحقق الحي (بعد النشر `v1.0.1-20260826T020928Z`)

1. `GET /api/v1/catalog/series/<NEO series>/seasons` (بجلسة + **بلا auth**) → مواسم تُنشأ لحظيًا.
2. `GET /api/v1/catalog/seasons/<id>/episodes` → **8 حلقات** (Troy) و**10** (Temiz Para — شكل seasons الفارغ).
3. `POST /streams/authorize {EPISODE}` → `mimeType: video/x-matroska`, proxy.
4. `GET playback` → **206 + 2MB بصمة MKV سليمة** (`1a 45 df a3`).
5. فيلم عبر authorize → 206 + 512KB MKV. الصحة ok.

## 6. ملاحظات تشغيلية

- **الفتح الأول لمسلسل** يستدعي NEO API مرة واحدة (~1-2ث) ثم يُخزَّن — هذا متوقع.
- أول 24 ساعة بعد النشر: موجة جلب lazy تدريجية مع فتح الزبائن للمسلسلات — طبيعي،
  معدل محدود بنمط التصفح لا دفعة واحدة.
- **الأفلام**: الزبون على إصدار < 1.0.12 يُجبر على التحديث (من جلسة التحقق) —
  إصلاح الـ mimeType شرط لتشغيل VOD التدريجي في التطبيق.
- حلقات ottstreambox اليتيمة: لا حلقات مستوردة أصلًا (episodes كانت 0 عالميًا)،
  والمسلسلات اليتيمة معطّلة الآن — لا تنظيف إضافي مطلوب. خرائط failover الـ263
  ما زالت يتيمة وخاملة (تنتظر مصدر backup جديد).

## 7. بانتظار المستخدم (كما في التقرير السابق — لا تغيير)

تحديث تطبيقه وتجربة مسلسل/فيلم • اشتراك backup جديد • كلمات المرور + 2FA • webhook • restic • CinetPay.
