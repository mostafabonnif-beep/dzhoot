# تقرير التسليم — جولة استكمال 2026-08-30 (السابعة)
**النطاق:** إغلاق البند الأمني المتبقي + توسيع EPG الشامل · **الحالة: ✅ مكتملة — الإنتاج على `e002f2f`**

---

## 1. أمن — PR #129 (مُدمج `e002f2f`)

**«كود القائمة المُلغى ما زال يُقبل في /pair و/verify»** — آخر بند أمني سريع من مراجعة 2026-08-23:

- `POST /api/v1/tv/pair` كان يفحص `isActive` فقط → **كود مُلغى يقدر يقرن أجهزة جديدة**. الآن 403 برسالة «جدّد كودك» (نفس سلوك باقي النقاط).
- `GET /api/v1/tv/verify/:code` كان يرد `valid: true` لكود ملغى. الآن `valid: false` **بدون كشف السبب** (تبقى oracle ضيقة — لا تسريب لاسم مستخدم/دور/حالة إلغاء).
- اختبار انحدار سلوكي جديد `tv-revoked-code.test.ts` (4 حالات: ملغى/نشط × pair/verify) — 4/4 ✅ محليًا، CI أخضر.

**بقية البنود الأمنية من القائمة اتأكد أنها مُغلقة في جولات سابقة:** PIN الاقتران لا يُسجَّل (اختبار `tv-pairing-log-safety` موجود) · client-redeem: حدّ IP لكل طلب (لا التفاف عبر deviceId) + جلسة مقيدة بانتهاء الاشتراك +7 أيام · Parental PIN: PBKDF2-210k · R8 مفعّل.

## 2. EPG — توسعة شاملة (تشغيلي)

بعد نجاح جولة 6 (0 فشل، +148k برنامج)، وسّعنا `EPG_EXTRA_SOURCES` من 8 إلى **37 مصدر epgshare01** مرتبطة بمجموعات الكتالوج الفعلية (إسرائيل 444 قناة، أفريقيا 407، البرازيل 203، الهند 197، التشيك 170، كندا 171، تايلند/فلبين/إيطاليا/إسبانيا/برتغال/بولندا/هولندا/تشيكيا/بلقان/يونان/نمسا/سويسرا/بلجيكا/أيرلندا/تركيا/مكسيك). كل الروابط مفحوصة 200/206 قبل الإضافة. نسخة احتياطية من env قبل التعديل.

## 3. النتائج المقيسة

| المقياس | جولة 5 | جولة 6 | **جولة 7** |
|---|---|---|---|
| برامج EPG في القاعدة | 120,925 | 268,594 | **860,842** |
| معرّفات أدلة متاحة | — | 2,811 | **9,623** |
| مصادر مكتشفة / فاشلة بآخر تحديث | 13 فاشل | 45 / 0 | **74 / 1** (إيطاليا — مؤقت) |
| قنوات لها معرّف EPG | 3,903 (23.4%) | 4,633 (27.8%) | **5,099 (30.5%)** |
| **قنوات تحلّل لبرامج حيّة** | ~2,831 (~17%) | 3,316 (19.9%) | **3,991 (23.9%)** |

- migration `0013 --commit` بالمحلل المعمّم + الأدلة الجديدة: **+672 قناة** إضافية بعد النشر (beIN 73%، FR 797، MBC الأساسية، Alkass…).
- المتبقي بنيوي وموثق: OSN/Shahid/STC + قنوات REPLAY/24-7/LQ — لا أدلة عمومية لها.

## 4. النشر

- `deploy-production.sh --apply` بعد ترقيع `/opt/dzhoot/server` إلى `e002f2f` (نسخة mongodump تلقائية + checksums مطابقة للمستودع).
- كل الحاويات healthy بعد النشر.

## ملاحظات مفتوحة

1. الترقيات الكبرى (dependabot #98/#99/#102/#105–#108) — هجرة يدوية.
2. مصدر EPG عربي مرخّص لـ OSN/Shahid/STC — قرار تجاري للمالك.
3. فشل `epgshare-italy` في آخر تحديث — يُعاد تلقائيًا في الدورة المجدولة.
4. التحديث المجدول يلتقط المصادر الـ37 كل 6 ساعات.
