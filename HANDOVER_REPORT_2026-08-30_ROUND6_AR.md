# تقرير التسليم — جولة استكمال 2026-08-30 (السادسة)
**النطاق:** رفع تغطية EPG (أهم بند متبقٍّ) — مصادر جديدة + إصلاح محلل الأسماء + نشر · **الحالة: ✅ مكتملة — الإنتاج على `7de144f`**

---

## ملخص تنفيذي

المشكلة الموثقة منذ 2026-08-23: «تغطية EPG ‏~9% + 13/50 مصدر فاشل (iptv-epg.org يرد 404)». التشخيص الجديد:

1. **iptv-epg.org ليس ميتًا بالكامل** — الجذر `/files` يرد 404 لكن ملفات الدول تعمل عبر تحويل 302. **15 دولة ميتة فعلًا** (404): `dz qa ma tn jo iq uk ie pk ir ke mn mu cm` — بينها **كل المغرب العربي وقطر** (قطر = دليل beIN الأساسي).
2. **epgshare01.online حيّ ويُحدَّث يوميًا** وبنفس نمط المعرّفات (`Name.country`). وُجدت بدائل كاملة: `AE1` (813 قناة/69k برنامج — فيها **MBC كاملة + Rotana + OSN**)، `EG1`، `FR1` (574 قناة)، `UK1`، `DE1`، `ALJAZEERA1`.
3. **خلل في `epgIdName()`**: قائمة لاحقات ثابتة من 24 رمزًا — اللاحقات الجديدة (`.eg` مثلًا) لم تُجرَّد، فمعرّف مثل `MBC.MASR.2.eg` لم يطابق قناة «MBC MASR 2» أبدًا.

## ما نُفّذ

### تشغيلي (الإنتاج)
- `EPG_EXTRA_SOURCES` في `/etc/dzhoot/.env.production`: من مصدرين إلى **8 مصادر epgshare01** (أُضيفت ALJAZEERA1/AE1/EG1/FR1/UK1/DE1 إلى TR1/BEIN1 القائمين). نسخة احتياطية: `.env.production.bak-20260830T230206`.
- إعادة إنشاء api+scheduler لالتقاط المصادر → تحديث EPG يدوي: **253,226 برنامجًا من 45 مصدرًا، 0 فشل، 128 ثانية** (بلغ AE1 سقف 50k برنامج/مصدر).

### كود — PR #127 (مُدمج squash `7de144f`)
- `epg-id-resolver.ts`: `epgIdName()` يجرد **أي** لاحقة دولة من حرفين + `.bein`/`.com` — بدل القائمة الثابتة. سلوك اللاحقات القديمة ثابت.
- اختبار انحدار يغطي `.eg`/`.ae`/`.il`/`.qa` ومعرّفات بلا لاحقة. CI أخضر كاملًا (backend + CodeQL + secret guard؛ android/frontend تُخطّيا بفلاتر المسارات).

### نشر + ترحيل
- نشر عبر `deploy-production.sh --apply` (mongodump + بناء) — الإنتاج الآن على `7de144f`، كل الحاويات healthy.
- إعادة تشغيل migration `0013 --commit` بالمحلل الجديد: **923 قناة رُبطت** (غالبيتها فرنسية/أوروبية: CANAL.J.fr، France 2-5، Eurosport 1/2.de…).

## النتائج المقيسة

| المقياس | قبل | بعد |
|---|---|---|
| برامج EPG في القاعدة | 120,925 | **268,594** |
| قنوات لها معرّف EPG | 3,903 (23.4%) | **4,633 (27.8%)** |
| قنوات تحلّل لبرامج حيّة فعلًا | ~2,831 | **3,316 (19.9%)** |
| فشل مصادر في آخر تحديث | 13 | **0** |

حسب المجموعة: **AR|BEIN 503/693 (73%)** — AR|MBC 46/191 (النواة MBC 1-4/Action/Drama مغطاة؛ المفقود REPLAY/PREMIUM) — AR|EGYPT 52/162 — FR 697/2414.

## حدود صادقة (لماذا لا ترتفع التغطية أكثر)

- **OSN (170 قناة، 0%)**: لا يوجد أي معرّف OSN تفصيلي في أي دليل عمومي (`osn.tv.ae` فقط). يحتاج مصدر EPG مرخّص من OSN.
- **SHAHID (320) وSTC (284)**: محتوى حصري بلا EPG عمومي إطلاقًا.
- بقايا MBC/EGYPT: قنوات REPLAY/حلقات 24/7 ونسخ LQ مكررة — لا أدلة لها، والمطابقة التقريبية ممنوعة عمدًا (تُظهر جداول خاطئة).

## ملاحظات مفتوحة (لجولات لاحقة)

1. الترقيات الكبرى (dependabot #98/#99/#102/#105–#108) — تحتاج هجرة يدوية.
2. قاعدة `epgsourceoverrides` تحتفظ بسجل 52 مصدر iptv-epg.org قديم (تاريخي؛ الاكتشاف الحالي لا يولّدها لغياب قنوات بلاحقات دول) — تنظيف تجميلي إن رغب المالك.
3. الجدول الزمني التلقائي للـ EPG يلتقط المصادر الثمانية منذ إعادة تشغيل scheduler.
4. القرار القانوني للمصادر — للمالك.
