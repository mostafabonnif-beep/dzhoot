# تقرير فحص شامل — DZ HOOF (2026-09-09): ما ينقص وما يلزم

> **النطاق**: فحص حي للإنتاج (VPS `5.135.79.221`) + المستودع + قاعدة البيانات + CI.
> الإصدار المنشور وقت الفحص: `fc80dba` (PR #205) — الحاويات الست healthy، CI على main أحمر (انظر §1).

---

## 🔴 أولاً — حرج: يلزم تدخّل فوري

### 1. ثغرة Next.js حرجة (CI على main أحمر الآن)
- آخر CI على main **فاشل** بسبب تقرير ثغرات جديد:
  - `next 16.0.0–16.3.2` — **critical**: RCE في Image Optimization (ملفات AVIF) + RCE على استضافة Windows — الحزمة الحالية `^16.3.1`.
  - `multer ≤2.2.0` — **high**: 4 ثغرات DoS (رفع ملفات).
  - `morgan`, `nodemailer`, `qs` — moderate.
- **الحل**: `npm audit fix` (كلها لها إصلاح متاح) أو ترقية `next` إلى `≥16.3.3` + `multer` إلى آخر إصدار، ثم دفع فرع `fix/security-deps` — CI يرجع أخضر.
- ملاحظة: PRs dependabot المفتوحة (#188–#200) لا تغطي هاتين الحزمتين مباشرة.

### 2. التنبيهات معطّلة فعليًا (`alertingConfigured: false`)
- `/health` يفصح أن التنبيهات **غير مكتملة**: `ALERT_WEBHOOK_URL` فارغ في `.env.production`
  (يوجد `ALERT_TELEGRAM_BOT_TOKEN` و`ALERT_CHAT_ID` لكن مسار الصحة يعتمد webhook/قاعدة البيانات).
- أي مصدر يسقط أو خدمة تتعطل — **لن يصلك إشعار**. يجب ضبط webhook أو إكمال مسار Telegram.

### 3. القرص عند 80% (61G من 77G)
- 21.4GB صور Docker قديمة قابلة للاسترجاع (83% من استهلاك Docker) + 3.7GB build cache.
- **الحل**: `docker image prune -a --filter "until=168h"` + `docker builder prune` (يحرّر ~20GB فورًا)
  ثم جدولة تنظيف أسبوعي. بدونها، امتلاء القرص سيوقف Mongo/النشر خلال أسابيع.

---

## 🟠 ثانيًا — مهم: ينقص من جودة الخدمة

### 4. وضع المصادر هشّ — مصدر نشط واحد فقط من 3
| المصدر | الحالة | التحقق | مرايا |
|---|---|---|---|
| neo 4k (احتياطي) | **Active** ✅ | verified | 1 |
| Business Cloud NEO | **Inactive** ⚠️ | verified | 1 |
| MIBOX | **Inactive + degraded** ⚠️ | غير مرئي للزبائن | 0 |
- 17,585 خريطة failover موجودة لكن فعليًا كلها معلّقة على مصدر واحد.
- **يلزم**: إعادة تفعيل/فحص NEO وMIBOX أو حذف المعطّل منها، وإضافة مرآة لـ MIBOX.

### 5. تغطية EPG: 54% فقط + نصف المصادر فاشلة
- 9,805 قناة من 18,112 نشطة لها EPG. آخر تحديث: **39 مصدر EPG فاشل من 78**.
- مهمة `epg-rematch` تعمل، لكن المصادر الفاشلة تحتاج مراجعة (روابط ميتة/محظورة).

### 6. المنتجات المبنية غير المفعّلة (استثمار مهدور)
- **الموزعون**: بوابة موزعين كاملة (الأجزاء 3/6/7) لكن **0 موزعين** في القاعدة.
- **الدفع**: صفحة `/buy` موجودة، CinetPay/Chargily جاهزان كوديًا لكن **بلا مفاتيح** → `payments: 0`.
- **التسجيل**: نظام تسجيل كامل (`/admin/recordings`) لكن **0 تسجيلات** — يحتاج تأكيد أنه مفعّل/يعمل.

### 7. صفحة VOD غير موجودة لمستخدمي الويب
- `/user/vod` → 404. الأفلام/المسلسلات (235K فيلم، 58K مسلسل) متاحة فقط عبر تطبيق Android
  وصفحات الأدمن. مستخدمو الويب لا يرونها — فجوة منتج واضحة (محرك الاستكشاف يعرض أحدث VOD
  لكن بلا صفحة تصفّح).

### 8. ديون dependabot متراكمة (13 PR)
- ترقيات major تحتاج مراجعة يدوية: gradle-wrapper 9.x، firebase-bom 34.x، navigation 2.10،
  room 2.8.4، actions/setup-java 6، express-rate-limit 8.x، lucide-react 1.x.

---

## 🟡 ثالثًا — أمان: ما تبقّى من الجزء 0

| البند | الحالة |
|---|---|
| كلمة مرور الأدمن + 2FA | ✅ منجز |
| ربط توكنات الويب بالمتصفح | ✅ منجز (PR #205، منشور) |
| مفتاح SSH `dzhoof-admin-key` مكشوف تاريخيًا في git | ⬜ تدوير + تنظيف التاريخ (BFG) |
| كلمة مرور root للـ VPS شاركتها في المحادثات | ⬜ يُنصح بتدويرها |
| باسورد لوحة MIBOX (`01020304`) | ⬜ ضعيفة — من لوحة المزوّد |
| Sentry/مراقبة خارجية | ⬜ غير موجودة — الاعتماد على السجلات فقط |

## 🔵 رابعًا — ملاحظات تشغيلية (ليست مشاكل، لكن يجب أن تعرفها)

- **نفق chisel عكسي** على المنفذ 7443 (`dzhoof-chisel.service` — مفتوح للعالم مع auth) +
  redsocks: بنية «home relay» مقصودة لتوجيه الترافيك. احفظ بيانات auth بأمان ودوّرها دوريًا.
- **sshd ثانٍ** على 9001 محصور لشبكة Docker الداخلية (172.18.0.0/16) — جزء من نفس البنية.
- cron `catalog-warm` كل 9 دقائق يُبقي الكتالوج ساخنًا في Redis — سليم.
- شهادة SSL صالحة حتى **2026-11-16** (تجديد تلقائي عبر Caddy) ✅.
- النسخ الاحتياطي: **محلي + بعيد (restic) يعملان يوميًا بنجاح** ✅ — آخر تشغيل ناجح قبل ساعات.
- إصدار التطبيق `v1.1.0` (APK) صدر 2026-09-08 ✅.
- 18,112 قناة نشطة / 31,012 إجمالًا · 16 اشتراكًا نشطًا · 18 مستخدمًا (3 أدمن).

---

## 📋 خطة العمل المقترحة (مرتّبة)

1. **اليوم**: `npm audit fix` (Next.js/multer) → CI أخضر → نشر. *(~30 دقيقة)*
2. **اليوم**: تنظيف قرص Docker (+20GB) + جدولة أسبوعية. *(~10 دقائق)*
3. **هذا الأسبوع**: إكمال التنبيهات (webhook أو Telegram) — بدونها أنت أعمى عن الأعطال.
4. **هذا الأسبوع**: مراجعة المصادر الثلاثة (تفعيل NEO/MIBOX أو تنظيفها) + مراجعة مصادر EPG الفاشلة.
5. **قرارك**: مفاتيح CinetPay/Chargily (تفعيل البيع) · دعوة أول موزعين · تفعيل التسجيل.
6. **عندما تجهز**: تدوير SSH + تنظيف تاريخ git · باسورد MIBOX · تدوير root VPS.
