# تقرير تحسين — DZ HOOF IPTV (الجولة 13.7: بوابة الموزع مجمّدة — السبب الجذري)

**التاريخ:** 2026-08-25 (جولة 13.7)
**النطاق:** اكتشاف وإصلاح السبب الجذري لمشكلة "التنقل من الأعلى إلى الأسفل" في بوابة الموزعين
**الحالة:** ✅ مُنفَّذ ومرفوع (`785ebba`) + منشور على الإنتاج

---

## 1) السبب الجذري (الذي لم تره الجولات 13.5 و13.6)

`globals.css` يطبّق **عمومًا**:

```css
html, body { @apply h-full overflow-hidden; }
```

هذا نمط "التطبيق ذو الشاشة الثابتة": لوحة الأدمن داخل `AppShell` حيث `<main>`
له `overflow-y-auto` — لذلك كانت تعمل. لكن **بوابة الموزع** (`app/reseller/page.tsx`)
و**صفحة دخول الموزع** و**الصفحة الرئيسية** (`/`) تستخدم `min-h-screen` وتدفق
المستند العادي — **بلا أي حاوية تمرير داخلية**:

- المستند `overflow: hidden` → لا تمرير إطلاقًا
- المحتوى تحت أول شاشة (حسابي، سجل الرصيد، تغيير كلمة المرور، دفعاتي)
  **غير مرئي وغير قابل للوصول نهائيًا** (تحقق حي: المحتوى 1242px والوثيقة 844px
  والمستند بلا تمرير — على الهاتف وعلى الحاسوب معًا)

هذا هو "التنقل من الأعلى إلى الأسفل" الذي شكا منه المستخدم منذ جولة 13.5:
الموزع يرى أول شاشة فقط وكل شيء تحتها مفقود.

> لماذا لم يظهر في فحوصاتي السابقة؟ كان الفحص يعتمد على شجرة إمكانية الوصول
> (snapshot) التي تتضمن المحتوى خارج الشاشة + لقطات لأعلى الصفحة فقط — التمرير
> الفعلي لم يكن يُختبر في بوابة الموزع. درس: **أي فحص جوال يجب أن يتضمن اختبار
> تمرير فعلي (سحب CDP) وقياس `scrollHeight` vs `clientHeight`**.

## 2) ما نُفّذ (3 ملفات)

| الملف | التغيير |
|---|---|
| `app/reseller/page.tsx` | الجذر: `min-h-screen` → `h-screen supports-[height:100dvh]:h-dvh overflow-y-auto` — بوابة الموزع أصبحت حاوية تمرير كاملة |
| `app/reseller/login/page.tsx` | نفس المعالجة (أمان للشاشات الصغيرة جدًا) |
| `app/page.tsx` (الصفحة الرئيسية) | نفس المعالجة — كانت مجمّدة أيضًا |

## 3) الاختبارات

- `tsc --noEmit` نظيف ✅ | `eslint` (3 ملفات): 0 ✅ | jest: 6/6 ✅

## 4) النشر والتحقق

GitHub `main` = `785ebba` → مزامنة 3 ملفات (checksums) → نشر رسمي → **تحقق حي
بموزع تجريبي على منظور 390×844: سحب الإصبع ينقل الصفحة من الأعلى للأسفل
والوصول إلى دفعاتي/تغيير كلمة المرور يعمل** ✓

## 5) قاعدة للجلسات القادمة

- `html, body { overflow: hidden }` معمم في globals.css — **أي صفحة جديدة خارج
  AppShell يجب أن يكون جذرها `h-screen supports-[height:100dvh]:h-dvh
  overflow-y-auto`** (وليس `min-h-screen`).
- **فحص الجوال يشمل دائمًا**: قياس `scrollHeight > clientHeight` للوثيقة +
  سحب لمس فعلي — لا تكتفِ بـ snapshot/layout.
