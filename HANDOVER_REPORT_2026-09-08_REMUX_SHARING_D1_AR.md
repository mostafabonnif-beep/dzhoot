# تقرير جولة — DZ HOOF (2026-09-08: D1 — مشاركة جلسات الـ remux لكل قناة)

> **السياق**: بعد إصلاح «تقف القنوات» (PR #209 + الـ home relay) طلب المالك
> توسيع السعة: «لو كان يستوعب 20 يبقى يستوعب 100». القرار: تنفيذ **D1** من
> بنك الأفكار (كاش/مشاركة على مستوى البث) بالطريقة الأنسب للستاك الحالي —
> مشاركة عملية ffmpeg واحدة لكل قناة بدل عملية لكل مشاهد.

## المشكلة
كل مشاهد ويب لقناة http-only كان يشغّل ffmpeg مستقل → جلب مستقل من المزوّد
وعبر الـ relay لكل مشاهد:
- 20 مشاهد = 20 جلب = يخنق الـ relay المنزلي وحساب المزوّد
- `MAX_HLS_REMUX` (6) كان سقفًا على **المشاهدين** فعليًا

## الحل (PR #211 → `35fc6bbf` منشور)
- جلسة ffmpeg **مشتركة لكل upstream stream**: كل التوكنات اللي بتحل لنفس
  القناة تنضم لنفس الجلسة وتتخدم من نفس نافذة HLS على القرص (قراءات محلية).
- `MAX_HLS_REMUX` بقى يحسب **العمليات (= القنوات المختلفة)**:
  100 مشاهد على قناة = **جلب واحد + stream واحد على الـ relay + خانة واحدة**.
- مصادقة كل مشاهد في الـ route **ما تغيرتش** — المشاركة للوسائط فقط (ما يعادل CDN).
- الـ sweep يطرد الخامل، والـ ffmpeg يُقتل فقط مع **آخر** مشاهد، وانقطاع
  المزوّد ينهي الجلسة المشتركة ويعيد تشغيلها مع أول طلب تالٍ.

## التحقق
| البوابة | النتيجة |
|---|---|
| اختبارات الوحدة الجديدة (6) | مشاركة spawn واحد، قنوات مختلفة منفصلة، معنى السقف، القتل عند آخر مشاهد، عزلة الخمول، إعادة التشغيل ✅ |
| الـ suites المرتبطة | 28/28 ✅ |
| typecheck / lint | نظيف ✅ |
| CI (GitHub) | أخضر ✅ |
| **تحقق حي بعد النشر** (جوه الحاوية، كود منشور، ffmpeg حقيقي) | توكنان لنفس القناة: `SAME_DIR: true` + `PROC_SAME: true` + عدّاد العمليات **= 1**، وبعد التنظيف 0 ✅ |

## الأثر
- سعة الـ remux للقنوات الحية (المباريات) لم تعد محدودة بعدد المشاهدين بل
  بعدد **القنوات المتزامنة** — مكمل لإصلاحات اليوم:
  1. mapping https (PR #209): 6,404 قناة direct بلا ضغط
  2. home relay: خروج سكني للـ http-only
  3. D1 (هذا): جلب واحد لكل قناة مهما كان عدد مشاهديها
- الخطوة المكملة المقترحة (من المالك): سؤال المزوّد عن حد الاتصالات
  المتزامنة للحساب وتفعيل https على نطاق `.com` — يكملان المعادلة.

## ملفات
- `server/backend/src/services/hls-remux-service.ts` (إعادة هيكلة المشاركة)
- `server/backend/src/services/hls-remux-service.test.ts` (جديد)
