# تقرير تنفيذ المرحلتين 3 و4 — الاحترافية والتشغيل (جلسة 2026-08-25 مساءً)

> تنفيذ خارطة الطريق `PROFESSIONALIZATION_ROADMAP_AR.md` — المرحلة 3 (الوجه
> الاحترافي) والمرحلة 4 (التشغيل). منشور: `v1.0.1-20260825T194539Z` — main = `501f158`.

---

## ✅ 3.1 تنظيف الكتالوج — منجز

| البند | النتيجة |
|---|---|
| **أسماء القنوات** | 17,004 اسماً نُظّفت (3 تمريرات): `###### RELAX ᴿᴬᵂ ######` → `RELAX`، `FR: COMEDY CENTRAL ᴴᴰ` → `COMEDY CENTRAL HD`، `CA: TSN 5 ⁽ᴮᴷ⁾ ᴿᴬᵂ` → `TSN 5` |
| **العلامات** | صفر متبقي من: `####`، `ᴿᴬᵂ`، `⁶⁰ᶠᵖˢ`، `⁽ᴮᴷ⁾`، بادئات FR/DZ/CA/UK/US/AR/NM/PRIME... |
| **المحفوظ** | العلامات الحقيقية سليمة: BEIN SPORTS، WWE RAW، OSN MOVIES (OSN: → OSN) |
| **التقنية** | `utils/catalog-name-cleaner.ts` (NFKC + قوائم منضبطة + اختبارات) + ترحيلة `0012` |
| **الموثّق** | 686 مجموعة أسماء مكررة بعد التنظيف (لجولة دمج قادمة) + 2 قناتان ميتتان (يُتجاوزان تلقائياً) |

## ✅ 3.2 EPG عربي — منجز (المشكلة الحقيقية كانت المطابقة لا الجلب)

- خط الجلب كان يعمل (122k+ برنامجاً) لكن **قناة واحدة فقط** كانت مرتبطة بالأدلة!
- ترحيلة `0013`: ربطت **1,887 قناة** بمعرفات موجودة فعلاً في الأدلة:
  - beIN SPORTS 1-7 → `beIN_SPORTS{n}_DIGITAL_Mono_AR.bein` (**803 قناة**)
  - ALKASS 1-8 → `Alkass_{n}_AR.bein` (36) • AL JAZEERA عربي/إنجليزي (31)
  - مطابقة دقيقة عامة: CARTOON NETWORK، DISNEY CHANNEL، TIJI، GULLI، RTL9... (244)
- **تحقق حي**: `/epg/<code>/json` يخدم **1,025 قناة / 9,594 برنامجاً** — BEIN SPORTS 3 HD
  يعرض جدول مباريات حقيقي (Málaga vs Deportivo)، الكأس والجزيرة ممتلئان.
- المتبقي (مصادر لا توفرها الأدلة الحالية): قنوات جزائرية محلية (Echorouk/Ennahar) + beIN MAX.

## ✅ 3.3 صفحة الهبوط + اشترِ + بطاقات QR — منجز

- **`/`**: صفحة زبون احترافية (RTL) — مميزات، 3 خطوات، أسعار مباشرة من القاعدة،
  JSON-LD `TVService`، CTA واتساب.
- **`/buy`**: باقات بأسعار حقيقية (شهري 500 / 6 أشهر 2500 / سنوي 4500 دج) + زر
  «اطلب عبر واتساب» لكل باقة + رابط «فعّل بالكود» + سياق محل عبر `?shop=<id>`.
- **`/api/v1/shop/plans`** (عام): الباقات المدفوعة + العلامة + واتساب رئيسي + معلومات
  الموزع عند `?shop=` (المطابقة عبر `status:'Active'`).
- **لوحة الأدمن**: زر «بطاقة QR» بجانب كل موزع → بطاقة A6 قابلة للطباعة
  (العلامة + رمز QR → `/buy?shop=<id>` + اسم المحل + واتسابه).

## ✅ 4.4 تحصين الخادم — منجز

- **fail2ban**: سجن `dzhoof-http` جديد (6×401/403 خلال 60 ثانية → حظر ساعة) +
  سجل وصول Caddy لملف (صيغة JSON — البناء المخصص بلا common_log) + سجن sshd كان نشطاً.
- **تنظيف القرص**: 80% → **24%** (حررت ~35GB: صور Docker القديمة + كاش البناء — 76% كانت
  قابلة للاسترجاع). أبقيت `current` فقط (التراجع = إعادة بناء من git).
- Caddyfile: ترويسات أمان كانت موجودة (HSTS/CSP...) — أضفت السجل فقط.
- SSH سليم أصلاً: بلا كلمات مرور + root بمفتاح فقط + UFW نشط.
- **متبقي بيد المستخدم**: تبديل كلمة مرور admin (مكشوفة) + تفعيل 2FA من اللوحة + تدوير root/github.token.

## ✅ 4.2 تنبيهات — منجز (ينتظر رابط webhook)

- مهمة **`system-disk-watchdog`** (كل 12 دقيقة): إنذار عند 80%، حرج عند 90%،
  تخميد + هبوط، عبر نفس قناة تنبيهات المصادر (`alert_webhook_url`).
  ✓ مجدولة فعلاً: «System Disk Watchdog scheduled every 0.2h» (13 مهمة الآن).
- قناة التنبيه جاهزة (Discord/Slack) — **تحتاج فقط رابط webhook منك** (الإعدادات).

## ✅ 4.3 نسخ احتياطي خارجي — السكربت جاهز (ينتظر مفاتيح)

- `scripts/backup/restic-backup.sh`: mongodump + الأسرار (env/token/compose/Caddyfile)
  → مستودع restic مشفّر (B2/S3) + احتفاظ 7 أيام/4 أسابيع/3 أشهر + ping اختياري
  + **إجراء استعادة كامل موثّق** + تثبيت cron يومي 03:30.
- خامل حتى تعطيني: `RESTIC_REPO` + `RESTIC_PASSWORD` + مفاتيح S3 (تُضاف إلى `.env.production`).

---

## الأرقام النهائية (تحقق حي)

- قائمة الزبون: 35,873 رابطاً على خادمنا، 16,987 شعاراً عبر وسيطنا، صفر روابط مزود خام
- التشغيل: توكين → مانيفست 3,078B بمقاطع عبر `/segments/` ✓
- قاعدة: 16,640 قناة (42 فقط بعلامات متبقية — فعاليات/حالات خاصة)، 128 كوداً، 263 خريطة تبديل
- القرص 30%، 6 حاويات سليمة، الاختبارات 335/335 + الواجهة تبني نظيفاً

## الجلسة القادمة (اقتراح)

1. **بوابة دفع CinetPay** (المرحلة 1 — أكبر عائد؛ تحتاج حساب تاجر + مفاتيح API)
2. **مشغّل ويب iPhone** (`/watch` بـ HLS.js — المرحلة 2)
3. إعدادات المتجر من اللوحة: `shop_brand` + `shop_whatsapp` (AppSetting) — اختياري الآن
4. دمج الـ 686 قناة المكررة (تقرير 0012) + مطابقة EPG للقنوات الجزائرية عند توفر دليل
5. مفاتيح restic + رابط webhook + كلمتا المرور (من المستخدم)
