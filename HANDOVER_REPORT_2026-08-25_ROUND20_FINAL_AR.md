# تقرير التسليم النهائي — DZ HOOF IPTV (جلسة 2026-08-25 — الجولات 17 → 20)

> **اقرأ هذا أولًا.** الوثيقة الوحيدة التي تحتاجها لبدء العمل. تغطي: الوصول، كل ما
> أُنجز اليوم (تحصين الفلوس، المصدر الاحتياطي التلقائي، إخفاء المصادر، ميزات الموزعين)،
> الحالة الحالية المُتحقق منها، ما تبقى بالأولوية، الأوامر السريعة، والمزالق.
> ⚠️ **توجد جلسة موازية تعمل على نفس الريبو — ادمج قبل أي push** (القسم 8).

---

## 1. الملخص التنفيذي

| الجبهة | النتيجة |
|---|---|
| **المستودع** | ✅ `main` = `09c0cbf` — نظيف ومتطابق مع الخادم (10/10 sha256) |
| **الإنتاج** | ✅ `v1.0.1-20260825T175550Z` — 6 حاويات (api/frontend healthy) |
| **الفلوس** | ✅ كود واحد = اشتراك واحد (ادعاء ذري)؛ استرجاع رصيد لا يلغي كودًا مُفعّلًا؛ تراجع كامل عند فشل التوليد |
| **المصدر الاحتياطي** | ✅ **مُفعّل ومنشور**: ottstreambox (263 خريطة تبديل) — **مُختبَر حيًا**: أساسي→احتياطي→استرجاع تلقائي |
| **إخفاء المصادر** | ✅ **الزبون/الموزع لا يرى أي مصدر**: كل البث بروكسي عبر `iptv.ld-11.net` + الشعارات عبر وسيطنا (صفر روابط مزود خام) |
| **الميزات الموازية** | ✅ دُمجت: «عملائي» للموزعين + معلومات الزبائن + مدة مخصصة + كشف حساب |
| **الاختبارات** | ✅ باك-إند **329/329** + واجهة 6/6 + tsc + eslint 0 أخطاء |

---

## 2. الوصول (ثابت)

- **SSH**: `dzhoof-admin@5.135.79.221` بمفتاح ed25519 —
  **`/home/user/.workspace/keys/dzhoof-admin-vps.key`** (chmod 600). sudo بلا كلمة مرور.
- **GitHub**: `mostafabonnif-beep/dzhoot` — PAT على الخادم `/etc/dzhoot/github.token`
  (يملك actions:read + workflow — يُستخدم للدفع وسحب artifacts وتشغيل release).
- **الإنتاج**: `/opt/dzhoot/server` (نسخة ملفات — **ليست git checkout**) + الأسرار
  `/etc/dzhoot/.env.production` (chmod 600) + `/etc/dzhoot/github.token`.
- **لوحة الأدمن**: `https://iptv.ld-11.net/admin` — `admin` / (كلمة المرور مكشوفة في
  المحادثة — **يجب تغييرها**).
- **بوابة الموزع**: `https://iptv.ld-11.net/reseller/login`.
- **قاعدة البيانات**: حاوية `dzhoof-mongodb` — القاعدة **`dzhoof-iptv`** دائمًا
  (mongosh يتصل `test` افتراضيًا — حدّد القاعدة دائمًا).

---

## 3. ما أُنجز في هذه الجلسة

### الجولة 17 — تحصين الفلوس + إصلاحات اللوحة (منشور)
- **سباق التفعيل**: `redeemCode` كان check-then-act (كود مدفوع → اشتراكان) — الآن
  ادعاء ذري `UNUSED→ACTIVATING` + ردّ الكود عند أي فشل + استرداد ACTIVATING العالق يوميًا.
- **استرجاع الرصيد/انتهاء الصلاحية**: كتابة محروسة بـ `status:'UNUSED'` + الرصيد من
  `modifiedCount` الفعلي — كود يُفعَّل في نفس اللحظة لا يُلغى ولا يُعاد رصيده.
- **توليد أكواد الموزع**: تراجع كامل عن الخصم عند أي فشل + إعادة محاولة E11000 (batchNumber).
- كلمة SMTP لا تُصدَّر/لا تُردّ • حذف محل مدين ممنوع • سعر سالب 400 • ObjectId في حذف
  القنوات • حارس إعادة إرسال الإشعارات • حدود `user|ip` للموزعين + حد لتغيير كلمة المرور •
  `serverUrl` كامل لـ Xtream + PATCH له • فهرس `{status, activatedAt}` (ترحيلة 0011) •
  ناقل SMTP يُعاد بناؤه • تقنين expireStale على القراءة.
- **الواجهة**: حلقة التسجيلات اللانهائية • قَصّ الصفحة بعد الحذف • CSV مع فلتر المحل •
  أزرار ConfirmDialog عربية • تحقق الباقات • تأكيد التعطيل • RTL • تنبيه حدّ 500 في
  «اختبار الكل» • إرشاد صفحة الإصدارات.

### الجولة 18 — «المصدر الاحتياطي التلقائي» (فكرة المستخدم — مُفعَّلة ومنشورة ومُختبَرة)
- `ChannelFailoverMap` (خريطة جانبية قناة→مصدر احتياطي، لا تمسّ الكتالوج).
- `source-failover-service`: كاش صحة + `isSourceDown` + `getFailoverTarget` + **watchdog
  60s** (المصادر المباشرة تُفحص ببثّها الفعلي، الاحتياطي ببثّ مطابَق، عينة 3 قنوات،
  تثبيت استرجاع بفحصين متتاليين ضد التقلّب) + تنبيهات انتقال الحالة + auto-match
  (قاموس أسماء + تشابه ضبابي + حارس القنوات الأجنبية + فلتر فئات).
- `tv.js /playback-token`: التبديل عند سقوط الأساسي (catch-up لا يُحوَّل أبدًا) +
  علامة `source:'backup'` في الرد.
- **مُفعَّل**: مصدر `Backup Maghreb (ottstreambox)` (اعتماديات مشفرة AES-256-GCM) +
  **263 خريطة** (فئات ALGERIE/MAROC/TUNISIE/BEIN/ARABIC) + تنظيف 10 خرائط خاطئة.
- **اختبار E2E حي ناجح** (مرتين): NEO سليم → أساسي؛ حقن انقطاع → احتياطي (مقطع
  video/mp2t حقيقي 6.3MB)؛ watchdog → NEO verified → الجلسة الجديدة ترجع له.

### الجولة 19 (جلسة موازية — دُمجت ونُشرت)
- ميزات الموزعين: «عملائي» (تجميع الزبائن + انتهاء الاشتراكات + تجديد واتساب) •
  معلومات الزبون + مدة مخصصة + كشف حساب لأكواد الموزعين • تحصين محاسبي للوحة.

### الجولة 20 — إخفاء المصادر (طلب المستخدم: «الزبون والموزع ما يجبش يعرفوا المصدر»)
- **اكتشاف**: direct playback كان يسلّم الزبون رابط المزود **مع اليوزر/الباسورد في
  المسار** (302) — أي حد يأخذ المصدر مباشرة. والشعارات من سيرفرات المزودين
  (51.158.145.100، cmshulk) ظاهرة في M3U/JSON/XMLTV.
- **الإصلاح**: `ALLOW_DIRECT_PLAYBACK` صار مفتاحًا رئيسيًا حقيقيًا (بدونه كل التوكينات
  بروكسي — tv.js + User.ts) و**انقلب إلى `false` في الإنتاج**؛ وسيط شعارات
  `/api/v1/tv/logo?url=` (SSRF + كاش 24h) مع إعادة كتابة في M3U وJSON وXMLTV.
- **تحقق حي**: قائمة الزبون = 33,241 رابط لخادمنا + 16,602 شعار عبر الوسيط +
  **صفر روابط مزود خام**؛ التوكين → مانيفست مرحّل بمقاطع عبر `/segments/`؛ التبديل
  الاحتياطي يعمل في البروكسي (مانيفست 2079B عبر خادمنا).

---

## 4. الحالة الحالية (مُتحققة 2026-08-25 مساءً)

- الإنتاج = main `09c0cbf` (10/10 sha256 للملفات المفتاحية).
- 6 حاويات سليمة؛ القرص ~67%.
- **المصدران verified**: NEO (أساسي) + Backup Maghreb/ottstreambox (احتياطي).
- 263 خريطة تبديل مفعّلة؛ 128 كود تفعيل؛ 16,640 قناة؛ 8 مستخدمين.
- التحديث الداخلي 1.0.10 حي (سجل AppVersion + GitHub latest).
- `ALLOW_DIRECT_PLAYBACK=false` (البروكسي إجباري — إخفاء المصادر).

---

## 5. ما تبقى (بالأولوية)

### 🔴 أمني (يحتاج المستخدم)
- [ ] تغيير كلمة مرور **admin** (الإعدادات ← تغيير كلمة المرور) — مكشوفة في المحادثة.
- [ ] تغيير كلمة مرور **root** عند المزود (OVH) + مراجعة صلاحيات github.token.

### 🟠 تجاري/نمو (أكبر عائد)
- [ ] **بوابة دفع** (CinetPay/Paymee/Stripe) — صفحة «اشترِ الآن» + تفعيل فوري.
- [ ] **مشغّل ويب** (iPhone/كمبيوتر) — صفحة `/watch` بـ HLS.js.
- [ ] **بطاقات QR للمحلات** (طباعة بطاقات قابلة للمسح من الدفعات).
- [ ] مفاتيح FCM + Brevo SMTP + webhook حقيقي (الواجهات جاهزة؛ webhook يُفعَّل من
      الإعدادات — الـ watchdog للمصادر يرسل تنبيهات السقوط/الشفاء).

### 🟡 كتالوج (احترافية — مهم لدخول السوق)
- [ ] **تنظيف أسماء القنوات** من علامات المزود (`##### AFRICA #####`، `AFR|`، `Dz|`،
      `ALG:`...) — ظاهرة للزبون وتلمّح للمصدر وتبدو غير احترافية.
- [ ] حذف القنوات الميتة (~7-10%) + EPG عربي + beIN SPORTS 5/MAX.

### 🟢 تشغيلي
- [ ] **كاش شرائح HLS** (D1) — البروكسي الآن يستهلك بندويث الخادم؛ الكاش = جلب واحد
      من المزود لآلاف المشاهدين (nginx proxy_cache أو كاش داخل التطبيق).
- [ ] نسخ احتياطي خارجي (restic) + اختبار استعادة.
- [ ] خطة عمل اللوحة المجزأة: `WORK_PLAN_ADMIN_PARTS_AR.md` (من الجلسة الموازية).

---

## 6. الأوامر السريعة

```bash
# SSH
ssh -i ~/.workspace/keys/dzhoof-admin-vps.key dzhoof-admin@5.135.79.221

# الصحة
curl -s https://iptv.ld-11.net/health/ready
curl -s 'https://iptv.ld-11.net/health?details=true' | head -c 400

# النشر: مزامنة tar (احذر المسار!) ثم deploy
tar --exclude=node_modules --exclude=.git --exclude=dist --exclude=.next --exclude=coverage -czf /tmp/dz.tar.gz server
scp -i ~/.workspace/keys/dzhoof-admin-vps.key /tmp/dz.tar.gz dzhoof-admin@5.135.79.221:/tmp/
ssh ... 'sudo tar -xzf /tmp/dz.tar.gz -C /opt/dzhoot && sudo chown -R dzhoof-admin:dzhoof-admin /opt/dzhoot/server'
# ⚠️ ثم طابق الملفات اليتيمة (انظر المزالق) ثم:
ssh ... 'cd /opt/dzhoot/server && sudo bash scripts/deploy/deploy-production.sh --apply'   # ~10 دقائق

# GitHub push (من الخادم PAT)
TOKEN=$(ssh ... 'sudo cat /etc/dzhoot/github.token')
git push "https://x-access-token:$TOKEN@github.com/mostafabonnif-beep/dzhoot.git" main

# Mongo (حدد القاعدة دائمًا!)
sudo docker exec dzhoof-mongodb mongosh dzhoof-iptv

# Watchdog يدوي + صحة المصادر (من اللوحة أو):
# POST /api/v1/admin/xtream-sources/watchdog/run
# GET  /api/v1/admin/xtream-sources/:id/health
```

---

## 7. المزالق (تعلمتها اليوم — مهمة)

1. **tar sync لا يحذف الملفات اليتيمة** — ملفات قديمة على الخادم (مثل
   `reseller-custom-codes.test.ts` القديم أو `.bak`) كسرت `docker build` (tsc).
   بعد كل فك حزمة: `comm -13` بين قائمة ملفات الريبو وقائمة الخادم تحت
   `backend/src frontend/src packages scripts` واحذف اليتيم.
2. **فك الحزمة**: الحزمة جذرها `server/` → `-C /opt/dzhoot` (لا `-C /opt` — أنشأت
   `/opt/server` بالغلط مرة).
3. **compose لا يمرر المتغيرات إلا المدرجة في بلوك `environment:`** — أي متغير جديد
   (مثل ALLOW_DIRECT_PLAYBACK) يجب أن يكون في compose أيضًا.
4. **`ALLOW_DIRECT_PLAYBACK` مفتاح الإخفاء**: انقلب إلى `false` في
   `/etc/dzhoot/.env.production` — **لا تعكسه** إلا إذا أضفت طبقة CDN/توكن موقّع
   (الزبون سيُعرض على روابط المزود بكلمات المرور).
5. **`-$quantity` في aggregation = null صامت** — استخدم `$subtract: [0, '$quantity']`.
6. **فحص صحة المصادر المباشرة بالبث الفعلي لا بالـ API** — NEO API مسدود من الخادم
   (TLS) بينما بثّه يعمل؛ الفحص بالـ API كان سيُفعّل التبديل الاحتياطي بالغلط دائمًا.
7. **ottstreambox manifest بروابط نسبية بعد 302** — الفحص العميق للمقاطع يفشل إيجابيًا
   كاذبًا؛ استخدم فحص manifest خفيف (200 + #EXTM3U).
8. **تثبيت الاسترجاع**: المصدر العائد من degraded/blocked يحتاج فحصين متتاليين
   ناجحين قبل الرجوع — ضد التقلّب.
9. **المطابقة التلقائية**: استبعد القنوات ذات البادئات الأجنبية (FI:/SE:/UK:/BR:...)
   — كانت تطابق قنوات جزائرية بالغلط (Yle TV1 → ENTV1).

---

## 8. ⚠️ الجلسة الموازية (مهم جدًا)

**يوجد عامل/جلسة أخرى تعمل على نفس الريبو** (نفس اسم المؤلّف «DZ HOOF Agent») وتدفع
ميزات (جولة 19: عملائي/معلومات الزبائن؛ جولة 20: خطة عمل اللوحة). القواعد:
- **قبل أي push**: `git fetch && git merge origin/main` (سيرفض البعيد غير fast-forward).
- **بعد أي merge**: أعد تشغيل الاختبارات (`npx jest`) قبل النشر.
- لا تفترض أن الخادم محدّث بميزاتهم — تحقق sha256 أو انشر بعد المزامنة الشاملة.
- التنسيق المتوقع: كل جلسة تدفع تقريرها (HANDOVER_REPORT_...) في نهاية عملها.

---

## 9. ملاحظات أخيرة

- اعتماديات ottstreambox: يوزر `144881537461` / باسورد `13347510096a34579b40773`
  (مشفّرة AES-256-GCM في القاعدة بمفتاح XTREAM_SECRET_KEY — لا تُكتب في الريبو).
- نسخ احتياطي تلقائي قبل كل نشر في `/var/backups/dzhoot/mongodb/` (نحتفظ بأحدث 12).
- سجل النشر: `/var/log/dzhoof-deploys.log`.
- تقارير الجولات في جذر الريبو: `ADMIN_IMPROVEMENT_REPORT_ROUND{15..19}_AR.md` +
  `IDEA_2026-08-25_BACKUP_SOURCE_FAILOVER_ROUND18_IMPLEMENTATION_AR.md` +
  `WORK_PLAN_ADMIN_PARTS_AR.md`.
