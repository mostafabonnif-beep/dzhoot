# تقرير تحسين — DZ HOOF IPTV (الجولة 14: ديون الزبائن + ديون المحلات)

**التاريخ:** 2026-08-25 (جولة 14)
**النطاق:** ميزتان جديدتان لتتبع الديون — (أ) بوابة الموزع: ديون زبائنه، (ب) لوحة الأدمن: **ديون المحلات عليه** (طلب المستخدم المتكرر)
**الحالة:** ✅ مُنفَّذ ومرفوع + منشور على الإنتاج

---

## 1) المشكلة (من المستخدم)

> "أحد الزبائن أخذ من عندي رصيد ولم يدفع — يجب أن أعرف أنه لم يدفع لي حتى أتذكره"

كرر المستخدم الطلب بعد الجولة الأولى → أدركنا أن السيناريو الحرج هو **صاحب
المنصة (الأدمن) يعطي رصيدًا للمحلات (الموزعين)** وبعضهم لا يدفع — ينسى مَن
عليه. الحل الكامل يغطي الجهتين.

## 2) الحل (أ) — بوابة الموزع: «ديون الزبائن» (نُشرت سابقًا)

تسجيل ديون الزبائن عند الموزع + تذكير واتساب — انظر القسم السابق من هذا التقرير.

## 3) الحل (ب) — لوحة الأدمن: «ديون المحلات عليهم» ⭐ (الأهم للمستخدم)

### تلقائي بالكامل
- **عند منح رصيد لمحل بدون استلام الدفع → يُسجَّل دين تلقائيًا** بقيمة الرصيد
  (الكمية × سعر الجملة) — لا يحتاج الأدمن كتابة أي شيء، ولا يمكن نسيانه.
- في نموذج إنشاء/تعديل المحل: مربع **«تم استلام الدفع لهذا الرصيد ✓»** —
  اتركه فارغًا لتسجيل الدين تلقائيًا، علّمه إذا دفع نقدًا.

### الباك-إند
- نموذج `ResellerCreditDebt` (adminId + resellerId + amount + paidAmount + status + autoFromGrant).
- مسارات جديدة `/api/v1/admin/reseller-debts` (قائمة + ملخص مستحق، إنشاء يدوي،
  تسوية/تعديل، حذف) — معزولة لكل أدمن + مسجلة في سجل النشاط.

### لوحة المحلات (الأدمن)
- قسم **«ديون المحلات عليهم»** أعلى الجدول: بطاقة تنبيه «لديك N دين — مجموع X دج»
  + قائمة مرتبة (غير المدفوع الأقدم أولًا — اللي نسيته يظهر في القمة) + شارات
  (غير مدفوع/جزئي/مدفوع/قديم 14+ يوم) + **زر واتساب برسالة تذكير جاهزة** + تسوية + حذف.
- **لوحة التحكم (الإحصائيات)**: تنبيه كهرماني أعلى الصفحة «ديون غير مسددة على
  المحلات: N — X دج» يفتح صفحة المحلات.

## 4) الاختبارات

- **5 اختبارات جديدة** (admin-reseller-debts): توليد دين تلقائي بقيمة صحيحة،
  عدم التوليد عند `creditPaid`، قائمة + تسوية + ملخص، عزل بين الأدمن، إنشاء/حذف يدوي.
- كامل المجموعة: **263/263** باك-إند + 6/6 واجهة + tsc + eslint نظيفة.

## 5) الملفات المتغيّرة (هذه الدفعة)

```
server/backend/src/models/ResellerCreditDebt.ts            (جديد)
server/backend/src/routes/admin-reseller-debts.js          (جديد — CRUD + ملخص)
server/backend/src/routes/admin-resellers.js               (توليد دين تلقائي عند منح الرصيد)
server/backend/src/server.js                               (تركيب المسار الجديد)
server/backend/src/__tests__/admin-reseller-debts.test.ts  (جديد — 5 اختبارات)
server/frontend/src/app/(dashboard)/admin/resellers/page.tsx (قسم الديون + نافذة + checkbox مدفوع)
server/frontend/src/app/(dashboard)/admin/stats/page.tsx   (تنبيه الديون)
server/frontend/src/components/reseller-debts-alert.tsx    (جديد)
server/frontend/src/components/locale-provider.tsx          (+21 مفتاح)
```

## 6) ملاحظات

- فكرة لاحقة: تقرير شهري بالديون + ربط تلقائي كامل بتدفق منح الرصيد من أي مسار.
- `creditPaid` يُمرَّر من واجهة تعديل المحل؛ الافتراضي «غير مدفوع» = تسجيل دين
  (أمان ضد النسيان).


- يمكن لاحقًا: ربط الديون تلقائيًا بالدفعات/الأكواد، تقرير شهري بالديون، ومشطوبة.
- الهاتف اختياري لكن **بدونه يختفي زر واتساب** — شجّع الموزعين على إدخاله.
