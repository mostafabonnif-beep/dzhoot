# تقرير تحسين — DZ HOOF IPTV (الجولة 14: ديون الزبائن في بوابة الموزع)

**التاريخ:** 2026-08-25 (جولة 14)
**النطاق:** ميزة جديدة — تتبع ديون الزبائن للموزع (طلب المستخدم: "زبون أخذ رصيدًا ولم يدفع — يجب أن أعرف حتى أتذكره")
**الحالة:** ✅ مُنفَّذ ومرفوع + منشور على الإنتاج

---

## 1) المشكلة (من المستخدم — بائع رصيد بالدين)

الموزع يعطي أكواد رصيد لزبائنه **بالدين** (يدفعون لاحقًا). لا يوجد أي نظام
يتذكره **بمَن لم يدفع** — ينسى الديون ولا يعرف مَن عليه مبالغ. طلب:
> "يجب أن أعرف أنه لم يدفع لي حتى أتذكره"

## 2) الحل: قسم «ديون الزبائن» في بوابة الموزع

### الباك-إند
- **نموذج جديد `ResellerDebt`**: `resellerId` + `customerName` + `customerPhone`
  (للواتساب) + `amount` (المبلغ المستحق) + `paidAmount` + `status`
  (UNPAID/PARTIAL/PAID) + `quantity`/`planName`/`note` + `paidAt`.
- **مسارات جديدة** في `routes/reseller.js` (محمية بـ `requireReseller` — كل موزع
  يرى ديون نفسه فقط):
  - `GET /reseller/debts` — قائمة + ملخص `{ outstanding, unpaidCount }`
    (إجمالي المستحق عبر aggregation — لا يعتمد على حد القائمة)
  - `POST /reseller/debts` — تسجيل دين جديد (تحقق: اسم + مبلغ ≥ 0)
  - `PATCH /reseller/debts/:id` — تسوية كاملة/جزئية + تعديل (PAID → `paidAt` الآن)
  - `DELETE /reseller/debts/:id`
- كل عملية مسجلة في سجل النشاط (audit: RESELLER_DEBT_CREATE/UPDATE/DELETE).

### الواجهة (بوابة الموزع)
- **بطاقة ملخص تنبيهية**: «لديك N دين غير مسدد — المجموع X دج» (برتقالية) أو
  «كل شيء مسدد ✓» (خضراء).
- **نافذة «إضافة دين»**: اسم الزبون* + هاتف (واتساب) + المبلغ* + عدد الأكواد +
  الباقة + ملاحظة.
- **قائمة ديون مرتبة بذكاء**: غير المدفوع أولًا **والأقدم أولًا** (اللي نسيته أكثر
  يظهر في القمة) → الجزئي → المدفوع (الأحدث أولًا).
- **شارات**: غير مدفوع (أحمر) / جزئي (كهرماني) / مدفوع (أخضر) + شارة **«قديم»**
  (حمراء) للديون غير المسددة منذ 14+ يومًا + عدد الأيام («منذ 10 أيام»).
- **زر «واتساب»** 🟢 — يفتح محادثة wa.me بهاتف الزبون مع **رسالة تذكير مهذبة
  جاهزة مكتوبة مسبقًا** بالدارجة:
  «السلام عليكم {name}، هذا تذكير بسيط بخصوص المبلغ المتبقي عندي ({amount} دج)…»
- **زر «تسوية»** (تأكيد → مدفوع بالكامل) + زر حذف (تأكيد).

## 3) الاختبارات

- **5 اختبارات جديدة** (`reseller-debts.test.ts`): إنشاء + ملخص صحيح، رفض بدون
  اسم، تسوية كاملة (تخرج من الملخص)، دفعة جزئية + عزل الديون بين الموزعين، حذف.
- كامل المجموعة: باك-إند (258+5=**263**) + واجهة 6/6 + tsc + eslint نظيفة.

## 4) الملفات المتغيّرة

```
server/backend/src/models/ResellerDebt.ts                 (جديد — النموذج)
server/backend/src/routes/reseller.js                     (+4 مسارات ديون + استيراد audit)
server/backend/src/__tests__/reseller-debts.test.ts       (جديد — 5 اختبارات)
server/frontend/src/app/reseller/page.tsx                 (قسم الديون + نافذة + إجراءات)
server/frontend/src/components/locale-provider.tsx        (+19 مفتاح ترجمة portal.debt*)
```

## 5) ملاحظات

- يمكن لاحقًا: ربط الديون تلقائيًا بالدفعات/الأكواد، تقرير شهري بالديون، ومشطوبة.
- الهاتف اختياري لكن **بدونه يختفي زر واتساب** — شجّع الموزعين على إدخاله.
