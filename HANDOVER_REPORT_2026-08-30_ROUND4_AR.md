# تقرير التسليم — جولة استكمال 2026-08-30 (الرابعة)
**النطاق:** مطابقة معرّفات EPG (تصحيح أخطاء + قياس السقف) · تعريب صفحة `/pair` · إصلاح الـ restore drill بعد تفعيل auth · · **الحالة: ✅ مكتملة — الإنتاج على `6a0d9d3` + جاهزية نشر PRs #115/#116/#117**

---

## ملخص تنفيذي

جولة استمرار على البنود المفتوحة من التقرير الثالث. الأهم: **تشخيص واقع التغطية EPG** (15% فعلياً — قريب من السقف العملي لهذا الكتالوج)، واكتشاف وإصلاح **خطأ بيانات حقيقي** (6 قنوات beIN 5 معروضة عليها برنامج beIN 1)، وإصلاح **الـ restore drill** الذي كان سيفشل شهرياً بعد تفعيل auth على Mongo، وتعريب صفحة الاقتران.

## 1. EPG — القياس النهائي وقرار السقف

- **القياس الدقيق**: من أصل 16,691 قناة: 4,060 لها tvgId (24%)، و**2,535 فقط تطابق برامج فعلية (15%)**. الـ 12,631 المتبقية بلا tvgId هي قنوات Relay من Xtream/M3U بأسماء مزخرفة (`ᴿᴬᵂ⭐⚽###`) **لا وجود لها في أدلة XMLTV القياسية** — قِست السقف بمحاكاة مطابقة (12 دليلاً × 3,922 قناة دليل): **11 قناة فقط** يمكن مطابقتها بأمان. الخلاصة: التغطية ~15% قريبة من السقف العملي؛ تحسينها الحقيقي يتطلب أدلة تغطي الكتالوج الجزائري/العربي تحديداً (مصدر جديد)، لا خوارزمية مطابقة.
- ما كان يُحسب 24% في التقارير السابقة = "قنوات لها tvgId"، وليس "قنوات مغطاة ببرامج".

## 2. خطأ beIN المصحح (PR #115)

- **الخطأ**: قنوات "beIN SP⚽RTS 5" / "BEIN SPORT TOD 5" كانت تُوسم بـ **beIN_SPORTS1** (emoji الكرة يكسر التقاط الرقم في migration 0013 فيسقط للرقم 1 صامتاً) → **6 قنوات في الإنتاج تعرض برنامج قناة خاطئ**.
- **الإصلاح**: util جديد `epg-id-resolver.ts` (مطابقة العائلة على الاسم المُنظَّف، معالجة `SP⚽RTS`→`SPORTS`، التقاط رقم لاحق "SPORT TOD 5"، و**عدم السقوط أبداً** للرقم 1 عندما لا يملك الدليل الرقم المطلوب) + تنظيف إضافي للرموز (⚽⭐🅰) + migration 0013 أُعيدت كتابته على الـ resolver مع **مسح** الأوسمة الخاطئة (اسم القناة رقمه ≠ رقم الـ tvgId) بدل تجاوزها للأبد. 10 اختبارات وحدة (بما فيها حالة الانحدار).
- **خطوة تشغيلية تالية**: تشغيل migration 0013 --commit على الإنتاج (ستُمسح الأوسمة الست الخاطئة).

## 3. صفحة `/pair` — تعريب (PR #116)

آخر صفحة عامة إنجليزية — أُضيفت مفاتيح `pair.*` (AR/EN/FR) وربطت الصفحة بـ `useLocale()`: كل الحالات (لا رمز، تسجيل الدخول، جارٍ الربط، نجاح، فشل). بند مؤجل من الجولتين الثانية والثالثة أُغلق.

## 4. الـ Restore drill — كان سيفشل شهرياً (PR #117)

- **الاكتشاف**: بعد تفعيل auth على Mongo (من الجهة الموازية)، الـ cron الشهري (1 من الشهر 04:10) كان سيستخدم mongorestore/mongosh **بلا auth** → فشل أكيد. كما أن مستخدم التطبيق `dzhoof` لا يملك صلاحيات على قاعدة الـ drill.
- **التحقق**: نفّذت drill يدوياً بصلاحيات `dzhoof-admin` (root): استرجاع **304,033 مستنداً عبر 40 مجموعة** من نسخة 03:15 اليومية — الأعداد مطابقة للإنتاج (16,691 قناة / 16 مستخدماً) — ثم حذف قاعدة الـ drill.
- **الإصلاح**: script يدعم `MONGO_AUTH_USER/PASSWORD_FILE` (قراءة من ملف 0600، لا أسرار في سطر الأوامر) مع fallback لبيئة بلا auth. طُبّق أيضاً كإصلاح مباشر في الشجرة النشطة (مع نسخة احتياطية) حتى لا يفشل cron 1 سبتمبر.

## 5. حالة PRs

| PR | القرار |
|----|--------|
| #115 fix(epg) resolver/beIN | ✅ مدمج (بانتظار آخر android job) |
| #116 feat(pair page i18n) | ✅ مدمج (بانتظار آخر android job) |
| #117 fix(restore drill auth) | ✅ مدمج ومنشور في الشجرة النشطة |

## 6. ملاحظات مفتوحة (لجولات لاحقة)

1. **تشغيل migration 0013 --commit** على الإنتاج (مسح أوسمة beIN الست الخاطئة) — خطوة واحدة، تحتاج فقط جلسة تنفيذ (DRY-RUN يعرضها).
2. **الترقيات الكبرى المعلّقة** (okhttp5, eslint10, typescript7, node26, hilt, coroutines, composeTv) — PRs #98/#99/#102/#105/#106/#107/#108 موثّقة.
3. **مصدر EPG جزائري/عربي** — الطريق الوحيد لرفع التغطية فوق ~15% بشكل ملموس.
4. `dzhoof-shared-network` + القرار القانوني للمصادر — كما هي.
5. تباين الثيم الفاتح في Android — مؤجل.
