# تقرير التسليم النهائي — DZ HOOF IPTV (جلسة 2026-08-24/25، الجولات 13 → 13.7)

> **اقرأ هذا أولًا.** الوثيقة الوحيدة التي تحتاجها لبدء العمل. كل ما أُنجز في هذه
> الجلسة (إصلاح الجوال جذريًا — **تأكيد المستخدم: "الآن تعمل"**)، حالة الإنتاج،
> ما تبقى، الأوامر السريعة، والمزالق. التفاصيل في تقارير الجولات:
> `ADMIN_IMPROVEMENT_REPORT_ROUND13_AR.md`، `ROUND13_5_AR.md`، `ROUND13_6_AR.md`،
> `ROUND13_7_AR.md` + `HANDOVER_REPORT_2026-08-24_ROUND13_FINAL_AR.md` +
> `PROJECT_STATE_HANDBOOK_AR.md`.

---

## 1. الملخص التنفيذي

| الجبهة | النتيجة |
|---|---|
| **المستودع** | ✅ GitHub `main` = `e66a27c` = الخادم (18 ملفًا بالـ checksums) |
| **الإنتاج** | ✅ `v1.0.1-20260825T003058Z` — 6 حاويات healthy + لا OOM |
| **مشكلة الجوال (الطلب الرئيسي)** | ✅ **حُلّت جذريًا — المستخدم أكد "الآن تعمل"** |
| **الجولات** | 13 (تحسينات الأدمن) + 13.5 (جلسة الموزع) + 13.6 (قائمة/تمرير) + 13.7 (السبب الجذري: تمرير بوابة الموزع) |
| **الاختبارات** | ✅ 253/253 باك-إند + 6/6 واجهة + tsc + eslint نظيفة (قبل كل نشر) |

---

## 2. الوصول (ثابت)

- **SSH**: `dzhoof-admin@5.135.79.221` بمفتاح ed25519 (نسخة في `/home/user/.workspace/dzhoot/dzhoof-admin-key`). sudo بلا كلمة مرور.
- **GitHub**: `mostafabonnif-beep/dzhoot` — PAT في `/etc/dzhoot/github.token` على الخادم.
- **الإنتاج**: `/opt/dzhoot/server` (نسخة ملفات) + `/etc/dzhoot/.env.production` (chmod 600).
- **لوحة الأدمن**: `https://iptv.ld-11.net/admin` — `admin` / `DZHOOF-b358a74599573e6e84ae7bcf18ab37da`. ⚠️ **مكشوفة — غيّرها (أولوية أمنية).**
- **بوابة الموزع**: `https://iptv.ld-11.net/reseller/login` (يوزر/باسورد لكل محل).
- **قاعدة البيانات**: حاوية `dzhoof-mongodb` — القاعدة **`dzhoof-iptv`** دائمًا.

---

## 3. ما أُنجز في هذه الجلسة (بالترتيب)

### الجولة 13 — تحسينات لوحة الأدمن (نُشرت `v1.0.1-20260824T195220Z`)
1. أسهم الترقيم معكوسة في RTL → أُصلحت (`pagination.tsx`).
2. خطأ regex 2FA مزدوج الإفلات → أُصلح (`settings`).
3. إحصائيات الأجهزة من الصفحة فقط → `stats` خادم (active7d/platforms/pending) + بحث/فلترة.
4. **جدولة الإشعارات**: `scheduledAt` + مهمة `notification-dispatcher` (كل 60s) + شارة "مجدول".
5. **تعديل بيانات الأفلام/المسلسلات**: PATCH يقبل حقول metadata + نافذة تعديل.
6. **بحث شامل ⌘K** عبر مستخدمين/أكواد/قنوات.

### الجولة 13.5 — بوابة الموزع: طرد الجلسة (نُشرت `v1.0.1-20260824T210223Z`)
- الموزع كان يُطرد لتسجيل الدخول عند كل تحديث صفحة (سباق الـ hydration) → انتظار
  `useAuthStore.persist` قبل فحص التوكن (نمط `useRequireAuth`).
- رسالة "تعذر تسجيل الدخول" المضللة → `portal.loadError` («تعذر تحميل البيانات»).

### الجولة 13.6 — قائمة الجوال + 100vh (نُشرت `v1.0.1-20260825T000436Z`)
- القائمة الجانبية لم تكن تُغلق بعد الضغط على رابط → `onClick` يغلق الدرج.
- `h-screen` (100vh) يقطع أسفل المحتوى في متصفحات الجوال → `supports-[height:100dvh]:h-dvh`
  في app-shell + 5 صفحات auth.

### الجولة 13.7 — **السبب الجذري: بوابة الموزع مجمّدة** (نُشرت `v1.0.1-20260825T003058Z`)
- **السبب**: `globals.css` يطبّق `html, body { overflow: hidden }` عمومًا (تصميم
  app-shell للوحة الأدمن). بوابة الموزع + دخول الموزع + الصفحة الرئيسية كانت
  `min-h-screen` بتمرير مستند طبيعي → **بلا أي تمرير إطلاقًا** (مُثبت: المحتوى
  1242px والوثيقة 844px — كل شيء تحت أول شاشة غير قابل للوصول).
- **الحل**: بوابة الموزع أعيدت هيكلتها بنمط AppShell المطابق للوحة الأدمن حرفيًا:
  `flex h-screen supports-[height:100dvh]:h-dvh overflow-hidden` → غلاف
  `flex flex-1 flex-col overflow-hidden` → `<main class="flex-1 overflow-y-auto">`.
  دخول الموزع + الصفحة الرئيسية أصبحتا قابلتين للتمرير.
- **✅ تأكيد المستخدم: "الآن تعمل"** — المشكلة الرئيسية حُلّت نهائيًا.

### فحص القنوات (طلب سابق)
- 16,640 قناة كلها نشطة من مصدر Xtream واحد (Primary Upstream). عينة 30:
  **~90% حية** (27 ALIVE / 2 DEAD "Cannot read ..._m3u8" / 1 EMPTY). القنوات
  تشتغل في التطبيق؛ ~7-10% ميتة عند المزود — تنظيف لاحق.

---

## 4. حالة الإنتاج (مُتحققة 2026-08-25)

- 16,640 قناة | 7 مستخدمين | 6 اشتراكات نشطة | 3 محلات (بن عكنون ×2 + dzsat) |
  ~66,896 فيلم VOD + مسلسلات | مهمة الإشعارات تعمل كل دقيقة.
- آخر نشر `v1.0.1-20260825T003058Z` | نسخ mongodump قبل كل نشر في
  `/var/backups/dzhoot/mongodb/`.
- البيانات نظيفة (0 إشعارات تجريبية، 0 حركات يتيمة، حُذف كل الموزعين التجريبيين).

---

## 5. ما تبقى (بالأولوية)

### 🔴 أمني (عاجل — كلمات المرور مكشوفة في محادثة المستخدم)
- [ ] تغيير كلمة مرور **root** + كلمة مرور **admin** + مراجعة `github.token`.

### 🔴 تفعيل التنبيهات والبريد (الواجهة جاهزة — تنقص المفاتيح)
- [ ] webhook حقيقي (Discord/Slack) + مفاتيح **Brevo SMTP** + مفاتيح **FCM**
      (مهمة `notification-dispatcher` تنتظر FCM فقط).

### 🔴 تجاري
- [ ] **بوابة دفع** (CinetPay/Paymee/Stripe) — الأكبر عائدًا.
- [ ] مراجعة الأسعار (شهري 500 / 6أشهر 2500 / سنوي 4500 دج).

### 🟡 جودة الكتالوج
- [ ] تنظيف القنوات الميتة (~7-10% متوقعة) عبر Liveness Check + أدوات bulk.
- [ ] أسماء متسخة (`## TR|…`, `4K-AR: ...`) | **مصدر عربي/جزائري ثانٍ** (أكبر رافعة).
- [ ] EPG عربي/جزائري + beIN SPORTS 5/MAX.

### 🟢 تشغيلي
- [ ] نسخ احتياطي خارجي (restic) + اختبار استعادة | R8 لتصغير APK.

---

## 6. أوامر سريعة

```bash
ssh -i ~/.workspace/dzhoot/dzhoof-admin-key dzhoof-admin@5.135.79.221
curl -s https://iptv.ld-11.net/health/ready
# نشر: cd /opt/dzhoot/server && sudo bash scripts/deploy/deploy-production.sh --apply   (~10 دقائق)
# اختبار: cd server && npm run typecheck && npm run test:backend && cd frontend && npx tsc --noEmit && npx eslint <files>
# GitHub: TOKEN=$(ssh ... 'sudo cat /etc/dzhoot/github.token'); git push https://x-access-token:$TOKEN@github.com/mostafabonnif-beep/dzhoot.git main
# Mongo: sudo docker exec dzhoof-mongodb mongosh dzhoof-iptv   ← حدد القاعدة دائمًا!
# مزامنة ملفات: tar + scp + استخراج sudo إلى /opt/dzhoot + تحقق sha256 (اقتبس مسارات (dashboard))
```

---

## 7. دروس ومزالق (أضيفت هذه الجلسة — مهمة)

1. **`html, body { overflow: hidden }` معمم في globals.css** — أي صفحة خارج
   AppShell يجب أن تكون بنمط AppShell: جذر `flex h-screen supports-[height:100dvh]:h-dvh
   overflow-hidden` + غلاف `flex flex-1 flex-col overflow-hidden` + `<main class="flex-1
   overflow-y-auto">`. **`min-h-screen` وحده = صفحة مجمّدة بلا تمرير.**
2. **فحص الجوال**: قِس `scrollHeight > clientHeight` دائمًا + التمرير البرمجي؛
   **محاكاة اللمس الرأسية (Input.dispatchTouchEvent) غير موثوقة** — نفس الصفحة
   تتمرر في تبويب ولا تتمرر في آخر بنفس الإعدادات. لا تعتمد عليها وحدها.
3. **أي درج/قائمة منزلقة على الجوال → أغلقها عند اختيار عنصر** (كانت تبقى مفتوحة).
4. **أي `h-screen`/`100vh` جديد → `supports-[height:100dvh]:h-dvh`** (قطع أسفل الشاشة).
5. **بوابة الموزع تشارك localStorage مع لوحة الأدمن** — تسجيل دخول أحدهما يمسح
   الآخر (تصميم مقصود بمخزن واحد؛ انتبه عند الاختبار بتبويبات متعددة).
6. `credit` في `Reseller` **مصفوفة** `{planId, quantity}` — رقم → 500 في `/reseller/me`.
7. مسارات `(dashboard)` بين أقواس تكسر اقتباس الصدفة البعيدة — اقتبس المسار كاملًا.
8. `while read` + SSH في حلقة → SSH يستهلك stdin — استخدم `for` مع `< /dev/null`.
9. mongosh داخل الحاوية يتصل بقاعدة `test` — حدد `dzhoof-iptv` دائمًا.
10. النشر ~10 دقائق؛ انتظر `DEPLOY COMPLETE`؛ النسخ الاحتياطي تلقائي قبل النشر.

---

## 8. الحالة النهائية

- GitHub `main` = `e66a27c` — متزامن مع الخادم (18 ملفًا).
- الإنتاج `v1.0.1-20260825T003058Z` سليم، **مشكلة الجوال الرئيسية حُلّت وتأكدها
  المستخدم**، والقنوات تشتغل في تطبيق DZ Hoof (~90% من العينة).
