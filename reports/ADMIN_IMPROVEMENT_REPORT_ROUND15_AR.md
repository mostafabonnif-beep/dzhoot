# تقرير تحسين — DZ HOOF IPTV (الجولة 15: تصحيح المحاسبة + تحصين اللوحة)

**التاريخ:** 2026-08-25 (جولة 15)
**النطاق:** إصلاح أخطاء مالية جوهرية في حساب الإيرادات ورصيد الموزعين، وسدّ
ثغرات أمنية/تجربة استخدام في لوحتي الأدمن والموزع (ناتجة عن تدقيق كود كامل).
**الحالة:** ✅ مُنفَّذ + مرفوع + **منشور** `v1.0.1-20260825T065111Z` — تحقق حي كامل.

---

## 1) الأخطاء المالية (الأهم — كانت تعرض أرقامًا خاطئة)

### أ. إيراد لوحة التحكم لم يكن الإيراد الحقيقي (لوحة الأدمن)
كان «إيراد هذا الشهر» يحسب فقط **تفعيلات الأكواد × سعر الباقة** ويظهر `0 DZD`
بينما اشترى المحل رصيدًا بـ 4,700 دج. الآن:
- الإيراد = **مشتريات المحلات (رصيد)** + **تسليمات الدفعات (جملة)** +
  **تفعيلات أكواد المشغّل المباشرة فقط** (أكواد الموزعين تُدفع عبر الرصيد/الدفعات،
  فاحتساب تفعيلها مجددًا = عدّ مزدوج).
- `byReseller.purchases` أصبح يجمع الرصيد الموجب + قيمة تسليمات الدفعات.
- «التفعيلات» (البطاقات) تبقى عدًّا خامًا لكل التفعيلات — فصلٌ مقصود بين
  «النشاط» و«الإيراد».

### ب. خصم الرصيد كان يرفع «قيمة المشتريات» (تضخيم وهمي)
سحب الرصيد يُسجَّل `GRANT` بكمية سالبة، وكان `purchasedValue` يضيف قيمته
المطلقة — أي أن سحب 10 أكواد أضاف 10,000 دج للأرباح! الآن تُحتسب المشتريات من
صفوف `GRANT` الموجبة فقط.

### ج. `netQty` في بوابة الموزع كان مكسورًا (والسبب أعمق)
- **سبب جذري قديم**: تعبير `-'$quantity'` في تجميع MongoDB ليس نفيًا صحيحًا
  (لا يوجد unary minus في aggregation) → `consumedQty` كان `null` دائمًا في
  الإنتاج، و`netQty` كان `null` أيضًا.
- **خطأ منطقي**: الديون المسترجعة (`RETURN/EXPIRE_RETURN`) تسترجع الرصيد، لكن
  المعادلة كانت **تطرحها**: `granted - consumed - returned`.
- الإصلاح: `netQty = granted - consumed + returned` مع تصحيح صيغة النفي
  (`$subtract: [0, '$quantity']`) → الرصيد المعروض للموزع أصبح صحيحًا.

### د. تسليمات الدفعات لم تكن تُسجَّل قيمتها
`CodeBatch` كان يتجاهل سعر الجملة. الآن يُحفظ `wholesalePrice/wholesaleTotal`
عند الإنشاء (وللدفعات القديمة يُحتسب من سعر الجملة الحالي للمحل).

## 2) تحصين وإصلاحات اللوحة

### أمني
- **تغيير كلمة مرور الأدمن من اللوحة** (قسم جديد في الإعدادات) — لم يكن هناك أي
  طريق ذاتي لتغييرها (صفحة المستخدم محجوزة لدور User فقط). يُسجِّل الخروج من كل
  الجلسات الأخرى تلقائيًا. خطأ كلمة المرور الحالية لا يقتل الجلسة (رأس
  `X-Skip-Auth-Redirect`).
- **كلمة مرور SMTP لا تُعرض بعد الآن** — واجهة API تُرجع `brevo_configured`
  فقط، والحقل الفارغ عند الحفظ = الإبقاء على الحالية.
- **حماية SSRF لإنشاء/تعديل القنوات** (channelUrl/channelImg) — نفس الحماية
  المعتمدة في مسار استيراد M3U.
- **إفلات البحث في `$regex`** (أكواد التفعيل + الباقات) لمنع ReDoS/أخطاء 500.
- **400 بدل 500** عند تكرار يوزر/بادئة محل.

### صحة البيانات
- **حالة الإشعارات صادقة دون كسر قناة التطبيق**: القناة داخل التطبيق
  (`/me/notifications`) تُسلّم دائمًا، لذا يبقى `SENT`؛ لكن `deliveryStats`
  يحمل `pushDelivered` + السبب (FCM غير مضبوط / لا أجهزة / كل المحاولات فشلت)
  وتعرض اللوحة شارة «دون دفع» + زر **إعادة إرسال الدفع** عند فشل FCM — لم يعد
  «مُرسَل» يخفي فشل الإشعارات الفعلية.
- **الإرسال لكل الأجهزة** (كان يقتطع بعد 5,000 توكن صامتًا) مع حدّ تزامن 25.
- **`maxConcurrentStreams` في الباقات كان يُهمَل** — الواجهة تعرضه لكن الخادم
  لا يحفظه؛ أصبح يُحفظ ويُتحقق (1–100).
- **الديون**: ترتيب الخادم أصبح غير المدفوع أولًا (لم يعد القطع عند 300 يخفي
  الديون القديمة)، `totalDebts` دقيق، رفض مبلغ 0 وكمية سالبة.
- **تأكيد قبل تعطيل/تفعيل مستخدم** (كان بضغطة واحدة يقتل الجلسات).

### تجربة الموزع
- **401 يوجّه الموزع لبوابة الموزعين** لا لدخول الأدمن (كان يرميه على `/login`).
- **حارس الدور في البوابة**: توكن غير موزّع (أدمن/يوزر) يُعيد التوجيه لدخول
  الموزعين بدل لوحة مكسورة. خطأ كلمة مرور الموزع يعرض الرسالة دون إعادة تحميل.

### صغائر
- إصدار الشريط الجانبي 1.0.1 (تطابق مع الخادم) • بطاقة الإيراد تعرض تفصيل
  «مشتريات المحلات» • إغلاق حدود الترقيم في الإشعارات/الأكواد • إزالة
  `module.exports` المكرر.

## 3) الاختبارات

- **7 اختبارات جديدة** (`round15-accounting.test.ts`): netQty مع الاسترجاع
  والخصم، ترتيب الديون + رفض القيم الخاطئة، إيراد شامل (مشتريات + تسليمات +
  تفعيلات مباشرة، بدون عدّ مزدوج/تضخيم)، حفظ `maxConcurrentStreams`، نتيجة
  الدفع (`pushOutcome`).
- **الكامل**: باك-إند **270/270** + واجهة 6/6 + tsc نظيف + eslint 0 أخطاء +
  E2E smoke أخضر في CI.

## 4) الملفات المتغيّرة (19 ملفًا)

```
server/backend/src/routes/admin.js                 (إيراد/تسليمات/SSRF)
server/backend/src/routes/reseller.js              (netQty + الديون + الحارس)
server/backend/src/routes/admin-plans.js           (maxConcurrentStreams + إفلات)
server/backend/src/routes/admin-notifications.js   (حالة صادقة + ترقيم)
server/backend/src/routes/admin-code-batches.js    (حفظ الجملة)
server/backend/src/routes/admin-app-settings.js    (إخفاء كلمة SMTP)
server/backend/src/routes/admin-activation-codes.js(إفلات + ترقيم)
server/backend/src/routes/admin-resellers.js       (400 للمكرر)
server/backend/src/models/CodeBatch.ts             (+wholesalePrice/Total)
server/backend/src/models/Notification.ts          (+FAILED +deliveryStats)
server/backend/src/services/fcm-service.ts         (كل الأجهزة + تزامن + helper)
server/backend/src/services/task-registry.ts       (حالة صادقة للمجدول)
server/frontend/src/app/(dashboard)/admin/settings/page.tsx  (تغيير كلمة المرور + SMTP)
server/frontend/src/app/(dashboard)/admin/users/page.tsx     (تأكيد التعطيل)
server/frontend/src/app/(dashboard)/admin/page.tsx           (بطاقة الإيراد)
server/frontend/src/app/(dashboard)/admin/notifications/page.tsx (فشل + إعادة إرسال)
server/frontend/src/app/reseller/page.tsx         (حارس الدور)
server/frontend/src/app/reseller/login/page.tsx   (تخطي إعادة التوجيه)
server/frontend/src/lib/api.ts                    (توجيه 401 للموزع)
server/frontend/src/components/layout/sidebar.tsx (الإصدار)
server/backend/src/__tests__/round15-accounting.test.ts (جديد)
```


## 5) ما تمّ في الجولة 15 (خلاصة)

1. **المحاسبة صارت صادقة**: إيراد الشهر = مشتريات المحلات + تسليمات الدفعات + تفعيلات مباشرة فقط (لا عدّ مزدوج، لا تضخيم من سحب الرصيد).
2. **رصيد الموزع صحيح**: `netQty` كان مكسورًا في الإنتاج (نفي خاطئ في التجميع + الديون المسترجعة كانت تُطرح بدل أن تُضاف) — أُصلح وأصبح دقيقًا.
3. **تسليمات الدفعات تُسجَّل قيمتها** (سعر الجملة) منذ الإنشاء.
4. **تغيير كلمة مرور الأدمن من اللوحة** (الإعدادات) — لأول مرة، مع خروج تلقائي من الجلسات الأخرى.
5. **حماية SSRF** للقنوات + **إفلات البحث** في `$regex` + **400** للمكرر + إخفاء كلمة مرور SMTP.
6. **حالة الإشعارات صادقة**: تبقى `SENT` داخل التطبيق لكن `deliveryStats` يسجّل فشل FCM وتعرض اللوحة «دون دفع» + زر إعادة إرسال.
7. **الموزع**: 401 يوجّه لبوابته، حارس دور لمنع توكن الأدمن، كلمة مرور خاطئة بلا إعادة تحميل.
8. **صغائر**: `maxConcurrentStreams` يُحفظ فعليًا، تأكيد قبل تعطيل مستخدم، ترتيب الديون، ترقيم كامل.

## 6) المتبقّي — للي بعدي (قائمة الانتظار بالترتيب)

| الأولوية | البند | التفاصيل |
|---|---|---|
| 🔴 عاجل | **تغيير كلمات المرور** | admin (من الإعدادات ← تغيير كلمة المرور) + **root على الخادم** — كلاهما مكشوف في المحادثة. لاحظ جولة 15 وفّرت أداة تغيير كلمة مرور admin ذاتيًا؛ root لا يزال يدويًا (`passwd`). |
| 🔴 عاجل | **تدوير التوكنات** | `github.token` في `/etc/dzhoot/github.token` و`JWT_SECRET` إذا أمكن. |
| 🟠 ربح | **بوابة الدفع** | CinetPay / Paymee / Stripe — الأسعار محسومة: شهري 500 / 6 أشهر 2500 / سنوي 4500 دج. |
| 🟠 ربح | **تفعيل الإشعارات الفعلية** | مفاتيح FCM + Brevo SMTP + webhook حقيقي — كل الواجهات جاهزة، فقط المفاتيح ناقصة (المجدول يعمل كل دقيقة وينتظرها). |
| 🟡 جودة | **تنظيف الكتالوج** | فحص حيوية الـ 16,640 قناة (~7–10% ميتة)، حذف الجماعي، إزالة التكرار (`## TR`، `4K-AR:`)، **مصدر ثانٍ عربي/جزائري**، EPG لـ beIN Sports. |
| 🟢 مقترح | **ربط الديون بالرصيد** | سؤال «هل هذا للزبون بالدين؟» عند إنشاء الكود + تقرير شهري للديون. |
| 🟢 مقترح | **«رصيد هدية» vs «شراء»** | علامة `paid` على حركة GRANT لتمييز الرصيد المجاني عن المدفوع في الإيراد (ملاحظة الجولة 15). |

## 7) حالة النشر

- **Commit على `main`**: `b5f16e4` (+ سابقوه `67cc354`، `38b8111`، `1b5f41f`).
- **النشر**: ``v1.0.1-20260825T065111Z`` — التحقق الحي في ملاحظات الجولة التالية.
- الاختبارات: باك-إند **270/270**، واجهة 6/6، tsc + eslint نظيفان.


### التحقق الحي بعد النشر
- ✅ **الإيراد صار صادقًا**: «إيراد هذا الشهر 13,700 دج» (كان 0 دج) = مشتريات dzsat 4,700 + بن عكنون/برج الكيفان 9,000 — مطابقة تامة.
- ✅ **«تغيير كلمة المرور»** ظهر في الإعدادات (الحالية/الجديدة/التأكيد) — يعمل.
- ✅ كلمة مرور SMTP لا تظهر؛ الحقل «لم تُضبط بعد» فقط.
- ✅ لوحة التحكم/بوابة الموزع تعملان (healthy)؛ لا ديون متبقية (0)؛ القنوات 16,640.

## 8) ملاحظات (الجولة 15)

- **تُرك عمدًا**: منح الرصيد المجاني عند إنشاء محل بقيمة 0 لا يؤثر (amount=0)،
  لكن أي إضافة رصيد بقيمة جملة تُحسب إيرادًا — إن أردت تمييز «رصيد هدية» عن
  «شراء» لاحقًا، نضيف علامة `paid` للحركة.
- **قبل النشر**: غيّر كلمة مرور `admin` من اللوحة (الإعدادات ← تغيير كلمة
  المرور) وكلمة مرور root على الخادم — ما زالتا مكشوفتين في المحادثة.
