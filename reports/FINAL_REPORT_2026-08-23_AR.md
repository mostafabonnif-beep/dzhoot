# التقرير النهائي — جلسة تطوير DZ HOOF (2026-08-23)

> **اقرأ هذا أولًا ثم `PROJECT_STATE_HANDBOOK_AR.md`.** هذا التقرير يلخص كل ما تم إنجازه في هذه الجلسة وما تبقى، ليبدأ من بعده أي شخص/وكيل من حيث انتهينا دون إعادة فحص.

---

## 1. ملخص تنفيذي

نُفِّذت **12 تحسينًا وإصلاحًا** (PRs #27–#38) في يوم واحد، كلها **مدفوعة إلى GitHub (main) ومنشورة على الإنتاج (VPS)** ومتحقق منها:

| المجال | النتيجة قبل | النتيجة بعد |
|---|---|---|
| القنوات "الميتة" في اللوحة | 16,635 معطلة | **1 فقط** (قناة M3U ميتة فعلًا) |
| مزامنة M3U المجدولة | فاشلة دائمًا (نقص مفاتيح) | **ناجحة** |
| تشغيل القنوات في اللوحة | فاشل (بروكسي معطّل 410) | **يعمل** — بث حي حقيقي (مُتحقق باللقطة) |
| تشغيل التطبيق | كان معطّلًا على كل قنوات Upstream (خطأ Media3) | **يعمل** — المانيفست يُطبَّع الآن (PR #42) |
| ذاكرة الحاويات | OOM kills عند 1.5GB | **2GB** — لا قتل للعمليات |
| إدارة VOD/Series | تصفح فقط | **إدارة كاملة** (تعطيل/حذف/جماعي) |
| إعدادات التطبيق | — | تصدير/استيراد |
| **تسجيل البث وتحميله** | غير موجود | **ميزة كاملة** (مثل يوتيوب لايف) — MP4 برابط ثابت |
| التوثيق | — | دليل حالة شامل + تقرير نهائي |

**المزامنة**: المستودع والخادم متطابقان **100%** (فحص checksums لـ 952 ملفًا — لا فرق).

---

## 2. ما تم إنجازه بالتفصيل (PRs #27–#42)

### جولة "القنوات لا تعمل في التطبيق" (PR #40 + #42 — آخر الجلسة)
- **الشكوى**: القنوات تعمل في اللوحة لكن التطبيق (Android) لا يشغّلها.
- **التشخيص بالدليل**: سجلات الخادم + تقارير QoE من قاعدة البيانات: 36 فشل تشغيل `ERROR_CODE_PARSING_CONTAINER_UNSUPPORTED` مقابل **نجاح واحد** (قناة M3U easybroadcast) — أي أن **كل قنوات Upstream** تفشل في media3 1.4.1 بينما يلعبها ffmpeg وhls.js.
- **السبب الجذري**: المانيفست المُوكَّل (رغم صحته وفق المعيار) كان يحمل: `TARGETDURATION` مساويًا لمدة المقطع، وسم `#EXT-X-ALLOW-CACHE` المهجور، وروابط مقاطع بتوكنات فرعية بطول 500+ حرفًا تتغير كل تحديث للقائمة.
- **الإصلاح (PR #42)**: تطبيع قوائم MEDIA في الوكيل — روابط مقاطع قصيرة ثابتة تحت التوكن الجذري (`/segments/<abs-seq>.ts` مع حلّ من النافذة الحالية)، رفع `TARGETDURATION` إلى ceil+1، حذف `ALLOW-CACHE`، ومسار جديد للمقاطع يتحقق من التوكن/الجلسة. قوائم MASTER بقيت على المسار العودي.
- **تحقق حي**: مانيفست مطبّع + مقطع 200 (7.1MB MPEG-TS) + ffmpeg 25 ثانية بلا توقف (750 إطارًا، نافذة تتقدم).
- **إصلاحات متراكمة (PR #40)**: القائمة تزوّد روابط مُرمّزة لعملاء TV، الجلسة كسولة عند أول تشغيل، `playback-token` يقبل _id أو channelId.

### الإصلاحات الأساسية
- **PR #27 — إصلاح 16,635 قناة "ميتة"**: قنوات التشغيل المباشر (Primary Upstream) كانت تحمل وسم dead قديمًا من عصر الفحص الخادمي (المصدر يحظر IP الداتاسنتر — HTTP 456). أُصلح `stream-health-service.ts`: تطبيعها إلى working (لأن قياسها من الخادم مستحيل — يُقاس بفشل التشغيل الفعلي)، إعادة فحص القنوات الميتة كل 6 ساعات (تعافٍ تلقائي)، رفع مهلة الفحص إلى 15 ثانية. كما أُصلحت حاوية الـ scheduler (كانت تنقصها مفاتيح التشفير فتفشل مزامنة M3U/Xtream المجدولة).
- **PR #28 — الذاكرة**: رفع حد api/scheduler من 1.5GB إلى 2GB (وقف OOM kills).

### مزامنة العمل غير المدفوع (حرج)
- **PR #30 — الجولة السادسة**: الخادم كان يحتوي **وحدة إدارة VOD/Series كاملة + إصلاح isActive + تصدير/استيراد الإعدادات** غير موجودة في Git إطلاقًا — كان أي نشر من GitHub سيفقدها. نُقلت كلها للمستودع بعد التحقق (234/234 اختبارًا).

### تشغيل اللوحة
- **PR #32 — لاعب اللوحة**: كان يستخدم البروكسي القديم المعطّل (410) فكل تشغيل يفشل بـ manifestLoadError. أصبح يستخدم **مسار التوكن الصحيح** (نفس مسار التطبيق) — same-origin HTTPS يتجاوز الـ mixed-content ويعمل عبر الـ Relay.
- **PR #33 — تصحيح شارة المصدر** (includes بدل startsWith).

### تسجيل البث (الميزة الجديدة — PR #35/36/37)
- **تسجيل أي قناة حية الآن → عند الإنهاء يتحول تلقائيًا إلى MP4 (faststart) برابط ثابت** للمشاهدة (`/watch`) والتحميل (`/download`) — مثل يوتيوب لايف.
- Backend: `Recording` model + `recording-service` (ffmpeg `-c copy` → TS → remux MP4، ffprobe للمدة، احتفاظ 30 يومًا، سقف تسجيلين متزامنين و12 ساعة) + مسارات API + ffmpeg داخل الصورة + حجم `recordings_data`.
- Frontend: صفحة `/admin/recordings` (بحث قناة + بدء/إيقاف/مشاهدة/تحميل/حذف + تحديث تلقائي).
- إصلاحان: الإيقاف يُنهي التسجيل حتى مع خروج ffmpeg برمز 255 (الملف سليم)؛ أسماء ملفات ASCII (رموز ᴴᴰ تكسر التحميل).
- **تحقق حي**: سُجّل "SPO: Al Kass 1 HD" 100 ثانية → MP4 صالح 31.2MB → تحميل/مشاهدة يعملان.

### التوثيق
- **PR #29/31/34/38 — `PROJECT_STATE_HANDBOOK_AR.md`**: المرجع الموحد (بنية، إنتاج، مصادر، نشر، إنجازات، مشاكل، قواعد) — في المستودع والخادم.

---

## 3. الحالة الحالية للإنتاج (متحقق 2026-08-23)

- **اللوحة**: https://iptv.ld-11.net/admin — البانر: "قناة واحدة معطلة" (كان 16,635). المصادر كلها "مستقرة".
- **البيانات**: 16,639 قناة (16,638 سليمة) | VOD 66,866 | مسلسلات 16,804 | مستخدمون 5 | أجهزة 4 | باقة اختبار واحدة.
- **EPG**: تغطية **~9% فقط** (15,220 قناة بلا دليل) + 13/50 مصدر EPG فاشل — **أكبر مشكلة متبقية**.
- **البنية**: 6 حاويات صحية، ذاكرة 2GB لكل من api/scheduler، ffmpeg داخل API، volume تسجيلات، نسخ احتياطي يومي mongodump+restic.
- **ميزات جديدة**: صفحة التسجيلات تعمل، إدارة VOD/Series تعمل، تصدير/استيراد الإعدادات يعمل.

---

## 4. ما تبقى (خارطة الطريق)

### المرحلة 0 — الاستقرار والبيع (الأولوية)
- [ ] **EPG**: إصلاح مصادر EPG الـ13 الفاشلة + رفع التغطية من 9% (أكبر أثر على الاحترافية)
- [ ] **باقات فعلية + بوابة دفع** (حاليًا باقة اختبار مجانية واحدة؛ لا Stripe/Paymee/CinetPay)
- [ ] **إكمال الترجمة** للصفحات العربية فقط (لوحة، قنوات، VOD، Series، استيراد، مصادر...)
- [ ] **تنظيف الكتالوج**: حذف نسخ "NM:" المكررة الميتة (مثل NM AL KASS 264104 — شاشة سوداء) وترك نسخة عاملة واحدة

### المرحلة 1 — البيع والتحصيل
- [ ] نظام **ريسيلر** (أدمن ثانوي يبيع أكوادًا) + حد الأجهزة عند التشغيل
- [ ] APK موقّع (release) مع إعادة تفعيل R8 + ترقية Media3 (1.5+)

### المرحلة 2 — التميز
- [ ] جدولة تسجيل تلقائية (سجّل مباراة من وقتها إلى وقتها) + Catch-up لمصادر M3U
- [ ] White-label (اسم/شعار/ألوان من الإعدادات) + رقابة أبوية حقيقية

### أمنية (من مراجعة الكود — مفصلة في دليل الحالة)
- [ ] إزالة api/frontend من الشبكة المشتركة `dzhoof-shared-network` + كلمات مرور Redis/Mongo
- [ ] أكواد تفعيل ملغاة ما زالت تُقبل في `/pair` و`/verify` (فحص `codeRevokedAt`)
- [ ] إزالة PIN من السجلات + `client-redeem` بجلسة أقصر
- [ ] Certificate pinning معلن لكن غير منفذ + Parental PIN بلا salt
- [ ] النسخ الاحتياطي **البعيد** غير مفعّل (يتطلب بيانات مزود تخزين)

### تشغيلية
- [ ] مراقبة مساحة قرص التسجيلات (37GB متاح؛ ~1.1GB/ساعة)
- [ ] ملاحظة قانونية: المصادر الحالية إعادة بث لمحتوى غير مرخّص — قرار استراتيجي للمالك

---

## 5. كيف يكمل من بعدي (القواعد الذهبية)

1. ابدأ بـ `PROJECT_STATE_HANDBOOK_AR.md` — لا تعيد فحص المشروع من الصفر.
2. التغيير: فرع `fix/`/`feat/` → PR صغير → اختبارات خضراء → دمج → نشر `deploy-production.sh --apply` → **تحديث الدليل**.
3. أي عمل مباشر على الخادم **يجب دفعه للمستودع فورًا** (الدرس من الجولة السادسة).
4. النشر يتطلب بناء محلي على الخادم (`docker build` للـ api عند تغيير backend؛ `deploy-production.sh` للكل).
5. لا أسرار في Git — كلها في `/etc/dzhoot/.env.production` (وتوكن GitHub في مساحة عمل الصندوق بصلاحيات 600).
6. عند الشك في الحالة: قارن checksums (`git ls-files` مقابل `/opt/dzhoot`).

---

## 6. مراجع

- **الدليل الشامل**: `PROJECT_STATE_HANDBOOK_AR.md` (في المستودع + `/opt/dzhoot` على الخادم)
- **وثائق ops**: `server/docs/ops/` (Upstream relay، إصلاح الـ dead، التسجيلات)
- **اللوحة الحية**: https://iptv.ld-11.net/admin
- **سجل الجلسة**: يوميات الصندوق `memory/2026-08-23.md` + الذاكرة الدائمة `memory/wiki/dzhoof-project.md`

---

_انتهى التقرير. الحالة: كل شيء مدفوع (main = 2b5a26f) ومنشور ومتطابق مع الخادم 100%._
