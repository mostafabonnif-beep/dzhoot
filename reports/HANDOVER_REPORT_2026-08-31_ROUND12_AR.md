# تقرير تسليم — DZ HOOF IPTV (2026-08-31: الجولة 12 — تطهير الكتالوج + فحص relay متعدد الوجهات)

**التاريخ:** 2026-08-31
**الحالة:** ✅ منجز — منشور على `main` (`811aa05`/`eaf1f57`) ومطبّق على الإنتاج.

---

## 1) اكتشاف: مكررات قريبة من المصدر الاحتياطي B في كتالوج الزبائن

بعد نشر التجاوز الثلاثي (جولة 11) تبيّن أن منطق الظهور في `tv.js`
(`verifiedXtreamChannelQuery`) يعرض قنوات أي مصدر `Active + verified` — وليس فقط
`customerVisible`. بما أن المصدر الاحتياطي B محقّق، كانت قنواته (6,977) **تظهر للزبائن**،
وبنفس القنوات الجزائرية/الدولية لكن بأسماء مختلفة عن المصدر الأساسي (مثل `Dz| ENTV 1 FULL HD`
مقابل `AR: Algerie EN TV 1`) — **مكررات قريبة لا يلتقطها الـ dedup الاسمي**.

كما اكتشفنا **16,661 قناة يتيمة** تشير لمصدر محذوف (`6a9588cb…` — بقايا إعادة
هيكلة سابقة) تستهلك مساحة فحص المطابقة التلقائية (حد 20,000) دون أي فائدة.

## 2) القرار: المصدر الاحتياطي B مصدر failover نقي (بدون كتالوج) — مطابق للنمط المقصود

النمط المعماري للمصدر الاحتياطي في المشروع = **بدون كتالوج** (مثل المصدر الاحتياطي A: 0 قناة).
طبّقناه على المصدر الاحتياطي B:

- **نسخ احتياطي كامل قبل الحذف**: `/var/backups/dzhoot/mibox-catalog-channels-only/`
  (channels 23,638 + movies 16,948 + series 3,571 — 44,157 سجل، ~40MB، sha256 موثّق).
- **حذف**: قنوات المصدر الاحتياطي B + اليتامى (23,638) + أفلام المصدر الاحتياطي B (16,948) + مسلسلات المصدر الاحتياطي B (3,571).
- **بعد**: الكتالوج المعروض = المصدر الأساسي فقط (16,684) — **لا تكرار**.
- **الخرائط لم تُمَس**: المصدر الأساسي→المصدر الاحتياطي A (16,661) + المصدر الأساسي→المصدر الاحتياطي B (881) — صفر خرائط للقنوات اليتيمة.
- إعادة تشغيل `dzhoof-api` لمسح كاش الكتالوج والـ dedup، ثم تحقق:
  - `/health` ok (commit f3a78d8) · المصادر الثلاثة verified.
  - تدريب حي على قنوات حقيقية (AF: HISTOIRE، NEWS TV، ALTAGHIER) → **المصدر الاحتياطي A (الطبقة 1)** ✓.

## 3) فحص صحة الـ relay متعدد الوجهات (منتج قبل النشر)

`relay-pool.py` كان يعتبر النفق ميتًا إذا تعذّر الوصول لهدف المصدر الأساسي الوحيد
(`104.18.22.66:443`) — فيُغلق :9000 وتُسقط قواعد iptables **لكل** الوجهات
(ottstreambox :80 يعود 403 مباشر). الإصلاح (`scripts/relay/`):

- `upstream_check_targets`: قائمة أهداف، النفق حي إذا **أي** هدف اتصل
  (`المصدر الأساسي :443` أو `ottstreambox :80`)؛ الاحتفاظ بـ host/port القديم fallback.
- الحالة تعرض `last_ok_target` لكل مدينة.
- **مثبّت على الإنتاج وتم التحقق حيًا**: الوجهتان `TARGET_OK` عبر النفق.

## 4) استعادة ملفات حذفها commit إعادة الهيكلة

commit الأتمتة `390be76` (build: configure Android project structure) حذف
جانبيًا: `scripts/relay/` (عدا ملفين)، `server/docs/FAILOVER_TIERS_AR.md`، و
`HANDOVER_REPORT_2026-08-31_ROUND11_AR.md` — استُعيدت كلها من `781da9e` في
`eaf1f57` (بنية حية على الخادم، لا يجوز فقدان نسختها المسجلة).

## 5) الملاحظات

- استرجاع المصدر الاحتياطي B كتالوج إن لزم مستقبلًا: من النسخة الاحتياطية أعلاه (mongoimport).
- `max_connections=1` على حساب المصدر الاحتياطي B عند البانل — حد بث متزامن واحد.
- اليتامى: إن أُعيد إنشاء مصادر، تأكد من حذف قنوات المصدر المحذوف أولًا.
