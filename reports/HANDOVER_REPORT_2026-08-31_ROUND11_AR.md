# تقرير تسليم — DZ HOOF IPTV (2026-08-31: الجولة 11 — تجاوز ثلاثي المستويات المصدر الأساسي → المصدر الاحتياطي A → المصدر الاحتياطي B)

**التاريخ:** 2026-08-31
**الطلب:** عند ضغط/توقف المصدر الأساسي يتحول البث تلقائيًا إلى المصدر الاحتياطي A ثم المصدر الاحتياطي B، **دون تكرار القنوات** في التطبيق.
**الحالة:** ✅ منجز — الكود على `main` (`f3a78d8`) ونُشر ذريًا على الإنتاج (`v1.0.1-20260831T160647Z`)، مع تدريبات حية ناجحة.

---

## 1) الفكرة والتصميم

```
المصدر الأساسي (أساسي، customerVisible — القناة الوحيدة الظاهرة)
   │  watchdog: degraded/blocked (ضغط/توقف)
   ▼
المصدر الاحتياطي A (احتياطي 1 — priority 10)
   │  وقع أيضًا
   ▼
المصدر الاحتياطي B (احتياطي 2 — priority 20)
```

- الزبون يرى قناة واحدة؛ الاحتياطات خرائط جانبية (`ChannelFailoverMap`) ولا تظهر في التطبيق (فلتر `customerVisible`).
- التبديل يحدث وقت إصدار توكن البث (`getFailoverTarget`) — بدون تحديث للتطبيق، 2-5 ثوانٍ.

## 2) ما الذي كان ناقصًا

- `getFailoverTarget` كان يختار **خريطة واحدة** (الأحدث `updatedAt`) — إذا وقع الاحتياطي الأول لا يوجد تتالٍّ.
- المصدر الاحتياطي B لم يكن مربوطًا بأي خرائط.

## 3) التعديلات (commit `f3a78d8`)

| الملف | التغيير |
|---|---|
| `models/ChannelFailoverMap.ts` | حقل `priority` جديد (افتراضي 100، أقل = يُجرَّب أولًا) |
| `services/source-failover-service.ts` | `getFailoverTarget` → اجتياز كل الخرائط مرتبة بـ `priority` ثم `updatedAt`، يتخطى غير المؤهل/غير الصحي، يعيد أول هدف صحي (تتالٍّ) |
| اختبارات `round18-failover-service.test.ts` | 3 اختبارات جديدة: تتالٍّ عبر المستويات، تفضيل الأقل أولوية، الانهيار الكامل → `null` |

الخرائط القديمة بدون الحقل تُعامل كأولوية 100 (`$ifNull` في الـ aggregate) — لا أثر رجعي.

## 4) البيانات في الإنتاج

| الطبقة | الخرائط | الأولوية | الحالة |
|---|---|---|---|
| المصدر الأساسي → المصدر الاحتياطي A | 16,661 | 10 | مفعّلة |
| المصدر الأساسي → المصدر الاحتياطي B | 881 | 20 | مفعّلة (بعد حذف 1,086 خريطة ذاتية) |

المصادر: المصدر الأساسي `6a84dce7…` (customerVisible) · المصدر الاحتياطي A `6a958d81…` (نفس البانل، حساب آخر) · المصدر الاحتياطي B `6a9597af…` (ottstreambox — عبر الـ relay المنزلي، راجع `scripts/relay/`).

## 5) التحقق (كلها خضراء)

- اختبارات jest للملفين المتأثرين: **36/36** (في حاوية الاختبار مع mongo معزول).
- CI على `main`: backend/frontend/android/CodeQL/Secret guard — **كلها success**.
- تدريبات حية على كود الإنتاج (بيانات معزولة مؤقتة ثم تنظيف):
  - ترتيب الطبقات على قنوات حقيقية (PRIME: 13EME RUE / CANAL J / CHASSE ET PECHE) → **المصدر الاحتياطي A أولًا** ✓
  - الطبقة 1 ميتة → الانتقال إلى الطبقة 2 ✓
  - كل الطبقات ميتة → `null` (خطأ صريح بدل بث ميت) ✓
- `/health` يعرض commit النشر: `f3a78d8…` ✓

## 6) ملاحظات العمليات

- كاش صحة المصادر 60 ثانية — انتشار حالة المصدر خلال دقيقة.
- النشر الذري: أول محاولة تراجعت تلقائيًا (تصادم مع نشر `f7215dc1` متزامن — شبكة الأمان عملت كما صُممت، المنصة بقيت سليمة)؛ المحاولة الثانية نجحت.
- تشغيل النشر الطويل: `setsid nohup … > log` (جلسة SSH القصيرة تقطع).
- توثيق تشغيلي كامل: `server/docs/FAILOVER_TIERS_AR.md` + `scripts/relay/README.md`.
- حدود معروفة: تغطية المصدر الاحتياطي B 881 قناة فقط (مطابقة بالاسم)؛ اختيار المصدر يدويًا من الزبون = جولة مستقبلية.

## 7) التالي المقترح

1. رفع تغطية المصدر الاحتياطي B بإعادة المطابقة على دفعات (الكثير: 20,000 صف في المطابقة).
2. إضافة فحص صحة مستقل للـ relay (حاليًا مربوط بـ المصدر الأساسي فقط).
3. ميزة اختيار المصدر يدويًا للزبون في الواجهة.
