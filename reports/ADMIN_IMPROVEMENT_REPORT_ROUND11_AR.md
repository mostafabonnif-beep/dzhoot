# تقرير إكمال التنبيهات والأسعار — DZ HOOF IPTV (الجولة الحادية عشرة)

**التاريخ:** 2026-08-24 (جولة 11)
**النطاق:** التنبيهات + البريد + مدة صلاحية الأكواد + الأسعار
**الحالة:** ✅ مُنفَّذ ومرفوع على GitHub (`f6747a4`) + منشور على الإنتاج

---

## 1) المشكلة

- `alertingConfigured: false` في `/health/ready` — **رابط الـ webhook كان فارغًا في البيئة**، وبيانات Brevo SMTP ناقصة (`BREVO_USER/PASSWORD` فارغان) رغم أن `MAIL_PROVIDER=brevo`. لا تنبيهات ولا بريد فعليًا.
- مدة صلاحية الأكواد كانت تُضبط عبر env/AppSetting فقط — بلا واجهة.
- أسعار الخطط كلها 0 — الإيراد وقيمة المشتريات بلا معنى.

## 2) ما نُفّذ

### أ) إعدادات التنبيهات والبريد من لوحة التحكم (بدون SSH)
- **قسم جديد "التنبيهات والإشعارات"** في صفحة الإعدادات: رابط Webhook (Discord/Slack…) + بيانات Brevo SMTP (المستخدم/كلمة المرور) + بريد المُرسِل.
- تُحفظ في القاعدة (AppSetting) و**تُقرأ عند التشغيل**:
  - `alert-notifier` يقرأ `alert_webhook_url` من القاعدة (مع fallback للبيئة)
  - خدمة البريد تقرأ `brevo_user`/`brevo_password`/`mail_from` من القاعدة (مع fallback)
- **زر "إرسال تنبيه تجريبي"** (`POST /admin/app-settings/test-alert`) و**زر "إرسال بريد تجريبي"** (`POST /admin/app-settings/test-email`) — تتحقق من الإعدادات فورًا.

### ب) مدة صلاحية أكواد الموزعين من الواجهة
- حقل في صفحة الإعدادات: `code_expiry_days` (1-365) — الأكواد الجديدة تنتهي بعده تلقائيًا ويُعاد الرصيد.

### ج) ضبط الأسعار (مقترحة — قابلة للتعديل من صفحة الباقات)
| الباقة | السعر المقترح |
|---|---|
| تجربة (7 أيام) | 0 دج |
| شهري | **500 دج** |
| 6 أشهر | **2,500 دج** |
| سنوي | **4,500 دج** |
| Android TV — اختبار داخلي | 0 دج |

النتيجة: `pricesSet: true` — الإيراد وقيمة المشتريات تُحسب تلقائيًا من الآن.

## 3) الاختبارات

- الباكند: **34 مجموعة / 251 اختبارًا ناجحًا** + tsc نظيف (باكند/واجهة) + ESLint بلا أخطاء.

## 4) النشر

- GitHub `main` = `f6747a4` (4 ملفات، +295 سطرًا) → مزامنة إلى الخادم (checksums 4/4 بعد إصلاح نسخ صفحة الإعدادات) → النشر الرسمي.

## 5) ملفات تغيّرت

```
backend/src/routes/admin-app-settings.js   (مفاتيح جديدة + test-alert + test-email)
backend/src/services/alert-notifier.ts     (webhook من AppSetting)
backend/src/services/email.ts              (Brevo + MAIL_FROM من AppSetting، sendEmail يعيد {ok,error})
frontend/src/app/(dashboard)/admin/settings/page.tsx (قسم التنبيهات + مدة الصلاحية)
```

## 6) ملاحظات

- **لتفعيل التنبيهات فعليًا**: ضع رابط webhook (Discord/Slack) في صفحة الإعدادات واضغط "إرسال تنبيه تجريبي".
- **لتفعيل البريد**: ضع بيانات Brevo (المستخدم = SMTP login، كلمة المرور = SMTP key) + بريد المُرسِل، ثم "إرسال بريد تجريبي".
- الأسعار المقترحة قابلة للتعديل فورًا من صفحة الباقات — راجعها وعدّلها حسب سوقك.
- التحقق الحي بعد النشر: حفظ webhook → تنبيه تجريبي → حفظ Brevo → بريد تجريبي → التحقق من `/health/ready`.
