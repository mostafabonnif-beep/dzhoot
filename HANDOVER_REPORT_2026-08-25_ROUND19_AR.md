# HANDOVER REPORT — 2026-08-25 (Round 19)

> تقرير شامل: ما تم عمله في المشروع والإنتاج، وما تبقى.
> أُعد بواسطة الوكيل بناءً على طلب صاحب المشروع.

---

## 1) ما تم عمله — ملخص الجلسة

### 1.1 الوصول والتأكد (مكتمل ✅)
- **SSH للـ VPS** (`5.135.79.221`): تأكد الوصول عبر المفتاح مع اليوزر `dzhoof-admin` (sudo بدون كلمة مرور).
- **لوحة الأدمن** (`https://iptv.ld-11.net/admin`): تأكد الدخول — البيانات الحقيقية في `SUPER_ADMIN_USERNAME/PASSWORD` بإعدادات الإنتاج (`/etc/dzhoot/.env.production`)؛ ملف `ADMIN_PASSWORD.txt` القديم **غير صالح** ويستحق الحذف.
- **الستاك**: 6 حاويات Docker (API / Scheduler / Frontend / Caddy / Redis / MongoDB) كلها صحية.
- الموقع حي: `https://iptv.ld-11.net` → HTTP 200 + `/health` → `{"status":"ok"}`.

### 1.2 فحص اللوحتين وتقرير الفجوات (مكتمل ✅)
- **لوحة الأدمن**: 16,640 قناة، 66,908 فيلم، ~7,000 مسلسل، 8 مستخدمين، 5 باقات، 3 محلات، مجدول 11 مهمة، EPG، إحصائيات، سجل نشاط، إشعارات، تسجيلات، 3 لغات.
- **بوابة الموزعين**: رصيد أكواد، توليد، دفعات (تصدير/نسخ)، سجل حركات، ديون زبائن مع واتساب، تغيير كلمة المرور، مشتريات.
- **المقارنة بلوحة موزعي MIBOX (IntroTik)**: حددنا 10 بنود ناقصة قابلة للنقل (بيانات الزبون، مدة مخصصة، باقات لكل كود، مستخدم مباشر، فواتير، تذاكر، مشاهدون الآن، MAC/Serial، تحويل رصيد، وايته لابل/API).

### 1.3 Round 19 — الميزات الجديدة (منشورة في الإنتاج ✅)
الإصدار: `v1.0.1-20260825T163654Z` — فرع `feat/reseller-customer-codes`.

| الميزة | الوصف | الحالة |
|---|---|---|
| بيانات الزبون على الكود | اسم الزبون + هاتفه عند التوليد من بوابة الموزعين؛ تظهر في لوحة الأدمن وفي ملف تصدير الدفعة | ✅ منشور |
| مدة مخصصة | توليد كود بمدة 1–730 يوم؛ مقيد بعلامة `allowCustomDuration` على الباقة (يفعّلها الأدمن) | ✅ منشور |
| كشف الحساب | قسم جديد في بوابة الموزع: مُمنح/مُستهلك/مُعاد/قيمة المشتريات + جدول حركات + تصدير CSV | ✅ منشور |

**التحقق قبل النشر**: Backend typecheck/build/lint ✅ · **304/304 اختبار** (5 جديدة) ✅ · Frontend tsc/lint/`next build` ✅
**النشر**: سكربت `deploy-production.sh --apply` (نسخ احتياطي mongodump + فحص صحة + بدون OOM). النسخة الاحتياطية: `/var/backups/dzhoot/mongodb/deploy-20260825T163654Z`.

### 1.4 فحوصات إضافية
- فحص لوحة موزع **IntroTik (MIBOX IPTV)** كمرجع منافس — ملخص في القسم 2.
- رصد **ديبلاي متوازن** من جلسة ثانية نشطة على نفس الخادم أثناء النشر (راجع الأمان، بند 3.5).

---

## 2) ما تبقى — خارطة العمل القادمة

### 2.1 أمان — أولوية قصوى 🔴
| البند | الخطورة | الإجراء |
|---|---|---|
| **مفتاح SSH الخاص مكشوف في الريبو العام** (`dzhoof-admin-key` في جذر GitHub) وهو **مركّب وفعّال** على الخادم بصلاحيات sudo | حرجة | توليد مفتاح جديد + حذف القديم من `authorized_keys` + حذف الملف من الريبو + نقل المفتاح لـ GitHub Secrets لو لزم |
| **جلسة/جهة ثانية نشطة** على الخادم (دخول من `35.247.55.65` بنفس المفتاح) | عالية | إن لم تكن لك، افترض اختراقًا: غيّر المفتاح فوراً + راجع `auth.log` |
| **2FA غير مفعّل** على لوحة الأدمن | عالية | تفعيل من الإعدادات قبل إطلاق النسخة النهائية |
| ملف `ADMIN_PASSWORD.txt` قديم على الخادم | متوسطة | حذفه (الباسورد الحقيقي في `.env.production`) |
| لوحة MIBOX على HTTP بباسورد ضعيف (`01020304`) | عالية | تغيير الباسورد فوراً + طلب HTTPS من المزوّد |

### 2.2 ميزات الموزعين (المرحلة القادمة — بالترتيب المقترح)
1. **تذاكر دعم** بين الموزع والإدارة.
2. **قائمة عملاء الموزع** (كل زبون + أكواده + اشتراكه + موعد التجديد).
3. **مشاهدون الآن** (الـ streams المباشرة بالـ IP والجهاز).
4. **تحويل رصيد بين الموزعين**.
5. **اختيار باقات Live/VOD للكود الواحد** (تغيير نموذج الاشتراك — الأكبر حجمًا).
6. **وايته لابل** (شعار/اسم لكل موزع) + **API للموزعين**.
7. **موزعين فرعيين** (مستويين).

### 2.3 تجاري — بوابة دفع جزائرية 💳
- تكامل **Edahabia / BaridiMob / CCP** — حالياً العمليات يدوية (ديون + متابعة واتساب).

### 2.4 تشغيلي — جودة المحتوى
| البند | الرقم الحالي | المطلوب |
|---|---|---|
| تغطية EPG | ~9% فقط (15,215 قناة من 16,640 غير مغطاة) | رفع التغطية (مصادر EPG إضافية/مطابقة) |
| سلامة البث (المختبر) | 1 تعمل / 41 متوقفة / 3 غير مستجيبة | إصلاح المصادر المتوقفة أو استبدالها |
| قنوات معطلة في الكتالوج | 2 | تنظيف/بديل |
| Multi-source / Fallback | 0 قناة لها بديل | تفعيل البدائل للقنوات المهمة |

### 2.5 عمليات
- **دفع فرع الميزات إلى GitHub**: تم في هذا التقرير (فرع `feat/reseller-customer-codes` + مزامنة `main`).
- **النسخ الاحتياطي عن بُعد** (off-site restic) — جاهز الإعداد، يحتاج مزود تخزين.
- **اختبار حقيقي على أجهزة** Android TV / Fire TV + APK Release موقّع.
- **مراقبة تزامن الديبلوي**: منع ديبلايين متوازيين (قفل/صف).

---

## 3) حالة الإنتاج (وقت التقرير)

- المنصة حية وصحية: `{"status":"ok","version":"1.0.1"}` — MongoDB وRedis متصلان، المصادر (M3U + Xtream) مستقرة.
- الإصدار المنشور: `v1.0.1-20260825T163654Z` (API + Frontend + Scheduler).
- عدد الموزعين: 3 محلات (dzsat + محل بن عكنون ×2) — باقات: سنوي/6 أشهر/شهري/تجربة/اختبار داخلي.
- المستخدمون: 8 (كلهم نشطون)، الأجهزة المقترنة: 9.

---

_الإصدار: 2026-08-25 — DZ HOOF Agent_

---

# UPDATE — Round 20 (نفس اليوم): قائمة عملاء الموزع (عملائي)

## ما تم
- **`GET /reseller/clients`**: تجميع الزبائن من الأكواد المولّدة ببيانات العميل (الاسم/الهاتف)، إثراء بتاريخ انتهاء الاشتراك الفعلي (من Subscription)، تمييز **قرب الانتهاء (7 أيام)**، بحث بالاسم/الهاتف، إحصائيات.
- **بوابة الموزعين — قسم "عملائي"**: بحث، شارات (الزبائن/الأكواد/قرب الانتهاء)، صف لكل زبون (الاسم، الهاتف، عدد الأكواد، المفعّل، تاريخ الانتهاء)، **زر واتساب لتذكير بالتجديد**، جدول أكواد قابل للتوسيع (الكود/الباقة/الحالة/الانتهاء).
- **i18n**: عربي/إنجليزي/فرنسي.
- **اختبارات**: `reseller-clients.test.ts` (3 اختبارات) — إجمالي الـ Backend: **43 مجموعة / 314 اختبار ✅**.
- **النشر**: `v1.0.1-20260825T171827Z` — تحقق حي: القسم ظاهر، زبون تجريبي بكوادين ظهر مع زر واتساب، الـ API حي (401 بدون تسجيل دخول).

## ملاحظات التنسيق (مهم)
- الجلسة الثانية (وكيل موازٍ على نفس الخادم) نشرت نسختها فوق نشري مرتين اليوم (16:46 و 17:14) — **تم دمج كل الشغل على `main`** (آخرها `4fbe524`) وأُعيد النشر من الكود المدمج، فالإنتاج الآن يحتوي كل شيء.
- **مطلوب من صاحب المشروع**: تحديد الجلسة الثانية (ملكك؟) وقفل/تنسيق الديبلوي بين الجلسات، وتدوير مفتاح SSH المكشوف.

## ما تبقى (بعد Round 20)
- تذاكر الدعم، مشاهدون الآن، تحويل رصيد بين الموزعين، باقات Live/VOD للكود، وايته لابل + API، موزعين فرعيين.
- بوابة دفع جزائرية (Edahabia/BaridiMob/CCP).
- تغطية EPG (~91% ناقصة)، إصلاح البث المتوقف، تفعيل Fallback، 2FA، نسخ احتياطي عن بُعد.
