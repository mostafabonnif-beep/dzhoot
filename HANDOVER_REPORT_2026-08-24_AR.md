# تقرير التسليم النهائي — جلسات 2026-08-23/24

> **اقرأ هذا أولًا.** يجمع هذا التقرير كل ما أُنجز في جلستي 2026-08-23 (مساءً) و2026-08-24 + كل ما تبقى، ليعرف أي وكيل/شخص قادم الوضع كاملًا من نقطة واحدة. المراجع الأقدم: `FINAL_REPORT_2026-08-23_AR.md` و`PROJECT_STATE_HANDBOOK_AR.md`.

---

## 1. الملخص التنفيذي

| الجبهة | النتيجة |
|---|---|
| **الوصول للسيرفر** | ✅ مفتاح `dzhoof-admin` (sudo بدون كلمة مرور) — `ssh dzhoof` |
| **تطبيق Android** | ✅ APK رسمي موقّع **v1.0.9** (إصلاح HLS) — منشور للتحميل |
| **EPG** | ✅ عناوين حقيقية لقنوات beIN التركية + مطابقة غير حساسة للحالة + حفظ tvgId اليدوي (3 جولات) |
| **ميزة المحلات والدفعات** | ✅ نظام بيع الأكواد للمحلات: محل + دفعات + ملف تسليم + خطط (أسبوع/شهر/6أشهر/سنة) |
| **التوثيق** | ✅ هذا التقرير + تحديث الذاكرة الدائمة |

**الحالة**: المستودع (GitHub `main`) = الخادم (VPS `/opt/dzhoot`) = 100% متطابقان (تحقق checksums)، والإنتاج منشور وصحي.

---

## 2. الوصول والبنية (ثابتة — لا تتغير)

- **SSH**: `ssh dzhoof` → `dzhoof-admin@5.135.79.221` بمفتاح `/home/user/.ssh/dzhoof-admin` (رفعه المستخدم؛ تعليقه `dzhoof-admin@vps`). صلاحيات sudo كاملة بدون كلمة مرور.
- **بيئة الإنتاج**: `/etc/dzhoot/.env.production` (chmod 600، فيها `EPG_EXTRA_SOURCES`). المصدر: `/opt/dzhoot/server` (نسخة ملفات — **ليست git** — تُزامن يدويًا من المستودع). الحاويات: `dzhoof-api/frontend/scheduler/redis/caddy/mongodb`.
- **النشر الآلي**: `cd /opt/dzhoot/server && sudo bash scripts/deploy/deploy-production.sh --apply` (نسخ احتياطي + بناء + ترقية + تحقق + سجل `/var/log/dzhoof-deploys.log`).
- **بناء APK**: انظر قسم 4. **GitHub push**: توكن الخادم `/etc/dzhoot/github.token`.
- **IP بيئة الوكيل**: `136.118.71.205` (قد يُحظر من fail2ban عند محاولات SSH خاطئة — لا تجرّب أسماء مستخدمين؛ فك الحظر: `fail2ban-client unban 136.118.71.205`).

---

## 3. سجل commits على main (هذه الجلسات)

| Commit | المحتوى |
|---|---|
| `6800f11` | EPG: حفظ tvgId اليدوي + مطابقة case-insensitive |
| `922d9db` | `.gitkeep` لمجلد `/downloads` (لا يضيع بعد النشر الذرّي) |
| `ae24344` | توثيق `EPG_EXTRA_SOURCES` في مثال البيئة |
| `48f9559` | EPG: التعيين اليدوي ينتصر دائمًا (عناوين حقيقية بدل No Data) |
| `1b3bc3d` | تقرير التسليم الأول |
| `1f8e369` | **ميزة المحلات والدفعات** (Reseller + CodeBatch + واجهات) |
| `8e55a67` | إصلاح تصدير النماذج (CommonJS) |
| `a74c210` | تحديث تقرير التسليم |

---

## 4. تطبيق Android — APK v1.0.9

- **السبب الجذري السابق**: R8 minify حذف وحدات Media3 HLS من الإصدارات القديمة؛ v1.0.9 يفرض `HlsMediaSource` صراحةً.
- **البناء**: `cd /opt/dzhoot/android && sudo bash -c 'source /etc/dzhoot/android-signing/signing.env; export ANDROID_HOME=/opt/android-sdk DZHOOF_API_URL=https://iptv.ld-11.net/; ./gradlew assembleRelease -PversionName=1.0.9'`
- **التحميل**: `https://iptv.ld-11.net/downloads/dzhoof-v1.0.9.apk` (77.4MB، sha256 `5a4b56ef…`). نسخة في `/var/backups/dzhoot/apks/`.
- ملاحظة: R8 معطّل حاليًا (APK كبير) — إعادة تفعيله مع keep-rules من التحسينات المستقبلية.

---

## 5. EPG — ما تم وما تبقى

**تم**: مصادر epgshare (TR1 + BEIN1) في `EPG_EXTRA_SOURCES`؛ تعيينات مجموعة `TR| BEIN SPORTS GOLD` (37 قناة) بمعرّفات TR1 (`Beinsports.tr`…)؛ المطابقة غير حساسة للحالة (collation)؛ tvgId اليدوي محفوظ من المزامنات. **الناتج**: قناة "TRI BEIN SPORTS GOLD" تعرض "beIN Süper Lig" وغيرها (9-19 برنامجًا)، التغطية 981 → 1184 قناة.

**يبقى**:
- [ ] beIN SPORTS 5 + MAX 1/2: لا مصدر ببيانات حقيقية (تبقى "No Data")
- [ ] **القنوات العربية/الجزائرية**: ملفات iptv-epg.org لـ dz/ma/tn/jo غير موجودة (404) — البحث عن مصدر عربي شامل. ⚠️ `epg_ripper_AR1.xml.gz` هو **أمريكا اللاتينية** وليس عربيًا
- [ ] تعطيل المصادر الـ 13 الفاشلة عبر `epgsourceoverrides` (disabled + lastError)

---

## 6. ميزة المحلات والدفعات (جديدة — منشورة)

- **Reseller** (`/api/v1/admin/resellers`): اسم + مدينة + هاتف + ملاحظات + حالة + إحصائيات (إجمالي/مفعّل/متبقي).
- **CodeBatch** (`/api/v1/admin/code-batches`): دفعة = محل + خطة (مدة) + عدد + **تاريخ الاستلام** + رقم تسلسلي لكل محل. `POST` يولّد الدفعة وأكوادها ويعيدها كنص؛ `GET /:id/export` يصدّر **ملف تسليم جاهزًا** (ترويسة + الأكواد).
- **الخطط** (في قاعدة الإنتاج): تجربة 7d / شهري 30d / 6 أشهر 180d / سنوي 365d — **الأسعار 0** (تُضبط من صفحة الباقات).
- **اللوحة**: `/admin/resellers` + `/admin/code-batches` + أعمدة المحل/الدفعة في صفحة الأكواد.
- الأكواد مرتبطة بـ `resellerId` + `batchId`؛ مدة الاشتراك تُحتسب من الخطة عند التفعيل تلقائيًا.
- **مُتحقق حيًا**: محل "بن عكنون / الجزائر العاصمة" + دفعة #1 (شهري، 3 أكواد) + ملف التصدير ✓
- ملاحظة تقنية: أي نموذج Mongoose جديد يجب أن يصدّر بـ `module.exports = Model; export default Model;` (نمط Plan.ts) — وإلا يفشل `require()` في الحاويات.

---

## 7. حالة الإنتاج الحالية (مُتحقق)

6 حاويات صحية | API 1.0.1 | 16,639 قناة | 55 مستخدمًا | EPG 118K برنامج / ~1184 قناة | قرص 63% | ذاكرة 4.2G متاحة | مصادر 0 أخطاء | نسخ احتياطي محلي + deploy تلقائية.

---

## 8. ما تبقى (بالأولوية)

### 🔴 أمني (عاجل — كلمات المرور مكشوفة في المحادثة)
- [ ] **تغيير كلمة مرور root** للسيرفر
- [ ] **تغيير كلمة مرور admin** للوحة
- [ ] مراجعة `github.token`

### 🔴 تجاري (للبيع الفعلي)
- [ ] **ضبط أسعار الخطط** (صفحة الباقات — حاليًا 0)
- [ ] **بوابة دفع** (CinetPay/Paymee/Stripe) — لا يوجد تحصيل
- [ ] **حد الأجهزة عند التشغيل** (كود واحد ≠ أجهزة كثيرة)
- [ ] **المرحلة 2 للريسيلر**: دخول خاص لكل محل يرى أكواده وإحصائياته فقط + سعر جملة لكل محل

### 🟡 EPG
- [ ] مصدر عربي/جزائري شامل (أكبر فجوة محتوى)
- [ ] beIN SPORTS 5 + MAX 1/2
- [ ] تعطيل الـ 13 مصدرًا الفاشل

### 🟡 كتالوج
- [ ] القناة المعطلة + نسخ "NM:" الميتة المكررة + تنظيف أسماء القنوات المتسخة (`## TR| …`)

### 🟢 تشغيلي/تقني
- [ ] **التنبيهات** (alertingConfigured: false)
- [ ] **نسخ احتياطي خارجي** (restic غير مكتمل) + **اختبار استعادة**
- [ ] إعادة تفعيل R8 مع keep-rules (تصغير APK 77MB → ~30MB)
- [ ] ترقية Media3 1.5+ | رفع APK إلى GitHub Releases
- [ ] إكمال الترجمة العربية للصفحات المتبقية

---

## 9. أوامر سريعة

```bash
ssh dzhoof                                   # السيرفر
curl -s https://iptv.ld-11.net/health/ready  # صحة
# تحديث EPG: POST /api/v1/scheduler/trigger/epg-refresh (X-Session-Id admin)
# نشر: cd /opt/dzhoot/server && sudo bash scripts/deploy/deploy-production.sh --apply
# دفعة جديدة: POST /api/v1/admin/code-batches {resellerId, planId, quantity, receiptDate}
# تقرير التسليم السابق: HANDOVER_REPORT_2026-08-23_AR.md (يُحفظ للسياق)
```

**الذاكرة الدائمة لبيئة الوكيل**: `memory/wiki/dzhoot-project.md` + يوميات `memory/2026-08-23.md` و`memory/2026-08-24.md`.

---

## 10. أفكار سريعة منفّذة (2026-08-24 — commit `45208be`، نشر v1.0.1-20260824T034650Z)

ثلاث "أفكار سريعة" نُفذت بالكامل (اختبارات 237/237 خضراء):

1. **سعر الجملة لكل محل** (سعر خاص لكل خطة): حقل `prices` في نموذج Reseller + محرر أسعار في صفحة المحلات + عرض السعر والإجمالي في صفحة الدفعات. **مُتحقق حيًا**: 450 دج × 10 أكواد = 4500 دج.
2. **تقرير يومي عبر البريد** (مهمة `daily-report`): كل صباح يرسل للمدراء: أكواد مُفعّلة أمس (إجمالي + لكل محل) + مستخدمون جدد + اشتراكات نشطة — قالب عربي.
3. **تنبيه انتهاء الاشتراك** (مهمة `subscription-expiry-alert`): يرسل للمستخدمين الذين تنتهي اشتراكاتهم خلال 3 أيام — قالب عربي.

⚠️ **مهم لإكمال تفعيل الإيميلات**: المهام تعمل وتُنفَّذ (مُتحقق)، لكن الإرسال يفشل لأن **مفاتيح Brevo فارغة** في `/etc/dzhoot/.env.production` (`BREVO_USER` و`BREVO_PASSWORD`). لتفعيل البريد:
- أنشئ حساب Brevo مجاني (300 إيميل/يوم) → SMTP keys
- ضع القيم: `BREVO_USER` (بريد الحساب) و`BREVO_PASSWORD` (مفتاح SMTP) في `/etc/dzhoot/.env.production`
- أعد تشغيل: `docker compose up -d api scheduler` (من `/opt/dzhoot/server`)
- يفضل أيضًا ضبط `MAIL_FROM` لبريد موثّق (مثل no-reply@ld-11.net)
- بدون ذلك، باقي الميزات (التحقق بالبريد، استعادة كلمة المرور) تبقى معطلة أيضًا — القيم الحالية فارغة أصلًا
