# تقرير التسليم — جولة استكمال 2026-08-30 (الخامسة)
**النطاق:** تطبيق التحسينات المتبقية على المشروع وVPS · **الحالة: ✅ مكتملة — الإنتاج على `f772a52` + جاهزية نشر PRs #121–#124**

---

## ملخص تنفيذي

جولة تحسينات على البنود المتبقية. أربعة تغييرات دُمجت (أو في طور الدمج):

1. **أتمتة تجاوز compose الإنتاجي في النشر** (#121) — يمنع تكرار حادثة 14:10 (API تتعطل عند نشر نسخة المستودع بلا auth على Mongo محميّ). الآن `atomic-deploy.sh` ينسخ تلقائياً `/etc/dzhoot/docker-compose.production.yml` فوق compose الإصدار قبل `compose up` (الملف مثبّت على الخادم 600).
2. **تحسين CI** (#122) — فلاتر مسارات: android/frontend يُتخطيان عندما لا تمسّ التغييرات شجرتيهما. الجولة الرابعة كانت تنتظر ~25 دقيقة android لكل PR؛ الآن PRs الخلفية/التوثيقية تنجز في دقائق. **مُتحقق عملياً**: PR #122 نفسه أظهر `android: SKIPPED` + `frontend: SKIPPED`.
3. **تباين الثيم الفاتح في Android** (#123) — بند مؤجل: أغمقت ألوان النص/الأيقونات الفاتحة لتلبية WCAG (نص خافت 4.84:1، أيقونات 3.66:1، نص معطّل 2.98:1) + اختبار انحدار تباين. يصل المستخدمين مع إصدار APK القادم.
4. **توثيق** (#124) — ملاحظة الأمان في دليل الحالة: Mongo/Redis أصبحا محميّين بمصادقة (2026-08-30).

## PRs

| PR | الموضوع | الحالة |
|----|---------|--------|
| #121 feat(deploy) compose override | ✅ مدمج |
| #122 ci path-filters | ✅ مدمج ومُتحقق |
| #123 fix(android) light contrast | ⏳ CI يعيد التشغيل (اختبار مُعاد كتابته بثوابت صريحة) |
| #124 docs mongo/redis auth | ✅ مدمج |

## ملاحظات تشغيلية

- بعد #121: النشر القادم لا يحتاج الخطوة اليدوية (نسخ الـ compose) — الآلية مدمجة. الـ override في `/etc/dzhoot/docker-compose.production.yml`.
- بعد #122: PRs التي تمسّ android/frontend فقط تشغّل وظائفها فقط؛ الخلفية والأمان دائماً.

## ملاحظات مفتوحة (لجولات لاحقة)

1. **الترقيات الكبرى** (okhttp5, eslint10, typescript7, node26, hilt, coroutines, composeTv) — PRs #98/#99/#102/#105/#106/#107/#108 موثّقة بتعليقات، تحتاج هجرة يدوية.
2. **مصدر EPG جزائري/عربي** — الطريق الوحيد لرفع التغطية فوق 14%.
3. `dzhoof-shared-network` — الملاحظة باقية (قواعد البيانات أصبحت محمية بالمصادقة).
4. القرار القانوني للمصادر — للمالك.
