# DZ HOOF Client 2.0 — جاهزية Release

**التاريخ:** 2026-08-27  
**الفرع:** `manus/observability-foundation`  
**الحالة:** غير مفعّل للإنتاج

## الفحص

تم تشغيل مهمة `:app:assembleOfficialRelease` مع رابط Backend الإنتاجي عبر HTTPS. نجحت مراحل التجميع والتحويل حتى خطوة تغليف APK، ثم أوقفها Android Gradle Plugin لأن SigningConfig باسم `release` لا يملك `storeFile` في بيئة الجلسة الحالية.

هذا توقف آمن ومقصود؛ فلا يجوز استخدام APK debug أو إنشاء مفتاح جديد عشوائياً، لأن أجهزة المستخدمين الموجودة يجب أن تستقبل تحديثاً موقّعاً بالشهادة نفسها المستخدمة في النسخ المنشورة سابقاً.

## الحالة الحالية

| العنصر | النتيجة |
|---|---|
| مصدر الإصدار الافتراضي | `1.0.18` / `10018` |
| flavor المطلوب | `official` |
| Backend URL | HTTPS؛ لم يُغيّر |
| Release compilation | وصل إلى التغليف قبل فشل التوقيع |
| سبب الفشل | متغير `SIGNING_KEY_STORE` وملف keystore غير متاحين في الجلسة |
| APK debug للمراجعة | موجود داخلياً فقط، غير صالح كتحديث إنتاجي |
| VPS / `/downloads` / `AppVersion` | لم تُمس |

## تحصين Workflow الإصدار

تم تعديل Workflow الإصدار ليبني `:app:assembleOfficialRelease` فقط، بدلاً من مهمة عامة قد تنتج flavors إضافية. كما أصبح يتحقق من أن versionName يبدأ بصيغة major.minor.patch وأن versionCode المحسوب أكبر من الإصدار المنشور `10017`. تحقق محلياً من أمثلة `1.0.17` و`1.0.18` و`1.0.18-rc.1` دون كشف أي أسرار.

## شرط الإصدار التالي

لتوليد Release صالح، يجب تشغيل البناء في بيئة CI أو جلسة تحتوي ملف keystore الإنتاجي والمتغيرات الأربعة المرتبطة به: مسار الملف، كلمة مرور المتجر، alias، وكلمة مرور المفتاح. يجب عدم تسجيل القيم في السجل أو إرسالها في المحادثة.

بعد توفر البيئة الصحيحة، يلزم التحقق من `applicationId` و`versionName` و`versionCode` وشهادة التوقيع وSHA-256، ثم اختبار تثبيت التحديث فوق نسخة أقدم على جهاز فعلي قبل تسجيل الإصدار في Endpoint التحديث.

## فحص Endpoint الحالي

تمت قراءة Endpoint التحديث دون أي كتابة أو تفعيل. النتيجة الحالية تعلن `1.0.17` برقم `10017`: الجهاز الذي يعلن `10016` يرى تحديثاً، والجهاز الذي يعلن `10017` لا يرى تحديثاً، كما أن APK المراجعة الداخلية `10018` لا يرى تحديثاً من الإنتاج. هذا يؤكد أن النسخة الداخلية لم تُسجل في النظام العام.

## تحسينات جلسة المتابعة

أضيفت بطاقات نتائج القنوات في البحث إلى تجربة أكثر وضوحاً: تعرض البطاقة شعار القناة عند توفره، واسم المجموعة، وتستخدم اختصاراً للنص مع `Ellipsis` حتى لا يكسر الاسم الطويل تخطيط الهاتف أو التلفاز. لم يتغير معرّف القناة أو callback التنقل، ولذلك بقيت المفضلة وفتح المشغل متوافقين مع السلوك السابق.

كما أضيفت معالجة تركيز موحّدة لبطاقات الأفلام والحلقات في الكتالوج. عند وصول التركيز عبر D-pad تحصل البطاقة على تكبير وظل بصري من مكوّن التركيز الموحد `tvFocusVisuals`، بينما يظل سلوك اللمس كما هو. هذا يعالج قابلية اكتشاف العنصر المحدد من مسافة المشاهدة دون إدخال مكتبة خارجية أو تغيير نموذج البيانات.

| التحقق | النتيجة |
|---|---|
| `:app:compileOfficialDebugKotlin` بعد تحسين البحث | ناجح |
| `:app:testOfficialDebugUnitTest` بعد تحسين البحث | ناجح |
| `:app:compileOfficialDebugKotlin` بعد تحسين التركيز | ناجح بعد إصلاح استيراد Compose الخاص بـ `setValue` |
| `:app:testOfficialDebugUnitTest` بعد تحسين التركيز | ناجح |
| Git working tree بعد الدفع | نظيف |
| آخر commit | `4f20f47` — `feat(android): improve catalog d-pad focus` |
| commit السابق | `50dabf7` — `feat(android): enrich channel search results` |
| النشر الإنتاجي وVPS | لم يُنفّذ؛ لا يزال محمياً من أي تغيير |

تظل الخطوات التي تحتاج جهازاً فعلياً كما هي: اختبار انتقالات D-pad في Guide والكتالوج، التحقق من HLS على شبكات مختلفة، وتجربة تحديث 1.0.17 إلى 1.0.18 بعد توفير keystore الإنتاجي الصحيح. لا يُعد أي من commits أعلاه إصداراً عاماً أو بديلاً عن توقيع الإنتاج.

**المؤلف:** Manus AI

## سجل المراجع

لا توجد مصادر خارجية مستخدمة في هذه الإضافة؛ النتائج مبنية على ملفات المشروع وسجلات البناء والاختبارات المحلية.

## تحسين Guide وEPG في جلسة المتابعة التالية

تم تحسين جدولة تحميل بيانات EPG أثناء التمرير وتبديل الفلاتر. كانت كل حركة في نطاق العرض تطلق مهمة تحميل مستقلة؛ أما الآن فيُلغى طلب النطاق السابق عند وصول نطاق أحدث، بحيث تظل الأولوية للصفوف التي يمر عليها تركيز D-pad حالياً، وتقل احتمالية نشر بيانات متأخرة فوق الفلتر أو الصف النشط. كما تُلغى مهمة الترطيب السابقة عند إعادة تحميل Guide بالكامل.

هذا التغيير لا يغيّر واجهة الخادم أو نموذج الحسابات، ولا يلغي التخزين المؤقت الموجود أو التحميل الأولي لأول 30 صفاً. وهو تحسين لحالة التنافس داخل ViewModel فقط.

| التحقق | النتيجة |
|---|---|
| `:app:compileOfficialDebugKotlin` بعد تعديل Guide/EPG | ناجح |
| `:app:testOfficialDebugUnitTest` بعد تعديل Guide/EPG | ناجح |
| النشر إلى VPS أو Endpoint التحديث | لم يُنفّذ |
| الإصدار العام | ما زال `1.0.17` |
| آخر تغيير تطبيقي | ينتظر commit والدفع إلى فرع التطوير بعد توثيقه |

يبقى الاختبار على جهاز Android TV فعلي ضرورياً للتأكد من الإحساس البصري للتركيز أثناء flings والتنقل السريع بين الصفوف، لأن اختبارات الوحدة لا تقيس زمن الشبكة أو سلوك جهاز التحكم عن بعد.

## تحسين تشخيص أخطاء المشغل

تم فصل أخطاء اتصال الشبكة عن أخطاء HTTP القادمة من خادم البث. أصبحت الاستجابات `401` و`403` تُعرض كرفض وصول إلى المصدر، و`404` كمصدر غير موجود، بينما تُعامل `408` و`429` وأخطاء `5xx` كحالات مؤقتة قابلة لإعادة المحاولة. أما أخطاء HTTP الدائمة فلا تُصنّف تلقائياً على أنها انقطاع شبكة، ويُنتقل إلى المصدر البديل عند توفره بدلاً من إعادة طلب المصدر نفسه بلا فائدة.

| التحقق | النتيجة |
|---|---|
| `:app:compileOfficialDebugKotlin` بعد تحسين المشغل | ناجح |
| `:app:testOfficialDebugUnitTest` بعد تحسين المشغل | ناجح |
| نشر التغيير إلى VPS أو Endpoint التحديث | لم يُنفّذ |
| آخر تعديل | جاهز للحفظ والدفع إلى فرع التطوير |

تظل رسائل الخطأ باللغة العربية ومتصلة بمسار الاسترداد الحالي، مع الحفاظ على fallback للـ proxy والمصادر البديلة وعدم تغيير حسابات المستخدمين أو واجهات Backend.

## تغطية اختبارية إضافية للمشغل

تمت إضافة اختبارات إلى `ErrorRecoveryManagerTest` للحالات التالية: استجابة `404` الدائمة التي تُظهر رسالة مصدر غير موجود وتنتقل إلى المصدر البديل، واستجابة `503` المؤقتة التي تُظهر خطأ خادم مؤقت وتبدأ إعادة المحاولة. أثناء التحقق كُشف أن أخطاء HTTP الدائمة كانت تُنهى مباشرة رغم وجود مصدر بديل؛ تم إصلاح ذلك بإضافة مسار fallback صريح لها.

| التحقق | النتيجة |
|---|---|
| اختبار `ErrorRecoveryManagerTest` المحدد | ناجح؛ 17 اختباراً |
| تجميع `compileOfficialDebugKotlin` | ناجح |
| `testOfficialDebugUnitTest` الكامل | ناجح |
| حالة GitHub | التغيير مدفوع إلى `manus/observability-foundation` |
| آخر commit | `a9c7da1` — `test(android): cover player http recovery` |
| الإنتاج وVPS | لم يُعدّل أي منهما |

## فحص جودة إضافي

تم تشغيل `:app:lintOfficialDebug` باستخدام Backend HTTPS الرسمي، وانتهى بنجاح. لم تظهر أخطاء Lint جديدة في دفعات Guide وEPG والمشغل الحالية. لم تُجرَ تعديلات كود إضافية في هذه الجولة بعد آخر commit؛ كان الهدف تأكيد سلامة الفرع قبل الانتقال إلى اختبار الأجهزة الفعلية.

| التحقق | النتيجة |
|---|---|
| `:app:lintOfficialDebug` | ناجح |
| حالة الفرع | نظيف ومتزامن مع `origin/manus/observability-foundation` قبل تحديث التقرير |
| الإنتاج وVPS | دون تغيير |
| الخطوة العملية التالية | اختبار Android TV والهاتف، ثم تجهيز `officialRelease` بعد توفير keystore الإنتاجي |

## تحصين رابط التحديث

تم تحصين `AppUpdater` قبل بدء `DownloadManager`: لا يُقبل رابط APK إلا عبر HTTPS ومن نطاق خادم المشروع المكوّن أو نطاقات GitHub الرسمية المستخدمة في fallback. تُرفض الروابط غير الآمنة برسالة عربية واضحة قبل إنشاء أي تنزيل، مع بقاء التحقق من توقيع APK قبل التثبيت كما هو.

هذا التغيير لا يلمس بيانات الحسابات ولا يغيّر Endpoint التحديث، ويمنع إعادة توجيه المستخدم إلى ملف APK من مصدر غير موثوق إذا أعاد خادم خارجي قيمة خاطئة.

| التحقق | النتيجة |
|---|---|
| `:app:compileOfficialDebugKotlin` | ناجح |
| `:app:testOfficialDebugUnitTest` | ناجح |
| Git working tree قبل التوثيق | نظيف |
| الإنتاج وVPS | لم يتم تعديلهما |
| الإصدار العام | ما زال `1.0.17` |

## اختبار سياسة روابط التحديث

فُصلت سياسة قبول رابط APK في `UpdateUrlPolicy` مستقل قابل للاختبار. تغطي الاختبارات قبول رابط HTTPS من خادم المشروع، وقبول نطاقات GitHub الرسمية، ورفض HTTP والنطاقات المجهولة والروابط المضللة أو غير الصالحة. تم كذلك إصلاح اعتماد الاختبار على `Android Uri` باستخدام `java.net.URI` داخل السياسة النقية، وهو ما جعل الاختبار مستقراً في بيئة Android JVM.

| التحقق | النتيجة |
|---|---|
| اختبارات `UpdateUrlPolicyTest` | ناجحة؛ 5 اختبارات |
| `compileOfficialDebugKotlin` | ناجح |
| `testOfficialDebugUnitTest` الكامل | ناجح |
| `lintOfficialDebug` | ناجح |
| آخر حالة الإنتاج وVPS | دون تغيير |
| الإصدار العام | ما زال `1.0.17` |

## حماية هوية APK ومنع التراجع

تم تعزيز مسار تثبيت التحديث بحيث يتحقق من أن APK المنزّل يحمل `packageName` نفسه للتطبيق، وأن `versionCode` المرشح أكبر من النسخة المثبتة حالياً. إذا كان الملف لحزمة مختلفة أو إصداراً قديماً/مساوياً، يُحذف الملف ويُرفض التثبيت قبل فتح مُثبّت Android. يبقى تحقق شهادة التوقيع إلزامياً بعد هذا الفحص.

كما تم التحقق من APK `officialDebug` الداخلي الموجود: يحمل `com.dzhoof.iptv` و`versionCode 10018` و`versionName 1.0.18`، وتحقق توقيع APK v2، لكنه يظل نسخة Debug غير صالحة للنشر العام.

| التحقق | النتيجة |
|---|---|
| `compileOfficialDebugKotlin` | ناجح |
| `testOfficialDebugUnitTest` | ناجح |
| `lintOfficialDebug` | ناجح |
| فحص package/version لـ APK الداخلي | ناجح |
| فحص توقيع APK الداخلي | v2 ناجح؛ Debug فقط |
| `officialRelease` الإنتاجي | ما زال متوقفاً لغياب keystore |
| VPS وEndpoint التحديث | لم يتم تعديلهما |

## تنظيف تحذيرات Release والتحقق من APK

تم استبدال مراجع اللون المهجور `Amber` في طبقات المشغل بالرمز الرسمي `DzGreen300`، مع الحفاظ على القيمة اللونية الفعلية نفسها وإزالة تحذيرين من تجميع Kotlin. لم يتم تغيير سلوك المشغل أو تصميم البطاقات.

أعيد فحص APK الداخلي: الحزمة `com.dzhoof.iptv`، الإصدار `1.0.18`، ورقم البناء `10018`، وتوقيع APK v2 صالح. هذه الحزمة Debug للمراجعة الداخلية فقط ولا يجوز استخدامها كتحديث للمستخدمين.

| التحقق | النتيجة |
|---|---|
| تجميع `officialDebug` | ناجح |
| `testOfficialDebugUnitTest` | ناجح |
| `lintOfficialDebug` | ناجح |
| `officialRelease` | وصل إلى `packageOfficialRelease` ثم توقف بسبب غياب `storeFile` للـ keystore |
| النشر على VPS أو Endpoint التحديث | لم يتم |

## تدقيق مطابقة versionCode في Workflow

تم تقوية خطوة التحقق في Workflow الإصدار الرسمي. بعد بناء APK وتوقيعه، يتحقق المسار الآن من `packageName` و`versionName` و`versionCode` معاً، ويحسب الرقم المتوقع من صيغة `major*10000 + minor*100 + patch`. هذا يمنع رفع ملف يحمل اسم إصدار صحيحاً لكن رقم بناء غير متوافق مع نظام التحديث التلقائي.

تمت محاكاة الصيغة محلياً للقيم `1.0.18 → 10018` و`1.0.18-rc.1 → 10018` و`1.2.3 → 10203`. لم يتم تشغيل Workflow على GitHub أو نشر أي APK، لأن توقيع الإنتاج ما زال يحتاج keystore الصحيح.

## بوابة الجودة النهائية قبل تسليم العملاء

تم تشغيل بوابة الجودة على آخر commit وتشمل بناء `officialDebug`، واختبارات الوحدة الكاملة، وAndroid Lint الرسمي. نجحت جميع المهام. APK الناتج داخلي للمراجعة فقط؛ لا يزال إنشاء `officialRelease` متوقفاً بأمان عند توقيع الإنتاج بسبب غياب ملف keystore ومتغيراته.

| بوابة الجودة | النتيجة |
|---|---|
| `assembleOfficialDebug` | ناجح |
| `testOfficialDebugUnitTest` | ناجح |
| `lintOfficialDebug` | ناجح |
| توقيع Release الإنتاجي | متوقف حتى توفير keystore الصحيح |
| اختبار الترقية على جهاز فعلي | لم يُنفذ بعد |
| النشر إلى VPS أو Endpoint | محظور ولم يُنفذ |

بعد توفير keystore الإنتاجي، ستكون الخطوات المتبقية محددة: تشغيل Workflow الرسمي، التحقق من شهادة التوقيع وv2 وpackage/version وSHA-256، تثبيت النسخة فوق `1.0.17` على جهاز اختبار، ثم فقط اتخاذ قرار نشر APK وتحديث `AppVersion`.

## بوابة حماية وتسليم إضافية

تم تشغيل فحص حارس الأسرار على المستودع، ونجح دون العثور على مفاتيح خاصة أو رموز وصول عالية الثقة ضمن الملفات المتتبعة. ثم أُعيد تشغيل بوابة الجودة الأخيرة على الفرع الحالي، وشملت بناء `officialDebug` واختبارات الوحدة وLint الرسمي، ونجحت جميعها.

| عنصر التسليم | الحالة |
|---|---|
| الأسرار داخل Git | لا توجد مفاتيح خاصة أو رموز وصول عالية الثقة متتبعة |
| `assembleOfficialDebug` | ناجح |
| `testOfficialDebugUnitTest` | ناجح |
| `lintOfficialDebug` | ناجح |
| APK العملاء الرسمي | غير منشأ؛ يحتاج keystore الإنتاجي |
| النشر أو تحديث AppVersion | لم يتم |

أصبح المشروع جاهزاً من ناحية الكود والاختبارات المحلية. لا يمكن إغلاق التسليم للزبائن فعلياً إلا بعد توفير keystore الإنتاجي نفسه، ثم تنفيذ توقيع Release واختبار التثبيت فوق `1.0.17` على جهاز اختبار.

## قائمة تسليم العملاء

تم إنشاء الملف `docs/DZ_HOOF_CLIENT_2_0_CUSTOMER_HANDOFF_CHECKLIST.md` ليكون مرجع التسليم الرسمي. يتضمن ما تم إغلاقه، شروط توقيع `officialRelease`، اختبار الترقية دون فقدان بيانات الحسابات، واختبارات الهاتف والتلفاز والمشغل قبل النشر.

أعيد تشغيل بوابة الجودة على آخر حالة للمشروع ونجحت: `assembleOfficialDebug` و`testOfficialDebugUnitTest` و`lintOfficialDebug`. APK الداخلي يحمل `com.dzhoof.iptv` و`versionCode 10018`، وتحقق توقيع v2، لكنه Debug فقط ولا يمثل حزمة العملاء.

تبقى حالة النشر كما هي: الإصدار العام `1.0.17`، ولا يوجد APK Release موقّع في المخرجات، وVPS و`AppVersion` لم يتغيرا.

## تدقيق استرجاع keystore الإنتاجي

تم البحث قراءةً فقط في المصادر المتاحة محلياً وفي المسارات المحتملة على VPS، ولم يُعثر على keystore إنتاجي؛ الموجود فقط هو `debug.keystore`. كما أن قائمة GitHub Actions لا تعرض أسراراً متاحة للاسترجاع عبر CLI، ولا يمكن استخراج قيم الأسرار من GitHub أو من APK.

تمت مقارنة شهادة APK الإنتاجي القديم `1.0.17` مع `1.0.14`: كلاهما يحملان شهادة الإنتاج نفسها، وبصمة SHA-256 للشهادة هي:

`5938049a7b7eb803d7354efb96ca1989fdf17af1f62ff0e7fb68bd765920bb11`

هذه البصمة تؤكد هوية الشهادة المطلوبة للمقارنة فقط؛ **المفتاح الخاص لا يمكن استعادته من APK**. أما `officialDebug 1.0.18` فيستخدم شهادة Android Debug مختلفة، لذلك لا يصلح كتحديث للعملاء.

قرار الإصدار: لا يجوز إنشاء keystore جديد أو نشر Debug. يجب استرجاع keystore الإنتاجي من النسخة الاحتياطية أو مالك بيئة التوقيع، وإلا يلزم تصميم انتقال منفصل لا يدّعي قابلية الترقية فوق `1.0.17`.

## إنشاء نسخة العملاء الجديدة بتوقيع دائم

بناءً على قرار إعادة تثبيت النسخة لدى الزبائن، تم إنشاء keystore إنتاجي جديد وتخزينه خارج المستودع في مسار محلي محمي بصلاحية المالك فقط. تم استخدام صيغة PKCS12 متوافقة مع Android Gradle Plugin، مع كلمة مرور موحدة للمتجر والمفتاح محفوظة في ملف أسرار محلي خارج Git.

تم بناء `officialRelease 1.0.18` بنجاح باستخدام هذا التوقيع الجديد. الحزمة جاهزة للتوزيع اليدوي بعد اختبار الجهاز، لكنها **ليست ترقية فوق 1.0.17** لأن شهادة الإصدار الجديدة تختلف عن شهادة الإنتاج القديمة؛ يجب إعادة تثبيت النسخة الجديدة كما قرر المالك.

| خاصية الحزمة | القيمة |
|---|---|
| packageName | `com.dzhoof.iptv` |
| versionName | `1.0.18` |
| versionCode | `10018` |
| APK Signature Scheme v2 | ناجح |
| بصمة شهادة التوقيع الجديدة | `8ab2ddd7e019b32c1df1ba82401cdd74230bbb3d102f8372bf565051a2eb30d4` |
| SHA-256 للـ APK | `9760c216c36ec38f1801208c9d790803b14924b074f26711f461c336bb003577` |
| مكان keystore | خارج المشروع، غير متتبع في Git |
| النشر على VPS وAppVersion | لم يتم بعد |

يجب قبل التوزيع حفظ نسختين احتياطيتين مشفرتين من keystore وملف الأسرار في أماكن منفصلة وآمنة. فقدان هذا المفتاح مستقبلاً سيمنع تحديث هذه النسخة دون إعادة تثبيت جديدة.

## دفعة إعادة التصميم: Client 2.0 Live TV Hub

تم بدء إعادة بناء واجهة Client 2.0 بصورة مستقلة داخل DZ HOOF، اعتماداً على دراسة المشاريع المفتوحة المصدر من دون إدخال كود GPL أو تغيير backend أو حسابات المستخدمين. تشمل الدفعة الأولى:

- واجهة Live TV جديدة للتلفاز بثلاث مناطق واضحة: مجموعات القنوات، قائمة قنوات مع EPG الآن، ولوحة Spotlight تعرض القناة المحددة مع الشعار والبرنامج الحالي والتالي وإجراء المشاهدة والمفضلة.
- بقاء الهاتف وحالات التحميل/الخطأ على المسار المتجاوب المثبت، مع عدم تغيير callbacks للتشغيل أو المفضلة أو MultiView.
- تحديث رأس الشاشات المشترك ليصبح Surface مرتفعاً بإطار brand خفيف بدلاً من شريط العنوان القديم، ما يحسن مظهر الشاشات التي تستعمل `ScreenScaffold` دون تغيير التنقل.
- تحصين توليد QR الاختياري في `ChannelsViewModel`: أي فشل في encoder أو Bitmap لا يعود قادراً على التأثير في تدفق القنوات أو اختبار دورة الحياة.
- إزالة MockK constructor غير المستقر من اختبار `ChannelsViewModelTest` بعد أن أصبح مسار QR يعزل فشله بصورة صحيحة.

نجحت بوابة الجودة بعد هذه الدفعة: `assembleOfficialDebug` و`testOfficialDebugUnitTest` و`lintOfficialDebug`.

يبقى اختبار D-pad وقياسات الـ 10-foot layout على Android TV فعلي أو AVD ضرورياً قبل اعتبار التصميم النهائي جاهزاً للزبائن.

## Client 2.0: توحيد Live TV والكتالوج وGuide

تمت متابعة دفعة Client 2.0 بعد Live TV Hub بإعادة تصميم لوحة اكتشاف الكتالوج. أصبحت شاشة الأفلام والمسلسلات تستخدم سطحاً موحداً مرتفعاً يحتوي عنوان اكتشاف واضحاً، اختياراً مرئياً للأفلام/المسلسلات، وبحثاً متجاوباً للهاتف والتلفاز مع حالات focus موحّدة. كما تم توحيد رمز اللون في فلاتر Guide مع التصميم الرسمي، واستفاد Guide وبقية شاشات `ScreenScaffold` من رأس Surface الجديد.

هذه التغييرات presentation-only ولا تعدّل مصدر القنوات، EPG، الحسابات، Node.js API، أو بيانات Room. بعد الدمج اجتازت بوابة الجودة: `assembleOfficialDebug` و`testOfficialDebugUnitTest` و`lintOfficialDebug`.

## Client 2.0: توحيد تجربة المشغل

تم توحيد شريط الإجراءات السريعة في مشغل Android TV مع لغة Client 2.0 باستخدام اللون الرسمي `DzGreen300` بدلاً من رمز التصميم القديم. تظل اختصارات D-pad وفتح الدليل وقائمة القنوات واختيار الصوت/الترجمة والمؤقت والمفضلة كما هي، بينما أصبحت إشارة التركيز وحالة المؤقت متسقة مع Live TV والكتالوج وGuide.

نجحت بوابة الجودة الكاملة بعد التغيير: `assembleOfficialDebug` و`testOfficialDebugUnitTest` و`lintOfficialDebug`.

## Client 2.0: بطاقة تفاصيل دليل البرامج

أصبحت تفاصيل البرنامج المحدد في رأس Guide تُعرض ضمن بطاقة معلومات ثابتة ذات إطار خفيف وحالات واضحة عند غياب الجدول أو قبل تحديد برنامج. يعزز ذلك وضوح العنوان والوقت والقناة من مسافة التلفاز ويمنع تذبذب العرض عند التنقل بالـ D-pad، من دون تغيير شبكة EPG أو catch-up أو تدفق بيانات القنوات.

تم التحقق من هذه الدفعة بنجاح عبر `assembleOfficialDebug` و`testOfficialDebugUnitTest` و`lintOfficialDebug`.

## Client 2.0: إعادة بناء الصفحة الرئيسية للهاتف

أظهرت مراجعة لقطة هاتف فعلية أن الهاتف كان ما زال يعرض بنية قديمة: لوحة أوامر كبيرة قابلة للقص، عنوان قناة/مجموعة غير منقح مثل `## ENTERTAI...`، بطاقات أفقية موروثة، وزر بحث عائم يحجب جزءاً من المحتوى فوق شريط تنقل عائم. هذه ليست مشكلة لون أو خط فقط، بل مشكلة بنية مخصصة للهاتف.

لذلك أصبحت الشاشة الرئيسية في العرض المدمج للهاتف تستعمل مكوّن `Client2MobileHome` مستقلاً عن تخطيط التلفاز. تعرض الواجهة الآن قناة مباشرة واحدة كبطاقة رئيسية بمعلومات القناة الفعلية والبرنامج الجاري وزر مشاهدة واضح، ثم ثلاثة اختصارات صغيرة للقنوات والدليل والمفضلة، وفئات قابلة للتصفح، وقوائم قنوات عمودية بشعار القناة واسمها والبرنامج الجاري. تم تنظيف أي بادئة Markdown غير مقصودة من أسماء القنوات أو البرامج قبل العرض، ولذلك لا يظهر العنوان بصيغة `##` أو بمسافة فارغة. لا تتغير معرفات القنوات أو إجراءات التشغيل أو المفضلة أو واجهات Backend.

كما أزيل زر البحث العائم المتكرر من الهاتف؛ فالبحث مقصد ثابت داخل شريط التنقل وفي رأس الصفحة. واستبدل الشريط السفلي العائم القديم بسكة تنقل Client 2.0 كاملة العرض ذات حافة علوية موحدة ومؤشر نشط مدمج، فلا تحجب البطاقات أو تعطي انطباع واجهة قديمة.

| التحقق | النتيجة |
|---|---|
| `:app:compileOfficialDebugKotlin` بعد صفحة الهاتف | ناجح |
| `:app:assembleOfficialDebug` | ناجح |
| `:app:testOfficialDebugUnitTest` | ناجح |
| `:app:lintOfficialDebug` | ناجح |
| Node.js API، الحسابات، بيانات Room | لم تتغير |
| VPS، APK العملاء، وEndpoint التحديث | لم تتغير |

تحتاج هذه الدفعة إلى معاينة بصرية على هاتف فعلي قبل بناء APK العملاء التالي، لكن المصدر لم يعد يستدعي بطاقة البطل القديمة أو لوحة الأوامر أو الصفوف الأفقية في العرض المدمج للهاتف.

**المؤلف:** Manus AI

## سجل المراجع

لا توجد مصادر خارجية لهذه الدفعة؛ التوثيق مبني على مراجعة لقطة التطبيق المرفقة وسجلات التجميع والاختبارات المحلية.

## APK مراجعة الهاتف: Client 2.0 الإصدار 1.0.19

لمنع تكرار تثبيت APK قديم أو التباسه مع حزمة مراجعة Client 2.0 السابقة، رُفع الإصدار الافتراضي إلى `1.0.19`، والذي ينتج `versionCode 10019` تلقائياً. تم بناء APK `officialRelease` جديد بعد نجاح بوابة جودة الهاتف الكاملة، والتحقق من الحزمة والتوقيع والمحاذاة قبل حفظه خارج المستودع كمادة مراجعة فقط.

| خاصية الحزمة | القيمة |
|---|---|
| packageName | `com.dzhoof.iptv` |
| versionName / versionCode | `1.0.19` / `10019` |
| APK Signature Scheme v2 | ناجح |
| بصمة شهادة التوقيع | `8ab2ddd7e019b32c1df1ba82401cdd74230bbb3d102f8372bf565051a2eb30d4` |
| SHA-256 للـ APK | `fdfea8732a460e9767de154a915bec5add6e7ae361fd96025c6896b33a84e0ba` |
| حزمة المراجعة | خارج Git وفي مسار مراجعة منفصل |
| VPS وEndpoint التحديث | لم تتغير |

يمكن تثبيت هذه الحزمة فوق مراجعة `1.0.18` ذات الشهادة الجديدة لأن رقم البناء أعلى والشهادة متطابقة. أما النسخة التاريخية `1.0.17` ذات الشهادة القديمة، فيجب حذفها قبل التثبيت كما هو موثق سابقاً. هذه حزمة معاينة للهاتف وليست نشراً عاماً أو تفعيل تحديث تلقائي.

## Client 2.0: إعادة بناء تصنيفات الهاتف

كشفت لقطة المراجعة أن شبكة التصنيفات الموروثة في الهاتف كانت تضغط اسم الفئة والعداد والأيقونة وصورة الخلفية ضمن مساحة صغيرة؛ لذلك ظهرت عناصر فوق بعضها وتكرر معنى عدد القنوات بصورة مربكة. لم يعد الهاتف يستخدم هذه الشبكة أو عنوان `ScreenScaffold` الدائري في شاشة التصنيفات.

أصبحت التصنيفات في العرض المدمج دليل قنوات مستقل: عنوان وصفي بسيط، ملخص حقيقي لعدد الفئات والقنوات، حقل بحث فوري، ثم بطاقات عمودية كاملة العرض. تفصل كل بطاقة بين أيقونة النوع، اسم التصنيف، عدد القنوات بصيغة عربية صحيحة، وزر المفضلة. يبقى الضغط على البطاقة لفتح فئتها كما كان، ويظل زر القلب مستقلاً للمفضلة؛ كما يتعامل البحث مع أسماء العرض العربية والاسم الخام القادم من المزود، ويظهر رسالة واضحة عند عدم وجود نتائج.

التصميم الخاص بالتلفاز لم يُستبدل: شبكة التلفاز وتفاعل D-pad بقيت في مسارها الخاص، بينما الهاتف فقط انتقل إلى البنية الجديدة. لا تغير هذه الدفعة API أو الحسابات أو المصدر أو Room.

| التحقق | النتيجة |
|---|---|
| `:app:compileOfficialDebugKotlin` | ناجح |
| `:app:assembleOfficialDebug` | ناجح |
| `:app:testOfficialDebugUnitTest` | ناجح |
| `:app:lintOfficialDebug` | ناجح |
| packageName | `com.dzhoof.iptv` |
| إصدار APK الموقّع للمراجعة | `1.0.20` / `10020` |
| APK Signature Scheme v2 | ناجح |
| SHA-256 للـ APK | `96d9311956ecf552c2cab613d5e2f67b367beb230e658eff0ee153a9f406e854` |
| VPS وEndpoint التحديث | لم يتغيرا |

هذه حزمة معاينة منفصلة، ويمكن تثبيتها فوق مراجعة `1.0.19` الجديدة ذات الشهادة نفسها. لا تمثل تحديثاً مباشراً من `1.0.17` ذات الشهادة التاريخية المختلفة.

## Client 2.0: تنظيم القنوات حسب البلدان والعلامات

أضيفت طبقة تنظيم محلية باسم `ChannelCollectionOrganizer` فوق بيانات القنوات المخزنة، بدلاً من تعديل روابط البث أو إعادة كتابة مجموعات المزود في الخادم. هذا القرار يحافظ على `Room` وعلى التوافق مع قوائم M3U وXtream التي غالباً لا ترسل بلد القناة بصورة مستقلة، ويستفيد عند توفر البلد من الحقل المرسل من المصدر.

تُشتق المجموعة من اسم القناة واسم مجموعة المزود والبلد المتاح، ثم تعرض القنوات ضمن رفوف افتراضية منظمة. تشمل الأولويات `beIN SPORTS` كمجموعة مستقلة، ثم الرياضة والأخبار والأفلام والمسلسلات والأطفال والوثائقيات والموسيقى/الراديو، ثم مجموعات البلدان المدعومة مثل الجزائر والمغرب وتونس ومصر والسعودية والإمارات وقطر والكويت ولبنان والأردن وتركيا وفرنسا وبريطانيا والولايات المتحدة وألمانيا وإيطاليا وإسبانيا والبرتغال والبرازيل والهند وباكستان. لا تُحذف القناة من مجموعة أخرى؛ يمكن أن تظهر في مجموعة العلامة ومجموعة بلدها عند انطباق القاعدتين، وهي طريقة مقصودة لسهولة العثور عليها.

تستطيع شاشة التصنيفات والرف في الرئيسية ولوحة Live TV للتلفاز فتح هذه المجموعات كقوائم حقيقية مصفاة من الكتالوج المحلي. ويدعم نظام مفضلة التصنيفات تثبيت مجموعة افتراضية مثل `beIN SPORTS` أو بلد، ثم يحسب عناصرها من القنوات المخزنة حتى لا تصبح المفضلة فارغة. تبقى روابط التصنيفات الخام القديمة قابلة للفتح عند الوصول إليها عبر رابط قديم أو عنصر سابق.

| التحقق | النتيجة |
|---|---|
| اختبارات `ChannelCollectionOrganizerTest` | ناجحة؛ تشمل beIN SPORTS، البلدان، وحدود رموز البلدان القصيرة |
| اختبارات `ChannelsViewModelTest` ذات الصلة | ناجحة |
| `:app:assembleOfficialDebug` | ناجح |
| `:app:testOfficialDebugUnitTest` الكامل | ناجح |
| `:app:lintOfficialDebug` | ناجح |
| packageName | `com.dzhoof.iptv` |
| إصدار APK الموقّع للمراجعة | `1.0.21` / `10021` |
| APK Signature Scheme v2 | ناجح |
| SHA-256 للـ APK | `1f9dec7d7a53aa29bc7e3b56415bb3d9c7ffb3b9a8c4be9d3f28eae1a43d1036` |
| Backend وRoom وروابط البث | لم تتغير |
| VPS وEndpoint التحديث | لم يتغيرا |

حزمة `1.0.21` مراجعة فقط ويمكن تثبيتها فوق مراجعة `1.0.20` ذات الشهادة الجديدة. لا يمكن تثبيتها مباشرة فوق النسخة التاريخية `1.0.17` ذات شهادة مختلفة من دون حذفها أولاً.

## Client 2.0: حماية الإقلاع بعد تقرير التوقف

بعد تقرير أن مراجعة `1.0.21` تتوقف بعد الشعار، أوقِف أي نشر وتحققت من مسار الإقلاع ومن التغيير الذي أضاف فهرس البلدان والعلامات. لم يتوفر سجل Android من الجهاز، ولذلك لا يُنسب سبب محدد إلى استثناء بعينه. لكن المراجعة كشفت مخاطرة أداء واقعية: النسخة الأولى من فهرس التنظيم كانت تعيد تطبيع النصوص ومطابقة كل قناة لكل مجموعة عبر تعبيرات منتظمة متكررة في أثناء رسم واجهة الهاتف.

حُوّل الفهرس إلى تنفيذ أكثر أماناً: يُطبع نص كل قناة مرة واحدة فقط، ثم تُحسب كل المجاميع في مرور واحد باستخدام عدادات ثابتة ومن دون إنشاء `Regex` لكل قناة/مجموعة. كذلك أصبحت واجهات الهاتف تستهلك `browseCollections` المحسوبة في `ChannelsViewModel`، بدلاً من إعادة تشغيل المصنف داخل `remember` أثناء تكوين الشاشة. أي فشل غير متوقع أثناء تجهيز رفوف التصفح يُعزل ويترك القنوات الأساسية ظاهرة بدلاً من منع الصفحة الرئيسية من العمل.

أضيف اختبار يحاكي 4,500 قناة ويثبت عدادات beIN SPORTS وقطر وفرنسا والجزائر، إضافة إلى اختبارات قواعد التنظيم وViewModel. اجتازت بوابة الجودة الكاملة بعد الإصلاح.

| التحقق | النتيجة |
|---|---|
| اختبار فهرس 4,500 قناة | ناجح |
| اختبارات `ChannelCollectionOrganizerTest` | ناجحة |
| اختبارات `ChannelsViewModelTest` ذات الصلة | ناجحة |
| `:app:assembleOfficialDebug` | ناجح |
| `:app:testOfficialDebugUnitTest` الكامل | ناجح |
| `:app:lintOfficialDebug` | ناجح |
| packageName | `com.dzhoof.iptv` |
| إصدار APK الموقّع للمراجعة | `1.0.22` / `10022` |
| APK Signature Scheme v2 | ناجح |
| SHA-256 للـ APK | `7e33209f2b9ca1136799ad3d9a0eeb8cea024b8f497e567fe377e9330b0f10be` |
| VPS وEndpoint التحديث | لم يتغيرا |

إصدار `1.0.22` حزمة تصحيح ومراجعة فقط. يلزم اختبار فتحه على الجهاز المبلّغ عن المشكلة؛ في حال تكرر التوقف، يجب إرسال سجل الخطأ أو لقطة شاشة من شاشة الخطأ حتى يربط الإصلاح بسجل الجهاز الفعلي بدلاً من التخمين.

## Client 2.0: إعادة تشكيل الرئيسية وعزل الإقلاع

استناداً إلى لقطة المراجعة، أُعيد بناء تسلسل الصفحة الرئيسية للهاتف لتكون واجهة بث موجزة بدلاً من قائمة بطاقات مكررة. أصبح الرأس شارة هوية `DZ HOOF` مع ملخص البث وعدد القنوات وزر بحث واحد واضح. واختيرت القناة البارزة من قنوات صالحة تحمل شعاراً أو برنامجاً حالياً بقدر الإمكان، بينما تُزال التكرارات من القوائم بالاعتماد على هوية القناة أو `tvgId` أو الاسم المنظف. صارت الاختصارات أقل ارتفاعاً، واستُبدلت شرائح التصنيفات الأفقية المقصوصة بأربع بطاقات تصنيفات متجاورة تعرض الاسم والأيقونة والعدد دون قطع.

وبسبب استمرار تقرير التوقف، خفّف مسار الإقلاع أكثر: يحمّل التطبيق في الإطار الأول كتالوج القنوات والتحديث فقط. ثم بعد 1.2 ثانية يبدأ العمل الثانوي غير الضروري للعرض الأول، وهو تحميل EPG وإعداد اقتراحات المشاهدة ومراقبة مفضلة التصنيفات وإنشاء رمز QR. فشل EPG أو أي عمل ثانوي لا يمنع ظهور القنوات، وكل عملية مؤجلة معزولة ضمن مسارها. لا يعالج ذلك كل خلل خاص بالجهاز من دون سجل crash فعلي، لكنه يزيل منافسة عمليات غير أساسية مع الانتقال من الشعار إلى الرئيسية.

| التحقق | النتيجة |
|---|---|
| `ChannelsViewModelTest` بعد التأجيل | ناجح |
| بناء Kotlin لشاشة الرئيسية | ناجح |
| `:app:assembleOfficialDebug` | ناجح |
| `:app:testOfficialDebugUnitTest` الكامل | ناجح |
| `:app:lintOfficialDebug` | ناجح |
| packageName | `com.dzhoof.iptv` |
| إصدار APK الموقّع للمراجعة | `1.0.23` / `10023` |
| APK Signature Scheme v2 | ناجح |
| SHA-256 للـ APK | `ed940aa695e6ef9de3ee709c750ea0830507e99cab496c5d472722955a76051f` |
| VPS وEndpoint التحديث | لم يتغيرا |

تظل هذه حزمة مراجعة. يلزم تثبيتها على الجهاز الذي أبلغ عن التوقف. عند تكرار الخلل بعد `1.0.23`، تُجمع رسالة Android أو سجل crash لنربط العلاج بالسبب الفعلي ولا نستمر في التخمين.

## Client 2.0: هوية DZ HOOF البصرية المميزة

بناءً على طلب تجاوز طابع التصاميم القالبية، عُدلت العناصر المحورية للهاتف لتستعمل لغة بث مخصصة لـ DZ HOOF بدلاً من أسطح دائرية وتدرجات متشابهة. الاتجاه الجديد يعتمد لوحات ذات زوايا مقصوصة مقصودة، وعلامة إشارة جانبية ملونة، وأزرار تحكم صغيرة مستقيمة نسبياً، ومؤشر تنقل خطي. لا يتطلب الاتجاه صوراً مولدة أو شعارات نصية مولدة، وتبقى هوية القناة الفعلية عبر شعارها الآتي من بياناتها.

| العنصر | قرار الهوية الجديد |
|---|---|
| رأس الرئيسية | لوحة بث مكتوبة `DZ HOOF / LIVE` مع رقم القنوات وزر بحث تحكم صغير |
| القناة البارزة | لوح بث بحد فئوي وخط إشارة جانبي بدلاً من بطاقة تدرج عامة |
| اختصارات الرئيسية | أزرار تحكم بزوايا قصيرة وحدود ذات دلالة بدلاً من مربعات كبيرة متطابقة |
| أقسام التصفح | ألواح متجاورة بزوايا مقصوصة تعرض الاسم والعدد والأيقونة بوضوح |
| بطاقات القنوات | صفوف بث بعلامة فئة جانبية وشعار وقيمة فعلية للمحتوى |
| التنقل السفلي | شريط تحكم قليل الارتفاع بمؤشر خطي للوجهة النشطة بدلاً من حاوية عائمة شديدة الاستدارة |

اجتازت هذه الدفعة `:app:assembleOfficialDebug` و`:app:testOfficialDebugUnitTest` و`:app:lintOfficialDebug`. حزمة المراجعة الموقعة هي `1.0.24` / `10024`، package `com.dzhoof.iptv`، وتحقق APK Signature Scheme v2 بنجاح. SHA-256: `07e1957044d5c94625d7e4180311486668003232b10e53fedfce32545e4b2bc2`.

لا تزال حزمة مراجعة فقط؛ لم يُنشر شيء إلى VPS ولم يتغير Endpoint التحديث. يلزم التحقق على الهاتف من عدم تكرار التوقف؛ عدم وجود سجل crash من الجهاز يمنع تأكيد السبب النهائي لأي توقف باقٍ.

## Client 2.0: متصفح البث للهاتف وحلقة استرداد الإقلاع

أضيف متصفح قنوات مستقل للهاتف بدلاً من شبكة الملصقات القديمة. يعرض المتصفح رأس `DZ HOOF / CHANNELS`، وعدد القنوات في المجموعة المحددة، وفلاتر مجموعات البلدان والعلامات، ثم قائمة صفوف بث قابلة للمس. كل صف يحوي علامة فئة جانبية، شعار القناة أو رمزاً احتياطياً، اسم القناة، البرنامج الحالي أو نوع البث، وإجراءً واضحاً للمفضلة أو المشاهدة. لا تتغير روابط البث ولا data model ولا تنظيم Android TV.

ووُحّد البحث والدليل مع لغة الإشارة والزوايا المقصودة نفسها. شاشة البحث صارت تستخدم رأس DZ HOOF خاصاً بدلاً من `ScreenScaffold` العام على الهاتف؛ وشارات حالة الدليل تستخدم لوحات إشارات متناسقة مع غيرها من شاشات البث.

أضيفت حلقة استرداد إقلاع محلية لا تحفظ حسابات أو روابط أو محتوى. يوضع مؤشر عند بدء العملية، ولا يمحى إلا بعد بقاء الواجهة نشطة بعد الشعار. إذا أغلق التشغيل السابق قبل الاستقرار، يدخل التشغيل التالي وضع استرداد مؤقت: لا تبدأ مزامنة WorkManager الدورية ولا أعمال اقتراحات/EPG/QR الثانوية تلقائياً، بينما يبقى تحميل الكتالوج الأساسي ومسارات الحساب متاحة. هذه آلية حماية وتشخيص وليست إعلاناً بأن سبب التوقف النهائي صار مؤكداً.

| التحقق | النتيجة |
|---|---|
| بناء Kotlin للقنوات والبحث والدليل والاسترداد | ناجح |
| `:app:assembleOfficialDebug` | ناجح |
| `:app:testOfficialDebugUnitTest` الكامل | ناجح |
| `:app:lintOfficialDebug` | ناجح |
| packageName | `com.dzhoof.iptv` |
| إصدار APK الموقّع للمراجعة | `1.0.25` / `10025` |
| APK Signature Scheme v2 | ناجح |
| SHA-256 للـ APK | `a8ca95acf9f695d6d438c387aeba1719ed4379998b449ba80251140897f669ba` |
| VPS وEndpoint التحديث | لم يتغيرا |

يبقى اختبار جهاز حقيقي مطلوباً لمعرفة ما إن كان التوقف باقياً، لأن سجل crash الخاص بالجهاز لم يُرسل بعد. لا تُنشر هذه الحزمة للزبائن ولا تفعل التحديث التلقائي.

## Client 2.0: مسار التشغيل الآمن بعد استمرار التوقف

بعد استمرار تقرير التوقف والإغلاق، أُضيف مسار تشغيل آمن حقيقي لا يحمّل التنقل أو قائمة القنوات أو صور الشعارات أو EPG أو Workers عند اكتشاف أن التشغيل السابق لم يصل إلى الحالة المستقرة. يضع التطبيق علامة بداية عند إنشاء العملية، ثم يمحوها فقط بعد ثماني ثوانٍ من انتهاء شاشة الشعار وبقاء shell نشطاً. إذا توقفت العملية قبل ذلك، يبدأ الفتح التالي في شاشة استرداد خفيفة تعرض أن الوضع الآمن فعّال وزر إعادة محاولة مقصودة.

قبل تمرير أي عطل غير معالج إلى Android، يسجّل التطبيق محلياً فقط اسم فئة الاستثناء وتاريخاً زمنياً. لا تُخزّن رسالة الاستثناء ولا رابط بث ولا رمز جهاز ولا token ولا بيانات حساب. شاشة الاسترداد تعرض رمز الفئة عند توفره، ما يسمح بتحديد سبب فعلي عند إرسال لقطة لها. وضع الاسترداد يوقف جدولة sync الخلفية، وتتجاوز `ChannelsViewModel` الأعمال الثانوية مثل EPG والاقتراحات وQR؛ ولا يغير المصادر أو الحساب أو قائمة القنوات المخزنة.

| التحقق | النتيجة |
|---|---|
| بناء Kotlin لمسار الاسترداد | ناجح |
| `:app:assembleOfficialDebug` | ناجح |
| `:app:testOfficialDebugUnitTest` الكامل | ناجح |
| `:app:lintOfficialDebug` | ناجح |
| packageName | `com.dzhoof.iptv` |
| إصدار APK الموقّع للمراجعة | `1.0.26` / `10026` |
| APK Signature Scheme v2 | ناجح |
| SHA-256 للـ APK | `2a91b020b0436b61ca79e34ffae669dbe7cdf8138e50b538ef4080a730303045` |
| VPS وEndpoint التحديث | لم يتغيرا |

هذه آلية استرداد لا تثبت تلقائياً أن كل عطل محتمل عولج. عند فتح `1.0.26` بعد إغلاق مبكر، يجب تصوير شاشة `DZ HOOF / RECOVERY` وإرسال رمز التشخيص الظاهر عليها. لا تُنشر الحزمة للعملاء إلى أن ينجح فتح عادي على جهاز فعلي.
