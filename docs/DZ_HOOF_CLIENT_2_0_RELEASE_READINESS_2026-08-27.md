# DZ HOOF Client 2.0 — جاهزية Release

**التاريخ:** 2026-08-27  
**الفرع:** `manus/observability-foundation`  
**الحالة:** غير مفعّل للإنتاج

## الفحص

تم تشغيل مهمة `:app:assembleOfficialRelease` مع رابط Backend الإنتاجي عبر HTTPS. نجحت مراحل التجميع والتحويل حتى خطوة تغليف APK، ثم أوقفها Android Gradle Plugin لأن SigningConfig باسم `release` لا يملك `storeFile` في بيئة الجلسة الحالية.

هذا توقف آمن ومقصود؛ فلا يجوز استخدام APK debug أو إنشاء مفتاح جديد عشوائياً، لأن أجهزة المستخدمين الموجودة يجب أن تستقبل تحديثاً موقّعاً بالشهادة نفسها المستخدمة في النسخ المنشورة سابقاً.

## الحالة الحالية

| العنصر | النتيجة |
|---|---|
| مصدر الإصدار الافتراضي | `1.0.18` / `10018` |
| flavor المطلوب | `official` |
| Backend URL | HTTPS؛ لم يُغيّر |
| Release compilation | وصل إلى التغليف قبل فشل التوقيع |
| سبب الفشل | متغير `SIGNING_KEY_STORE` وملف keystore غير متاحين في الجلسة |
| APK debug للمراجعة | موجود داخلياً فقط، غير صالح كتحديث إنتاجي |
| VPS / `/downloads` / `AppVersion` | لم تُمس |

## تحصين Workflow الإصدار

تم تعديل Workflow الإصدار ليبني `:app:assembleOfficialRelease` فقط، بدلاً من مهمة عامة قد تنتج flavors إضافية. كما أصبح يتحقق من أن versionName يبدأ بصيغة major.minor.patch وأن versionCode المحسوب أكبر من الإصدار المنشور `10017`. تحقق محلياً من أمثلة `1.0.17` و`1.0.18` و`1.0.18-rc.1` دون كشف أي أسرار.

## شرط الإصدار التالي

لتوليد Release صالح، يجب تشغيل البناء في بيئة CI أو جلسة تحتوي ملف keystore الإنتاجي والمتغيرات الأربعة المرتبطة به: مسار الملف، كلمة مرور المتجر، alias، وكلمة مرور المفتاح. يجب عدم تسجيل القيم في السجل أو إرسالها في المحادثة.

بعد توفر البيئة الصحيحة، يلزم التحقق من `applicationId` و`versionName` و`versionCode` وشهادة التوقيع وSHA-256، ثم اختبار تثبيت التحديث فوق نسخة أقدم على جهاز فعلي قبل تسجيل الإصدار في Endpoint التحديث.

## فحص Endpoint الحالي

تمت قراءة Endpoint التحديث دون أي كتابة أو تفعيل. النتيجة الحالية تعلن `1.0.17` برقم `10017`: الجهاز الذي يعلن `10016` يرى تحديثاً، والجهاز الذي يعلن `10017` لا يرى تحديثاً، كما أن APK المراجعة الداخلية `10018` لا يرى تحديثاً من الإنتاج. هذا يؤكد أن النسخة الداخلية لم تُسجل في النظام العام.

## تحسينات جلسة المتابعة

أضيفت بطاقات نتائج القنوات في البحث إلى تجربة أكثر وضوحاً: تعرض البطاقة شعار القناة عند توفره، واسم المجموعة، وتستخدم اختصاراً للنص مع `Ellipsis` حتى لا يكسر الاسم الطويل تخطيط الهاتف أو التلفاز. لم يتغير معرّف القناة أو callback التنقل، ولذلك بقيت المفضلة وفتح المشغل متوافقين مع السلوك السابق.

كما أضيفت معالجة تركيز موحّدة لبطاقات الأفلام والحلقات في الكتالوج. عند وصول التركيز عبر D-pad تحصل البطاقة على تكبير وظل بصري من مكوّن التركيز الموحد `tvFocusVisuals`، بينما يظل سلوك اللمس كما هو. هذا يعالج قابلية اكتشاف العنصر المحدد من مسافة المشاهدة دون إدخال مكتبة خارجية أو تغيير نموذج البيانات.

| التحقق | النتيجة |
|---|---|
| `:app:compileOfficialDebugKotlin` بعد تحسين البحث | ناجح |
| `:app:testOfficialDebugUnitTest` بعد تحسين البحث | ناجح |
| `:app:compileOfficialDebugKotlin` بعد تحسين التركيز | ناجح بعد إصلاح استيراد Compose الخاص بـ `setValue` |
| `:app:testOfficialDebugUnitTest` بعد تحسين التركيز | ناجح |
| Git working tree بعد الدفع | نظيف |
| آخر commit | `4f20f47` — `feat(android): improve catalog d-pad focus` |
| commit السابق | `50dabf7` — `feat(android): enrich channel search results` |
| النشر الإنتاجي وVPS | لم يُنفّذ؛ لا يزال محمياً من أي تغيير |

تظل الخطوات التي تحتاج جهازاً فعلياً كما هي: اختبار انتقالات D-pad في Guide والكتالوج، التحقق من HLS على شبكات مختلفة، وتجربة تحديث 1.0.17 إلى 1.0.18 بعد توفير keystore الإنتاجي الصحيح. لا يُعد أي من commits أعلاه إصداراً عاماً أو بديلاً عن توقيع الإنتاج.

**المؤلف:** Manus AI

## سجل المراجع

لا توجد مصادر خارجية مستخدمة في هذه الإضافة؛ النتائج مبنية على ملفات المشروع وسجلات البناء والاختبارات المحلية.

## تحسين Guide وEPG في جلسة المتابعة التالية

تم تحسين جدولة تحميل بيانات EPG أثناء التمرير وتبديل الفلاتر. كانت كل حركة في نطاق العرض تطلق مهمة تحميل مستقلة؛ أما الآن فيُلغى طلب النطاق السابق عند وصول نطاق أحدث، بحيث تظل الأولوية للصفوف التي يمر عليها تركيز D-pad حالياً، وتقل احتمالية نشر بيانات متأخرة فوق الفلتر أو الصف النشط. كما تُلغى مهمة الترطيب السابقة عند إعادة تحميل Guide بالكامل.

هذا التغيير لا يغيّر واجهة الخادم أو نموذج الحسابات، ولا يلغي التخزين المؤقت الموجود أو التحميل الأولي لأول 30 صفاً. وهو تحسين لحالة التنافس داخل ViewModel فقط.

| التحقق | النتيجة |
|---|---|
| `:app:compileOfficialDebugKotlin` بعد تعديل Guide/EPG | ناجح |
| `:app:testOfficialDebugUnitTest` بعد تعديل Guide/EPG | ناجح |
| النشر إلى VPS أو Endpoint التحديث | لم يُنفّذ |
| الإصدار العام | ما زال `1.0.17` |
| آخر تغيير تطبيقي | ينتظر commit والدفع إلى فرع التطوير بعد توثيقه |

يبقى الاختبار على جهاز Android TV فعلي ضرورياً للتأكد من الإحساس البصري للتركيز أثناء flings والتنقل السريع بين الصفوف، لأن اختبارات الوحدة لا تقيس زمن الشبكة أو سلوك جهاز التحكم عن بعد.

## تحسين تشخيص أخطاء المشغل

تم فصل أخطاء اتصال الشبكة عن أخطاء HTTP القادمة من خادم البث. أصبحت الاستجابات `401` و`403` تُعرض كرفض وصول إلى المصدر، و`404` كمصدر غير موجود، بينما تُعامل `408` و`429` وأخطاء `5xx` كحالات مؤقتة قابلة لإعادة المحاولة. أما أخطاء HTTP الدائمة فلا تُصنّف تلقائياً على أنها انقطاع شبكة، ويُنتقل إلى المصدر البديل عند توفره بدلاً من إعادة طلب المصدر نفسه بلا فائدة.

| التحقق | النتيجة |
|---|---|
| `:app:compileOfficialDebugKotlin` بعد تحسين المشغل | ناجح |
| `:app:testOfficialDebugUnitTest` بعد تحسين المشغل | ناجح |
| نشر التغيير إلى VPS أو Endpoint التحديث | لم يُنفّذ |
| آخر تعديل | جاهز للحفظ والدفع إلى فرع التطوير |

تظل رسائل الخطأ باللغة العربية ومتصلة بمسار الاسترداد الحالي، مع الحفاظ على fallback للـ proxy والمصادر البديلة وعدم تغيير حسابات المستخدمين أو واجهات Backend.

## تغطية اختبارية إضافية للمشغل

تمت إضافة اختبارات إلى `ErrorRecoveryManagerTest` للحالات التالية: استجابة `404` الدائمة التي تُظهر رسالة مصدر غير موجود وتنتقل إلى المصدر البديل، واستجابة `503` المؤقتة التي تُظهر خطأ خادم مؤقت وتبدأ إعادة المحاولة. أثناء التحقق كُشف أن أخطاء HTTP الدائمة كانت تُنهى مباشرة رغم وجود مصدر بديل؛ تم إصلاح ذلك بإضافة مسار fallback صريح لها.

| التحقق | النتيجة |
|---|---|
| اختبار `ErrorRecoveryManagerTest` المحدد | ناجح؛ 17 اختباراً |
| تجميع `compileOfficialDebugKotlin` | ناجح |
| `testOfficialDebugUnitTest` الكامل | ناجح |
| حالة GitHub | التغيير مدفوع إلى `manus/observability-foundation` |
| آخر commit | `a9c7da1` — `test(android): cover player http recovery` |
| الإنتاج وVPS | لم يُعدّل أي منهما |

## فحص جودة إضافي

تم تشغيل `:app:lintOfficialDebug` باستخدام Backend HTTPS الرسمي، وانتهى بنجاح. لم تظهر أخطاء Lint جديدة في دفعات Guide وEPG والمشغل الحالية. لم تُجرَ تعديلات كود إضافية في هذه الجولة بعد آخر commit؛ كان الهدف تأكيد سلامة الفرع قبل الانتقال إلى اختبار الأجهزة الفعلية.

| التحقق | النتيجة |
|---|---|
| `:app:lintOfficialDebug` | ناجح |
| حالة الفرع | نظيف ومتزامن مع `origin/manus/observability-foundation` قبل تحديث التقرير |
| الإنتاج وVPS | دون تغيير |
| الخطوة العملية التالية | اختبار Android TV والهاتف، ثم تجهيز `officialRelease` بعد توفير keystore الإنتاجي |

## تحصين رابط التحديث

تم تحصين `AppUpdater` قبل بدء `DownloadManager`: لا يُقبل رابط APK إلا عبر HTTPS ومن نطاق خادم المشروع المكوّن أو نطاقات GitHub الرسمية المستخدمة في fallback. تُرفض الروابط غير الآمنة برسالة عربية واضحة قبل إنشاء أي تنزيل، مع بقاء التحقق من توقيع APK قبل التثبيت كما هو.

هذا التغيير لا يلمس بيانات الحسابات ولا يغيّر Endpoint التحديث، ويمنع إعادة توجيه المستخدم إلى ملف APK من مصدر غير موثوق إذا أعاد خادم خارجي قيمة خاطئة.

| التحقق | النتيجة |
|---|---|
| `:app:compileOfficialDebugKotlin` | ناجح |
| `:app:testOfficialDebugUnitTest` | ناجح |
| Git working tree قبل التوثيق | نظيف |
| الإنتاج وVPS | لم يتم تعديلهما |
| الإصدار العام | ما زال `1.0.17` |

## اختبار سياسة روابط التحديث

فُصلت سياسة قبول رابط APK في `UpdateUrlPolicy` مستقل قابل للاختبار. تغطي الاختبارات قبول رابط HTTPS من خادم المشروع، وقبول نطاقات GitHub الرسمية، ورفض HTTP والنطاقات المجهولة والروابط المضللة أو غير الصالحة. تم كذلك إصلاح اعتماد الاختبار على `Android Uri` باستخدام `java.net.URI` داخل السياسة النقية، وهو ما جعل الاختبار مستقراً في بيئة Android JVM.

| التحقق | النتيجة |
|---|---|
| اختبارات `UpdateUrlPolicyTest` | ناجحة؛ 5 اختبارات |
| `compileOfficialDebugKotlin` | ناجح |
| `testOfficialDebugUnitTest` الكامل | ناجح |
| `lintOfficialDebug` | ناجح |
| آخر حالة الإنتاج وVPS | دون تغيير |
| الإصدار العام | ما زال `1.0.17` |

## حماية هوية APK ومنع التراجع

تم تعزيز مسار تثبيت التحديث بحيث يتحقق من أن APK المنزّل يحمل `packageName` نفسه للتطبيق، وأن `versionCode` المرشح أكبر من النسخة المثبتة حالياً. إذا كان الملف لحزمة مختلفة أو إصداراً قديماً/مساوياً، يُحذف الملف ويُرفض التثبيت قبل فتح مُثبّت Android. يبقى تحقق شهادة التوقيع إلزامياً بعد هذا الفحص.

كما تم التحقق من APK `officialDebug` الداخلي الموجود: يحمل `com.dzhoof.iptv` و`versionCode 10018` و`versionName 1.0.18`، وتحقق توقيع APK v2، لكنه يظل نسخة Debug غير صالحة للنشر العام.

| التحقق | النتيجة |
|---|---|
| `compileOfficialDebugKotlin` | ناجح |
| `testOfficialDebugUnitTest` | ناجح |
| `lintOfficialDebug` | ناجح |
| فحص package/version لـ APK الداخلي | ناجح |
| فحص توقيع APK الداخلي | v2 ناجح؛ Debug فقط |
| `officialRelease` الإنتاجي | ما زال متوقفاً لغياب keystore |
| VPS وEndpoint التحديث | لم يتم تعديلهما |

## تنظيف تحذيرات Release والتحقق من APK

تم استبدال مراجع اللون المهجور `Amber` في طبقات المشغل بالرمز الرسمي `DzGreen300`، مع الحفاظ على القيمة اللونية الفعلية نفسها وإزالة تحذيرين من تجميع Kotlin. لم يتم تغيير سلوك المشغل أو تصميم البطاقات.

أعيد فحص APK الداخلي: الحزمة `com.dzhoof.iptv`، الإصدار `1.0.18`، ورقم البناء `10018`، وتوقيع APK v2 صالح. هذه الحزمة Debug للمراجعة الداخلية فقط ولا يجوز استخدامها كتحديث للمستخدمين.

| التحقق | النتيجة |
|---|---|
| تجميع `officialDebug` | ناجح |
| `testOfficialDebugUnitTest` | ناجح |
| `lintOfficialDebug` | ناجح |
| `officialRelease` | وصل إلى `packageOfficialRelease` ثم توقف بسبب غياب `storeFile` للـ keystore |
| النشر على VPS أو Endpoint التحديث | لم يتم |

## تدقيق مطابقة versionCode في Workflow

تم تقوية خطوة التحقق في Workflow الإصدار الرسمي. بعد بناء APK وتوقيعه، يتحقق المسار الآن من `packageName` و`versionName` و`versionCode` معاً، ويحسب الرقم المتوقع من صيغة `major*10000 + minor*100 + patch`. هذا يمنع رفع ملف يحمل اسم إصدار صحيحاً لكن رقم بناء غير متوافق مع نظام التحديث التلقائي.

تمت محاكاة الصيغة محلياً للقيم `1.0.18 → 10018` و`1.0.18-rc.1 → 10018` و`1.2.3 → 10203`. لم يتم تشغيل Workflow على GitHub أو نشر أي APK، لأن توقيع الإنتاج ما زال يحتاج keystore الصحيح.

## بوابة الجودة النهائية قبل تسليم العملاء

تم تشغيل بوابة الجودة على آخر commit وتشمل بناء `officialDebug`، واختبارات الوحدة الكاملة، وAndroid Lint الرسمي. نجحت جميع المهام. APK الناتج داخلي للمراجعة فقط؛ لا يزال إنشاء `officialRelease` متوقفاً بأمان عند توقيع الإنتاج بسبب غياب ملف keystore ومتغيراته.

| بوابة الجودة | النتيجة |
|---|---|
| `assembleOfficialDebug` | ناجح |
| `testOfficialDebugUnitTest` | ناجح |
| `lintOfficialDebug` | ناجح |
| توقيع Release الإنتاجي | متوقف حتى توفير keystore الصحيح |
| اختبار الترقية على جهاز فعلي | لم يُنفذ بعد |
| النشر إلى VPS أو Endpoint | محظور ولم يُنفذ |

بعد توفير keystore الإنتاجي، ستكون الخطوات المتبقية محددة: تشغيل Workflow الرسمي، التحقق من شهادة التوقيع وv2 وpackage/version وSHA-256، تثبيت النسخة فوق `1.0.17` على جهاز اختبار، ثم فقط اتخاذ قرار نشر APK وتحديث `AppVersion`.

## بوابة حماية وتسليم إضافية

تم تشغيل فحص حارس الأسرار على المستودع، ونجح دون العثور على مفاتيح خاصة أو رموز وصول عالية الثقة ضمن الملفات المتتبعة. ثم أُعيد تشغيل بوابة الجودة الأخيرة على الفرع الحالي، وشملت بناء `officialDebug` واختبارات الوحدة وLint الرسمي، ونجحت جميعها.

| عنصر التسليم | الحالة |
|---|---|
| الأسرار داخل Git | لا توجد مفاتيح خاصة أو رموز وصول عالية الثقة متتبعة |
| `assembleOfficialDebug` | ناجح |
| `testOfficialDebugUnitTest` | ناجح |
| `lintOfficialDebug` | ناجح |
| APK العملاء الرسمي | غير منشأ؛ يحتاج keystore الإنتاجي |
| النشر أو تحديث AppVersion | لم يتم |

أصبح المشروع جاهزاً من ناحية الكود والاختبارات المحلية. لا يمكن إغلاق التسليم للزبائن فعلياً إلا بعد توفير keystore الإنتاجي نفسه، ثم تنفيذ توقيع Release واختبار التثبيت فوق `1.0.17` على جهاز اختبار.
