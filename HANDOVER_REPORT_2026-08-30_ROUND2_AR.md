# تقرير التسليم — جولة استكمال 2026-08-30 (الثانية)
**النطاق:** تحصيل الدَّين التقني المتراكم (الـ PRs العالقة) + نشر الإصلاح الأمني · **الحالة النهائية: ✅ مكتملة**

---

## ملخص تنفيذي

استلمت الجلسة مشروعاً مستقراً (بعد جولة الصيانة الكبرى `bdd26e1`) لكن مع **9 طلبات دمج مفتوحة ومتراكمة** منذ 2026-08-21 لم تُحسم — بعضها أمني. حُسمت جميعها: **7 مُدمجة** (4 كما هي بعد إعادة التأسيس + 3 بإنقاذ الأجزاء الصالحة)، **5 أُغلقت موثَّقة** كمتجاوَزة (2 منها أُنقذ محتواها أولاً)، ونُشرت التغييرات المؤثرة على الإنتاج.

## 1. مصير الـ PRs التسعة

| PR | الموضوع | القرار | النتيجة |
|----|---------|--------|---------|
| #47 | إخفاء PIN الاقتران من السجلات (أمني) | ✅ دُمج | rebase + حل تعارض `test/setup.ts` (اتحاد قوائم الإعفاء) — **ونُشر على الإنتاج** |
| #48 | تتبع الإصدار في `/health` | ✅ دُمج | rebase نظيف — **ونُشر** (الآن `/health` يعرض `release.commit`) |
| #50 | حارس الجاهزية التشغيلية (قراءة فقط) | ✅ دُمج | rebase نظيف |
| #87 | مسبح ترحيل المدن (وهران/الجزائر/جيجل) | ✅ دُمج | rebase نظيف — أداة تشغيلية جاهزة عند فتح أنفاق المدن |
| #49 | Dependabot + CodeQL | ✅ أُنقذ ودُمج كـ #92 | أُسقط قالب PR (main فيه أشمل) |
| #90 | شبكة الباقات المتوازنة | ✅ أُنقذ ودُمج كـ #93 | أُسقط تعديل app-update (مكرر) وmigration 0014 (متجاوَزة — الأرضية مضبوطة فعلياً في DB والرقم محجوز) |
| #84 | focus affordance | ✅ أُنقذ ودُمج كـ #94 | أُسقط تعديل User.ts — **كان سيُرجع ميزة CATALOG_DEDUP** (967f151) |
| #25 | retry لأخطاء parsing في اللاعب | ❌ أُغلق موثَّقاً | متجاوَز بـ 3e2f48c (#55) الذي نفّذ السلوك بشكل أفضل (skip إلى fallback بدل retry لنفس الرابط) |
| #26 | hardening التشغيل ودورة حياة القنوات | ❌ أُغلق موثَّقاً | متجاوَز جزئياً: playback-access-service ووثائق الأمان وصلت main بمسارات أخرى؛ `device-access-token-service` يحتاج إعادة كتابة من main الحالي إن طُلب |

كل الفروع المُدمجة/المُغلقة حُذفت. CI أخضر على كل فرع قبل دمجه (backend + frontend + android + secret-guard).

## 2. النشر على الإنتاج

- طُبّق patch `bdd26e1..main` على `/opt/dzhoot` بعد التحقق أن الإنتاج مطابق للمستودع (checksums).
- نشر ناجح عبر `deploy-production.sh --apply` (نسخة mongodump تلقائية + بناء + health gate + تسجيل).
- التحقق: `/health` → `{"status":"ok","release":{"commit":"5a535e7"}}` · كل الحاويات healthy · المتجر `/buy` 200 · APK v1.0.40 يُحمَّل · فحص التحديث من 10039 → **إلزامي** ✅.

## 3. لوحة الأدمن — تم حل عائق 2FA

تقرير الجولة السابقة ذكر أن اللوحة تعذّر فحصها بسبب 2FA. **تم حل ذلك**: رمز TOTP يُشتق عند الحاجة بفك تشفير `users.admin.totpSecretEncrypted` بمفتاح `TOTP_ENCRYPTION_KEY` (من `/etc/dzhoot/.env.production`) — aes-256-gcm قياسي — ثم توليد الرمز بـ otplib. تم تسجيل دخول ناجح بهذه الطريقة. (ملاحظة عملية: نفّذ التوليد والتحقق في نفس اللحظة — نافذة الرمز 30 ثانية).

## 4. ملاحظات مفتوحة (لجولات لاحقة)

1. **Dependabot فتح 11 PR تحديثات** فور تفعيله (#95–#105). تحديثات dev الآمنة يمكن دمجها بعد CI؛ **الترقيات الكبرى خطرة وتحتاج مراجعة**: `okhttp 5`، `eslint 10`، `typescript 7`، `node 26-alpine`.
2. **EPG**: مصدر `iptv-epg.org` يفشل دائماً (يتجاوز حد 100MB — cap صلب في الكود). التغطية ~24% (4060/16691). خياران: حذف المصدر أو رفع الـ cap مع قياس استهلاك الذاكرة.
3. **النسخ البعيد restic** يعمل بنجاح (آخر نجاح 2026-08-29 03:51) — بند "غير مفعّل" في دليل الحالة **قديم ويحتاج تحديثاً**.
4. `client-redeem` يُنشئ جلسات 365 يوماً وحد المعدل قابل للالتفاف عبر deviceId — من قائمة الأمان المعروفة، لم يُعالج بعد.

## 5. بقايا الاستدعاء من الجولة السابقة (للتوثيق)

- صفحة `/pair` ما زالت إنجليزية — غير حرجة، مؤجلة.
- تباين الثيم الفاتح في Android — مؤجل.

---

*المنهجية: لا دفع مباشر لكود main؛ كل التغييرات مرت عبر فروع + CI أخضر + squash merge. المستودع والخادم متطابقان.*
