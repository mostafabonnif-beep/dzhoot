# تقرير تسليم — DZ HOOF IPTV (2026-08-31: الجولة 13 — البث لا يتوقف أبدًا: تبديل وسطي في البروكسي)

**التاريخ:** 2026-08-31
**الطلب:** أولوية المشغّل القصوى — **زبون DZ HOOF لا يتوقف عنده البث أبدًا**.
**الحالة:** ✅ منجز — `fbe8081` على `main` ومنشور ذريًا (`v1.0.1-20260831T180036Z`). الاختبارات: **470/470**.

---

## 1) الثغرات الثلاث التي كانت توقف البث

| # | الثغرة | الخطورة |
|---|---|---|
| 1 | التبديل الاحتياطي كان يعمل **عند إصدار التوكن فقط** — الزبون الجالس يشاهد، إذا مات المصدر **تتوقف جلسة البث فعليًا** | 🔴 حرجة |
| 2 | 23 قناة NEO بلا أي خريطة احتياطية | 🟠 |
| 3 | كاش صحة المصادر 60 ثانية → اكتشاف متأخر للانهيار | 🟠 |

## 2) الحل — تبديل وسطي داخل بروكسي البث (commit `fbe8081`)

```
الزبون يشاهد قناة (بث TS خام عبر /api/v1/tv/playback/:token)
        │  مات المصدر وسط البث
        ▼
البروكسي: upstream error → getFailoverTarget (أولوية: NEO 4K ← ثم MIBOX)
        ▼
إعادة اتصال بالمصدر الاحتياطي ومواصلة الضخ في نفس استجابة الزبون
        ▼
الزبون يلاحظ لمحة قصيرة فقط — البث لا يتوقف
```

- `playback-token.ts`: توكين v1 يحمل `channelId` + `primarySourceId` (لحل الهدف الاحتياطي وقت الانقطاع).
- `upstream-proxy.ts`: `FailoverContext`؛ تجاوز عند **الفتح** (يغطي إعادة تحميل مانيفست HLS) + حلقة **إعادة اتصال وسطية** (حتى مستويين، `MAX_MID_STREAM_FAILOVERS = 2`)، وعند استنفاد كل المستويات يُغلق الاتصال (خطأ صريح بدل بث ميت).
- `tv.js`: يصك التوكن بمرجع القناة ويمرر `failoverCtx` للبروكسي.
- `source-failover-service.ts`: كاش الصحة **60s → 20s**.
- اختبارات جديدة: استمرار الضخ في نفس الاستجابة بعد موت المصدر / لا تجاوز بدون سياق / تجاوز عند الفتح — **3/3**، والسويت الكامل 470/470.

## 3) تغطية 100%

سكربت ملء الخرائط أضاف 23 خريطة ناقصة (نفس stream id — نفس البانل) →
**كل قنوات NEO (16,684) لها طبقة NEO 4K**، و881 قناة لها طبقة MIBOX ثالثة. تحقق: `NEO4K=16684 MIBOX=881`.

## 4) حادثة أثناء النشر (مُعالجة)

نشر أتمتة موازية (~17:57) أعاد إنشاء الحاويات/الشبكات ففقدت حاوية MongoDB
نقطة اتصالها على `server_dzhoof-network` (IP فارغ + EAI_AGAIN لـ `mongodb`) →
الـ API `degraded` ("mongodb disconnected"). **الإصلاح**: `docker network
disconnect` ثم `connect --alias mongodb` → IP `172.19.0.4` + الاسم المستعار
رجعا؛ API healthy و`/health/ready` ok.
**درس**: بعد أي نشر من الأتمتة، تحقق من `/health/ready` + وجود الاسم المستعار
`mongodb` على الشبكة.

## 5) حالة الإنتاج النهائية (متحقق)

- النشر الفعّال `fbe8081` (staged-ok + وسوم الصورة + سجل النشر).
- الميزة حية في الحاوية (grep: `Mid-stream failover context` / `mid-stream upstream error`).
- `/health/ready`: ok (mongodb/redis connected) · الحاويات كلها healthy.
- الـ relay: redsocks + relay-pool + timer active (فحص متعدد الوجهات).
- المصادر الثلاثة: verified.

## 6) ملاحظات

- النشر المحلي للاختبارات (npm ci + بناء `@dzhoof/shared`) أصبح المرجع الثابت
  لأن حاوية الـ API تُعاد إنشاؤها باستمرار من نشرات الأتمتة (تمسح الملفات المنسوخة).
- المتبقي المقترح: طبقة ثالثة لبقية القنوات (يتطلب مصدرًا مستقلاً ثالثًا)،
  اختيار المصدر يدويًا للزبون (واجهة)، تفعيل Brevo للتنبيهات البريدية.
