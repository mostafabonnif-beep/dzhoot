# تقرير التسليم النهائي — DZ HOOF IPTV (جلسات 2026-08-26)

> **اقرأ هذا أولًا.** الوثيقة الوحيدة لبدء العمل بعد اليوم. تغطي: ما أُنجز اليوم
> (جلستان: التحقق من «القنوات لا تشتغل» + إصلاح «الأفلام والمسلسلات لا تشتغل»)،
> الحالة الحالية المُتحقق منها، ما تبقى، الوصول، الأوامر، والمزالق.
> تفاصيل كل جلسة في: `HANDOVER_REPORT_2026-08-26_VERIFICATION_SESSION_AR.md` +
> `HANDOVER_REPORT_2026-08-26_VOD_FIX_AR.md`.
> ⚠️ **جلسة موازية تعمل على نفس الريبو — ادمج قبل أي push** (القسم 9).

---

## 1. الملخص التنفيذي

| الجبهة | النتيجة |
|---|---|
| **«القنوات لا تشتغل»** | ✅ محلول ومنشور ومُتحقق: 101 فشل `PARSING_CONTAINER_UNSUPPORTED` قبل نشر `v1.0.1-20260826T004609Z` → **0 فشل بعده**. 18 قناة مُختبرة يدويًا (جزائرية/beIN/MBC/فرنسية) كلها تبث. |
| **«الأفلام لا تشتغل»** | ✅ المحتوى الميت (16,922 فيلم ottstreambox) عُطّل — الكتالوج النشط **66,908 فيلم** = عدّاد Upstream بالضبط. بث الأفلام سليم (MKV 206 + Range). |
| **«المسلسلات لا تشتغل»** | ✅ **سبب جذري ثابت**: parser الحلقات قرأ صيغة غير موجودة (`seasons[].episodes`) بينما Xtream يرجع قاموس `episodes` → **0 حلقة منذ بداية المشروع**. + `duration` نصي يقتل الاستيراد. + الجلب الكسول كان admin-only في مسار لا يستخدمه التطبيق. كلها أُصلحت. |
| **الاختبارات** | ✅ **341/341** (6 اختبارات regression جديدة) + tsc + eslint 0 أخطاء |
| **الإنتاج** | ✅ `v1.0.1-20260826T020928Z` — 6 حاويات سليمة، sha256 للملفات المفتاحية مطابق للريبو |

---

## 2. ما أُنجز اليوم (جلستان)

### الجلسة 1 — تحقق + التحديث الإجباري (commit `0a18d87`)
- **شخّصت «القنوات لا تشتغل»**: بق Media3 في التطبيق — الخادم كان يرسل
  `application/vnd.apple.mpegurl` بينما ثابت Media3 هو `application/x-mpegURL` →
  التطبيق يمرر الـ playlist للمشغل التدريجي → `PARSING_CONTAINER_UNSUPPORTED`
  لكل قنوات Upstream (السبب الأصلي، أُصلح في PRs #51–55 بالجلسة الموازية ونُشر 00:50).
- **تحققت حيًا**: 101 فشل قبل النشر → 0 بعده؛ 18 قناة تبث عبر البروكسي.
- **رفعت `minCompatibleVersion` إلى 10012** (AppVersion النشط) → كل تطبيق أقدم
  **يُجبر على التحديث** لـ v1.0.12 (الذي يحوي كل إصلاحات التشغيل). تحققت عبر
  `/api/v1/app/version?currentVersion=10006/10009/10012`.
- **اكتشفت**: المصدر الاحتياطي ottstreambox **مات** (403 على API والبث) والمستخدم
  حذفه بنفسه ليلًا (audit: `XTREAM_SOURCE_DELETE`) + حذف جماعي 2163 قناة ميتة.
  **لا backup حاليًا — Upstream نقطة فشل وحيدة.**

### الجلسة 2 — إصلاح الأفلام والمسلسلات (commit `60a2c58`, نشر `v1.0.1-20260826T020928Z`)
- **المسلسلات — 3 أسباب جذرية**:
  1. `xtream-service.ts`: parser الحلقات قرأ `seasons[].episodes` (صيغة مدمجة نادرة)
     بينما Xtream/يNEO يرجع الحلقات في قاموس `episodes` مفهرس برقم الموسم
     (`{"1":[...]}`) ⇒ **صفر حلقات مستوردة عالميًا**. الإصلاح: union أرقام المواسم
     من المكانين + تفضيل القاموس + دعم الصيغة المدمجة + شكل Upstream (`seasons:[]`
     مع قاموس ممتلئ).
  2. `info.duration` نص حر («45 min», «N/A») → `Number()` = NaN → Mongoose CastError
     **يوقف استيراد المسلسل كاملًا** (شوهد حيًا في سجل الإنتاج). الإصلاح: regex
     للرقم القائد + try/catch لكل حلقة (حلقة فاسدة لا تقتل الباقي).
  3. الجلب الكسول كان `role === 'Admin'` فقط في `/api/v1/series` — بينما التطبيق
     يستخدم **`/api/v1/catalog/*`** (بلا جلب أصلًا). الإصلاح: جلب كسول **للجميع**
     في `catalog.js` (مسارات التطبيق) + `series.js`، محكومًا بختم `episodesFetchedAt`
     (نافذة 24 ساعة؛ **يُختم عند النجاح فقط** — الفشل العابر يُعاد في الفتح التالي).
- **الأفلام — محتوى ميت**: 16,922 فيلم + 3,571 مسلسل من ottstreambox المحذوف
  (روابط 403 نهائيًا) عُطّلت (`isActive:false`). الكتالوج النشط = عدّادات Upstream.
- **التحقق الحي بعد النشر**: مسلسلان فُتحا (بجلسة وبلا auth) → مواسم وحلقات
  استُوردت لحظيًا (8 + 10 حلقات) → authorize → **بث MKV حقيقي 206** بصمة `1a 45 df a3`.
  فيلم → 206 + 512KB.

---

## 3. الحالة الحالية (مُتحققة 2026-08-26 ~02:15 UTC)

- الإنتاج = main `60a2c58`؛ 3 نشرات اليوم: `T004609Z` (إصلاح القنوات)، `T020012Z`
  (جلسة 2 الأولى)، `T020928Z` (إصلاح duration — النهائي).
- 6 حاويات up (api/frontend healthy)؛ القرص ~49%؛ mongo/redis متصلان.
- الكتالوج النشط: **16,638 قناة / 66,908 فيلم / 16,819 مسلسل** — لا يتيم.
- الحلقات تُجلب lazy من Upstream عند أول فتح (API Upstream يعمل من الخادم الآن — الحظر متقطع، لا تفترض).
- التحديث الإجباري مفعّل (minCompatibleVersion=10012) — كل الزبائن سيتحدثون لـ v1.0.12.
- صفر فشل تشغيل قنوات منذ 00:50. اللوحتان (admin/reseller) 200. الشعارات عبر الوسيط.
- 112k برنامج EPG؛ خريطة failover 263 يتيمة لكن خاملة بأمان.

---

## 4. ما تبقى (بعد اليوم)

### 🔴 ينتظر المستخدم (مفاتيح/أفعال — لا يمكن تنفيذها بدونه)
- [ ] **تحديث تطبيقه** لـ v1.0.12 وتجربة القنوات + مسلسل + فيلم.
- [ ] **اشتراك backup جديد** (بديل ottstreambox) → نركبه + auto-match جديد للخرائط
      (الـ263 القديمة مرتبطة بالـ ID المحذوف — تُنظف أو تُعاد كتابتها).
- [ ] تغيير كلمة مرور admin (مكشوفة في المحادثة) + **2FA** + كلمة root عند OVH + تدوير github.token.
- [ ] رابط webhook (Discord/Slack) → `alert_webhook_url` (مهمتا التنبيه تنتظرانه).
- [ ] مفاتيح restic (`RESTIC_REPO` + `RESTIC_PASSWORD` + S3 في `.env.production`) → النسخ الخارجي.
- [ ] حساب تاجر CinetPay + مفاتيح → الدفع الإلكتروني.

### 🟠 تجاري/نمو (من خارطة الطريق)
- [ ] بوابة دفع CinetPay/Paymee (المرحلة 1 — أكبر عائد).
- [ ] مشغّل ويب iPhone (`/watch` بـ HLS.js — المرحلة 2).
- [ ] تعبئة `shop_whatsapp` (رقم المتجر — يُظهر أزرار واتساب في /buy).
- [ ] دمج مكررات القنوات (686 مجموعة — ترحيلة 0012)؛ EPG جزائري محلي؛ تنظيف ~7-10% قنوات ميتة.

### 🟢 تشغيلي
- [ ] كاش شرائح HLS (D1) — البروكسي يستهلك بندويث الخادم.
- [ ] تطبيق Android TV + صفحة تسعير الباقات.
- [ ] خطة عمل اللوحة المجزأة: `WORK_PLAN_ADMIN_PARTS_AR.md`.
- [ ] **مراقبة**: بعد أيام، راجع `playbackevents` — هل ظهرت فشلات VOD؟ هل الجلب الكسول
      يضغط على Upstream API؟ (كل مسلسل = استدعاء واحد حتى أول 24 ساعة من فتحه).

---

## 5. الوصول (ثابت)

- **SSH**: `dzhoof-admin@5.135.79.221` بمفتاح ed25519 —
  `~/.workspace/keys/dzhoof-admin-vps.key` (نسخة من `dzhoof-admin-key` في الريبو). sudo بلا كلمة مرور.
- **GitHub**: `mostafabonnif-beep/dzhoot` — PAT على الخادم `/etc/dzhoot/github.token`.
- **الإنتاج**: `/opt/dzhoot/server` (نسخة ملفات — ليست git checkout) + الأسرار
  `/etc/dzhoot/.env.production`. 6 حاويات: api/frontend/scheduler/caddy/redis/mongodb.
- **لوحة الأدمن**: `https://iptv.ld-11.net/admin` — `admin` / (مكشوفة — غيّرها).
- **قاعدة البيانات**: حاوية `dzhoof-mongodb` — القاعدة **`dzhoof-iptv`** دائمًا.

---

## 6. الأوامر السريعة

```bash
ssh -i ~/.workspace/keys/dzhoof-admin-vps.key dzhoof-admin@5.135.79.221
curl -s https://iptv.ld-11.net/health/ready
curl -s 'https://iptv.ld-11.net/health?details=true' | head -c 400
# النشر: tar (جذر server/) → scp → فك بـ -C /opt/dzhoot → chown → comm -13 لليتامى → deploy --apply
# Mongo: sudo docker exec dzhoof-mongodb mongosh dzhoof-iptv
# أحداث التشغيل: db.playbackevents.find({eventType:'startup_failure'}).sort({createdAt:-1}).limit(10)
# سجل النشر: /var/log/dzhoof-deploys.log
```

---

## 7. المزالق (مهمة)

1. **tar sync لا يحذف اليتيم** — بعد كل فك حزمة: `comm -13` واحذف قبل البناء (كسر tsc مرة).
2. فك الحزمة: جذرها `server/` → `-C /opt/dzhoot` (لا `-C /opt`).
3. متغير env جديد يجب إدراجه في compose `environment:`.
4. **التطبيق يستخدم `/api/v1/catalog/*`** (انظر `android/.../DzhoofApiService.kt`) —
   وليس `/api/v1/series` — تحقق من مسارات التطبيق قبل تعديل مسارات API.
5. `ALLOW_DIRECT_PLAYBACK=false` = إخفاء المزود — لا تعكسه.
6. فحص صحة المصادر المباشرة بالبث الفعلي لا بالـ API (Upstream API حظر متقطع).
7. ottstreambox manifest بروابط نسبية بعد 302 — فحص manifest خفيف (200 + #EXTM3U).
8. `-$quantity` في aggregation = null صامت — استخدم `$subtract`.
9. مسلسل بلا حلقات فعلًا يُختم 24 ساعة — لا تعيد الجلب يدويًا قبل انقضاء النافذة
   (أو امسح `episodesFetchedAt` للتجربة).

---

## 8. ملاحظات أخيرة

- اعتماديات Upstream: يوزر/باسورد داخل روابط البث نفسها (`/movie/U/P/ID.mkv`) —
  مشفرة AES-256-GCM في القاعدة بمفتاح `XTREAM_SECRET_KEY` — لا تُكتب في الريبو.
- نسخ احتياطي تلقائي قبل كل نشر في `/var/backups/dzhoot/mongodb/` (أحدث 12).
- تقارير الجولات في جذر الريبو: `HANDOVER_REPORT_2026-08-26_*` + `ADMIN_IMPROVEMENT_REPORT_*` + `IDEA_2026-08-25_BACKUP_SOURCE_FAILOVER_*`.

---

## 9. ⚠️ الجلسة الموازية

عامل آخر يدفع لنفس الريبو («DZ HOOF Agent»). القواعد:
- **قبل أي push**: `git fetch && git merge origin/main` (البعيد يرفض non-fast-forward).
- **بعد أي merge**: أعد تشغيل الاختبارات (`npx jest`) قبل النشر.
- لا تفترض أن الخادم محدّث بميزاتهم — تحقق sha256 أو انشر بعد المزامنة الشاملة.
- كل جلسة تدفع تقريرها في نهاية عملها — هذا التقرير هو تقرير جلسات 2026-08-26.
