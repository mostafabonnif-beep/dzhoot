# تقرير التسليم — DZ HOOF IPTV (جلسة 2026-08-26 — جلسة التحقق + التحديث الإجباري)

> **اقرأ بعد `HANDOVER_REPORT_2026-08-25_ROUND20_FINAL_AR.md`.** جلسة قصيرة: تحقق
> من إصلاح «القنوات لا تشتغل» (الجولات 21→#55) + فرض التحديث الإجباري + اكتشاف موت
> المصدر الاحتياطي. لا تغييرات كود — تغيير تشغيلي واحد في القاعدة (مفصّل أدناه).

---

## 1. الملخص التنفيذي

| الجبهة | النتيجة |
|---|---|
| **«القنوات لا تشتغل»** | ✅ **محلول ومُتحقق منه**: 101 فشل (`PARSING_CONTAINER_UNSUPPORTED`) قبل نشر `v1.0.1-20260826T004609Z` (00:50 UTC) → **0 فشل بعده** (11+ نجاح android_tv حي) |
| **بث البروكسي** | ✅ 18 قناة مُختبرة يدويًا عبر البروكسي (جزائرية/beIN/MBC/شاهد/تونس/فرنسية) — كلها `video/mp2t` حقيقي |
| **التحديث الإجباري** | ✅ `minCompatibleVersion` رُفع إلى **10012** على AppVersion 1.0.12 النشط — كل إصدار أقدم يُجبر على التحديث |
| **المصدر الاحتياطي** | ❌ **مات ومحذوف** — ottstreambox يرد 403 على API والبث (الحساب طاح)؛ المستخدم حذفه من اللوحة ليلًا. **لا backup حاليًا — Upstream نقطة فشل وحيدة** |

---

## 2. التغيير التشغيلي (في القاعدة — ليس في الكود)

```js
// قاعدة dzhoof-iptv
db.appversions.updateOne(
  { versionCode: 10012, isActive: true },
  { $set: { minCompatibleVersion: 10012 } }
);
```

- **الأثر**: `app-update.js:99` يحسب `isMandatory = isMandatory || current < minCompatible`
  → أي تطبيق أقدم من 1.0.12 يرى التحديث **إجباريًا**؛ من هو على 1.0.12 لا يتأثر.
- **السبب**: إصلاح MIME (PR #55) يتطلب التطبيق الجديد؛ الإصدارات < 1.0.8 فقدت دعم HLS
  كليًا (R8 stripping). بدون إجبار، الزبائن القدامى يبقون يرون «القنوات لا تشتغل».
- **أجهزة الزبائن لحظة الفحص** (9): 3× 1.0.9، 1× 1.0.5، 1× 1.0.5-staging،
  1× 1.0.2-staging-dev، 2× فارغ، 1× 1.0.12 — أغلبها كانت ستبقى مكسورة لولا الإجبار.
- **تحقق**: `/api/v1/app/version?currentVersion=10006` و `10009` →
  `updateAvailable:true, isMandatory:true`؛ `10012` → `false/false`.

## 3. وقائع الجلسة (تشخيص)

1. صحة النظام سليمة: 6 حاويات up (api/frontend healthy)، القرص 49%، mongo/redis متصلان.
2. آخر نشر `v1.0.1-20260826T004609Z` (00:50 UTC) — الملفات المفتاحية على الخادم
   مطابقة للريبو (sha256 `tv.js` + `utils/playback-mime.js` ✓).
3. `playback-events`: كل الفشلات قبل النشر بخطأ `ERROR_CODE_PARSING_CONTAINER_UNSUPPORTED`
   (android_tv) — يطابق جذر المشكل في PR #55. **صفر فشل بعد النشر**.
4. سجل التدقيق يُظهر أن **المستخدم بنفسه** (IP 154.244.50.231، ليلًا): اختبر Upstream
   (ok)، حذف مصدر ottstreambox (`XTREAM_SOURCE_DELETE` 6a8dc2ba…ebd)، وحذف جماعيًا
   **2163 قناة ميتة** (`bulk_delete_dead_channels`).
5. ottstreambox مؤكد ميت من جهة المزود: 403 nginx على `player_api.php` وعلى بث
   `/live/.../59192.m3u8` حتى مع UA تاع VLC — الحساب/اللوحة طاحت.
6. **263 خريطة `channelfailovermaps` يتيمة** (backupSourceId محذوف) — **خاملة بأمان**:
   `getFailoverTarget` يرجع `null` بنظافة عند غياب المصدر (لا كراش، لا تبديل أعمى).
   تُنظّف أو تُعاد كتابتها عند تركيب backup جديد.

## 4. الحالة الحالية (مُتحققة 2026-08-26 ~01:10 UTC)

- الإنتاج = main `6b88df2`؛ 0 فشل تشغيل منذ 00:50؛ اللوحتان 200؛ الشعارات عبر الوسيط ✓.
- 16,638 قناة في قائمة الزبون (بعد تنظيف المستخدم للميت)؛ 112k برنامج EPG.
- القنوات الحية تبث عبر البروكسي بنجاح (مانيفست + مقاطع 1.5–7MB).
- **الفجوة الوحيدة**: لا مصدر احتياطي → سقوط Upstream = توقف شامل بلا تبديل.

## 5. بانتظار المستخدم (محدّث)

1. **تحديث تطبيقه** لـ v1.0.12 (يُفرض تلقائيًا الآن) وتجربة القنوات.
2. **اشتراك backup جديد** (بديل ottstreambox) → نركبه + auto-match جديد للخرائط.
3. تغيير كلمة مرور admin (مكشوفة) + 2FA + كلمة root عند OVH + تدوير github.token.
4. webhook تنبيهات → `alert_webhook_url` • مفاتيح restic للنسخ الخارجي • CinetPay.

## 6. عند تركيب المصدر الاحتياطي الجديد (تذكرة تنفيذية)

- أضفه `status: Inactive` + `directPlayback: true` (لا يمس البث الحي)، فعّله بعد auto-match.
- نظّف/أعد كتابة الخرائط اليتيمة الـ263 (مرتبطة بالـ ID المحذوف).
- الفحص بالبث الفعلي لا بالـ API؛ manifest خفيف (200 + `#EXTM3U`) — بعض المزودين
  يردّون 302 بروابط نسبية والفحص العميق يفشل إيجابيًا كاذبًا.
- اختبار E2E حي قبل الإعلان: أساسي→احتياطي→استرجاع (مرتين).

---

_الجلسة: تحقق + minCompatibleVersion + توثيق. لا نشر، لا تغيير كود._
