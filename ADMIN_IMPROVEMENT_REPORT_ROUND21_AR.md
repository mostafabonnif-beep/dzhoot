# تقرير الجولة 21 — فلاتر صحة القنوات السريعة (2026-08-26)

> الجلسة: MoClaw — بطلب المستخدم: «لا يمكنني مشاهدة القنوات التي لا تعمل وحدها».

## المشكلة

- شريط إحصائيات الصحة في `/admin/channels` (يعمل / لا يعمل / لم يُختبر) كان نصًا للقراءة فقط.
- الإجراء الوحيد للقنوات الميتة كان زرّي حذف — لا طريقة لعرضها وحدها قبل الحذف.
- فلتر الحالة الموجود مدفون في قائمة رأس عمود «الحالة» (أيقونة صغيرة)، وخيار «لم يُختبر» غير مدعوم أصلًا في الـ backend.

## الحل (PR #59 — مدموج ومنشور)

- شريط الصحة أصبح **أزرار فلترة سريعة**: نقرة على «89 لا يعمل» تعرض الميتة وحدهم، نقرة ثانية تلغي، مع حالة تفعيل واضحة (تسطير) وزر «إظهار الكل». (وضع الأدمن فقط؛ المستخدم العادي يبقى شريط نصي.)
- `GET /admin/channels`: دعم `status=Untested` (`metadata.isWorking` لم يُضبط قط) + تعدد القيم بمنطق OR سليم (مغلّف بـ `$and` حتى لا يتعارض مع `$or` الخاص بالبحث النصي).
- تجميع الصحة (health) أصبح يتجاهل فلتر الحالة نفسه — العدّادات تبقى تعكس السياق (مجموعة/لغة/بحث) أثناء تفعيل فلتر حالة، بدل أن تصفر.
- `filter-options` يعلن الحالة الجديدة.

## التحقق

- +6 اختبارات جديدة (`admin-channels-status-filter.test.ts`) — **350/350** backend ✅، tsc + eslint نظيفان.
- CI أخضر بالكامل على PR #59 (backend/frontend/android/secret-guard).
- النشر: `v1.0.1-20260826T145721Z` — 6 حاويات healthy، health/ready ok.
- **تحقق حي**: ضغط «89 لا يعمل» في اللوحة → القائمة تعرض 89 قناة Dead وحدها + زر «إظهار الكل» ظهر.

## ملاحظات اكتُشفت أثناء الجلسة (لا تخص هذا التغيير)

1. **`server/package-lock.json` في git يحمل حزمة phantom `upstream-async`** (بدل `neo-async` الشرعية) منذ الالتزام الأول — 404 على npm ويكسر `npm ci` لأي بيئة جديدة. الإنتاج لديه lockfile مُصلح **لم يُدفع لـ git**؛ الجلسة الموازية أصلحته في main عبر `d935df8` (تمت المزامنة قبل push). ⚠️ يوجد أيضًا إدخال `minimizer-webpack-plugin@5.6.1` (نُشر على npm في ماي 2026) — **يوصى بتدقيق أمني لهذه الحزمة ولسجل الحواسيب التي ولّدت هذه الـ lockfiles**.
2. سجل النشر أظهر نشرًا سابقًا اليوم `T144031Z` (14:44 UTC) من الجلسة الموازية — لم يتعارض مع نشرنا (15:00 UTC).
3. فشل CI السابق على main (`8cdcdda`) سببه غالبًا الـ lockfile المكسور — حُلّ بإصلاح الجلسة الموازية.
4. قفل النشر `/tmp/dzhoot-deploy.lock` استُعمل وأُزيل بعد الانتهاء.

## الملفات

- `server/backend/src/routes/admin.js` — فلتر الحالة الجديد + healthFilter
- `server/backend/src/__tests__/admin-channels-status-filter.test.ts` — جديد
- `server/frontend/src/components/channels-page-shell.tsx` — أزرار الفلترة السريعة
- `server/frontend/src/components/locale-provider.tsx` — مفتاح `channels.showAll` (ع/En/Fr)
