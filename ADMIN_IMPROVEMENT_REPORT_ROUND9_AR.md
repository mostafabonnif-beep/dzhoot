# تقرير إكمال منظومة الموزع والأكواد — DZ HOOF IPTV (الجولة التاسعة)

**التاريخ:** 2026-08-24 (جولة تاسعة)
**النطاق:** منظومة الرصيد والأكواد للموزعين (إكمال الفكرة التي طلبها المستخدم)
**الحالة:** ✅ مُنفَّذ ومرفوع على GitHub (`72347ce`) + منشور على الإنتاج

---

## 1) الفكرة (كما طلبها المستخدم)

> "الموزع يقوم بإنشاء الكود الخاص به حسب الرصيد الذي يشتريه من عندي، لكي عندما يفعل الكود تبدأ تحسب له الأيام"

**التحقق:** هذه الحلقة شغّالة منذ الجولة 7 (رصيد → توليد ذاتي → التفعيل يبدأ احتساب مدة الاشتراك من `plan.durationDays`). الجولة 9 أكمَلت الفكرة بـ 5 تحسينات تجعلها منظومة متكاملة.

## 2) ما نُفّذ

### أ) بادئة أكواد خاصة بكل موزع — "الكود خاصته"
- حقل `prefix` فريد (3-6 أحرف/أرقام) لكل محل، يضبطه الأدمن.
- كل كود يولّده المحل يحمل بادئته: `ALG1-XXXX-XXXX` — تعرف فورًا أي محل باع أي كود.
- تظهر البادئة كشارة بجانب اسم المحل في القائمة + حقل في نموذج الإنشاء/التعديل.

### ب) محاسبة المشتريات — "الرصيد الذي يشتريه من عندي"
- حركات المنح تسجّل الآن **سعر الجملة** (`unitPrice`) و**قيمة المشتريات** (`amount = الكمية × السعر`).
- الموزع يرى في بوابته قسم **"حسابي (المشتريات)"**: الرصيد المُشترى (كمية وقيمة بالدج)، المُستهلك، الصافي المتبقي.
- سجل الحركات (للأدمن والموزع) يعرض المبلغ لكل حركة منح.

### ج) الموزع يرى احتساب الأيام بعد التفعيل
- نافذة الأكواد في البوابة تعرض لكل كود مُفعَّل: **بداية الاشتراك** و**تاريخ النهاية** (من الاشتراك الفعلي المرتبط بالتفعيل عبر `ActivationRedemption → Subscription`).
- الموزع يطمئن الزبون ويؤكد له المدة المتبقية فعليًا.

### د) تنبيه الأكواد القريبة من الانتهاء
- `/reseller/me` يعيد عدد الأكواد غير المستخدمة التي تنتهي خلال **7 أيام**.
- البوابة تعرض بطاقة تنبيه: "⚠️ N كود غير مستخدم سينتهي خلال 7 أيام — بعها أو استرجعها".

## 3) الاختبارات

- **الباكند: 34 مجموعة / 250 اختبارًا ناجحًا** (كان 247 → **+3**): بادئة فريدة وتخزينها UpperCase، توليد الأكواد ببادئة المحل، تسجيل سعر/مبلغ المشتريات في حركة GRANT.
- tsc نظيف (باكند + واجهة) + ESLint بلا أخطاء.

## 4) النشر

- GitHub `main` = `72347ce` (9 ملفات، +271 سطرًا). مزامنة إلى `/opt/dzhoot/server` (checksums 9/9) ثم النشر الرسمي.

## 5) ملفات تغيّرت

```
backend/src/models/Reseller.ts                            (+prefix فريد)
backend/src/models/CreditTransaction.ts                   (+unitPrice, amount)
backend/src/services/subscription-service.ts              (recordCreditTx بالأسعار)
backend/src/routes/reseller.js                            (بادئة التوليد + نوافذ الاشتراك + ملخص الحساب + expiringSoon)
backend/src/routes/admin-resellers.js                     (بادئة + تسجيل المشتريات + سجل بالمبالغ)
backend/src/__tests__/credit-ledger.test.ts               (+3 اختبارات)
frontend/src/app/(dashboard)/admin/resellers/page.tsx     (حقل + شارة البادئة)
frontend/src/app/reseller/page.tsx                        (حسابي + تنبيه + تواريخ الاشتراك)
frontend/src/components/locale-provider.tsx               (مفاتيح عربي/إنجليزي/فرنسي)
```

## 6) ملاحظات

- الأكواد القديمة (بدون بادئة) تبقى كما هي بـ `DZHF`؛ المواليد الجدد فقط يحملون بادئة المحل.
- `unitPrice` تُسجَّل لحظة المنح — إذا غيّر الأدمن سعر الجملة لاحقًا، المشتريات السابقة تبقى بقيمتها الأصلية (سجل ثابت).
- التحقق الحي بعد النشر: محل مؤقت ببادئة → توليد أكواد بالبادئة → عرض نهاية الاشتراك لكود مُفعَّل → ملخص الحساب → تنظيف.
