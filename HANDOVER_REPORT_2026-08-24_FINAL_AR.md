# تقرير التسليم النهائي — DZ HOOF IPTV (جلسة 2026-08-24، جولات 7→11)

> **اقرأ هذا أولًا.** الوثيقة الوحيدة التي تحتاجها لبدء العمل: الوصول، كل ما أُنجز اليوم (5 جولات)، حالة الإنتاج، ما تبقى، الأوامر السريعة، والمزالق. المراجع التفصيلية: `ADMIN_IMPROVEMENT_REPORT_ROUND7..11_AR.md` + `HANDOVER_REPORT_2026-08-24_AR.md` (جلسة سابقة) + `PROJECT_STATE_HANDBOOK_AR.md`.

---

## 1. الملخص التنفيذي

| الجبهة | النتيجة |
|---|---|
| **المستودع** | ✅ GitHub `main` = الخادم = `154e27c` — 22 ملفًا متحققًا بالـ checksums |
| **منظومة الموزعين** | ✅ حلقة كاملة: شراء رصيد → توليد ذاتي ببادئة المحل → تفعيل → احتساب الأيام (مرئية للموزع) + محاسبة + سجل + استرجاع + انتهاء صلاحية |
| **لوحة الأدمن** | ✅ نظرة الأعمال + مبيعات المحلات + سجل الرصيد + قيمة المشتريات + إعدادات التنبيهات/البريد/الصلاحية |
| **التنبيهات** | ✅ جاهزة من الواجهة (كانت معطلة: webhook فارغ) — تنتظر رابطًا حقيقيًا من المستخدم |
| **الأسعار** | ✅ مضبوطة (مقترحة): شهري 500 / 6أشهر 2500 / سنوي 4500 — `pricesSet: true` |
| **الإنتاج** | ✅ `v1.0.1-20260824T180501Z` — 6 حاويات healthy |
| **الاختبارات** | ✅ 251/251 + tsc + eslint نظيفة |

## 2. الوصول (ثابت)

- **SSH**: `dzhoof-admin@5.135.79.221` بمفتاح `/home/user/.ssh/dzhoof_id` (تعليقه `dzhoof-admin@vps`). sudo بلا كلمة مرور.
- **GitHub**: `mostafabonnif-beep/dzhoot` — التوكن `/etc/dzhoot/github.token` (PAT). الدفع: `git push https://x-access-token:$TOKEN@github.com/mostafabonnif-beep/dzhoot.git main`.
- **الإنتاج**: `/opt/dzhoot/server` (نسخة ملفات، ليست git) + `/etc/dzhoot/.env.production` (chmod 600).
- **لوحة التحكم**: `https://iptv.ld-11.net/admin` — `admin` / `DZHOOF-b358a74599573e6e84ae7bcf18ab37da`. ⚠️ **كلمات المرور مكشوفة في محادثة — غيّرها (أولوية أمنية)**.
- **قاعدة البيانات**: الحاوية `dzhoof-mongodb` — **قاعدة الإنتاج اسمها `dzhoof-iptv`** (ليست `test`!) — راجع المزالق.

## 3. ما أُنجز اليوم (الجولات 7-11)

### جولة 7 — مزامنة + نظرة الأعمال
- رفع ميزة "رصيد المحلات" غير المرفوعة من الخادم إلى GitHub + إصلاح نقص استيراد `Reseller` (كان سيعيد 500).
- `GET /admin/business/summary`: تفعيلات/إيراد/اشتراكات/رصيد + آخر التفعيلات بأكواد مقنّعة. قسم "نظرة الأعمال" في اللوحة. عمود الرصيد في المحلات.

### جولة 8 — سجل الرصيد + استرجاع + انتهاء صلاحية + استقلالية الموزع
- `CreditTransaction` (GRANT/CONSUME/RETURN/EXPIRE_RETURN + balanceAfter) — للأدمن والموزع.
- استرجاع يدوي للأكواد غير المستخدمة (يعيد الرصيد). مهمة `code-expiry-check` اليومية (صلاحية 30 يومًا افتراضيًا) تعيد الرصيد تلقائيًا.
- بوابة الموزع: تغيير كلمة المرور ذاتيًا + أسعار الجملة.

### جولة 9 — إكمال فكرة الموزع (طلب المستخدم)
- بادئة أكواد فريدة لكل محل (`Reseller.prefix` 3-6) — الكود "خاصته".
- محاسبة المشتريات: `unitPrice` + `amount` في المنح + قسم "حسابي" للموزع.
- نوافذ الاشتراك في البوابة (بداية/نهاية) — الأيام تُحسب من التفعيل ويراها الموزع. تنبيه انتهاء قريب (7 أيام).

### جولة 10 — تحسين لوحتي الأدمن والموزع
- الأدمن: جدول "مبيعات المحلات" (تفعيلات الشهر/الإجمالي + مشتريات) + عمود "قيمة المشتريات".
- الموزع: نسخ كود فردي + بحث في الدفعات + بطاقة "تفعيلات الشهر".

### جولة 11 — التنبيهات + البريد + الصلاحية + الأسعار
- **سبب تعطل التنبيهات**: `ALERT_WEBHOOK_URL` فارغ + Brevo SMTP ناقص في `.env`.
- قسم "التنبيهات والإشعارات" في الإعدادات (webhook + Brevo + MAIL_FROM → AppSetting يُقرأ عند التشغيل) + زرا "تنبيه تجريبي"/"بريد تجريبي" + حقل `code_expiry_days` (1-365) + `/health?details=true` يعكس webhook المحفوظ.
- **الأسعار مضبوطة**: تجربة 0 / شهري 500 / 6أشهر 2500 / سنوي 4500 دج (قابلة للتعديل من صفحة الباقات).

## 4. حالة الإنتاج (مُتحققة 2026-08-24)

- 16,640 قناة | 7 مستخدمين | 6 اشتراكات نشطة | 2 محل (بلا بادئة/رصيد بعد) | EPG ~1184 قناة.
- 6 حاويات healthy | آخر نشر `v1.0.1-20260824T180501Z` | نسخة mongodump قبل كل نشر.
- القاعدة نظيفة (0 حركات رصيد يتيمة، 0 بيانات اختبار) | `pricesSet: true` | `code_expiry_days: 45` محفوظ.

## 5. ما تبقى (بالأولوية)

### 🔴 أمني (عاجل)
- [ ] **تغيير كلمة مرور root** + **كلمة مرور admin** (مكشوفتان في المحادثة) + مراجعة `github.token`

### 🔴 لتفعيل التنبيهات والبريد (خطوتان من اللوحة)
- [ ] وضع **رابط webhook حقيقي** (Discord/Slack) في الإعدادات + "إرسال تنبيه تجريبي"
- [ ] وضع **مفاتيح Brevo** (SMTP login + key) + بريد المُرسِل + "إرسال بريد تجريبي"

### 🔴 تجاري
- [ ] **مراجعة الأسعار المقترحة** (شهري 500 / 6أشهر 2500 / سنوي 4500 — غيّرها من صفحة الباقات حسب سوقك)
- [ ] **بوابة دفع** (CinetPay/Paymee/Stripe)
- [ ] **حد الأجهزة عند التشغيل** (تحقق: `DEVICE_LIMIT_REACHED` موجود في redeemCode — جرّبه فعليًا)
- [ ] تعيين بادئات للمحلين الحقيقيين + شحن رصيد فعلي

### 🟡 EPG
- [ ] مصدر عربي/جزائري شامل (أكبر فجوة محتوى) | beIN SPORTS 5 + MAX | تعطيل 13 مصدرًا فاشلًا

### 🟡 كتالوج
- [ ] تنظيف: 16,635 قناة معطلة + أسماء متسخة (`## TR|…`, `NM:`) — أدوات bulk/purge موجودة

### 🟢 تشغيلي
- [ ] نسخ احتياطي خارجي (restic) + اختبار استعادة | R8 لتصغير APK | ترقية Media3

## 6. أوامر سريعة

```bash
ssh dzhoof                                    # الخادم
curl -s https://iptv.ld-11.net/health/ready   # صحة
curl -s "https://iptv.ld-11.net/health?details=true" | jq .details.alertingConfigured  # حالة التنبيهات
# نشر: cd /opt/dzhoot/server && sudo bash scripts/deploy/deploy-production.sh --apply
# اختبار محلي: cd server && npm ci && npm run build -w @dzhoof/shared && cd backend && npx jest
# واجهة: cd server/frontend && npx tsc --noEmit && npx eslint "src/app/**/*.tsx"
# GitHub push: TOKEN=$(ssh dzhoof "sudo cat /etc/dzhoot/github.token"); git push https://x-access-token:$TOKEN@github.com/mostafabonnif-beep/dzhoot.git main
# Mongo: sudo docker exec dzhoof-mongodb mongosh dzhoof-iptv   ← حدد القاعدة دائمًا!
# مهام EPG: POST /api/v1/scheduler/trigger/epg-refresh (X-Session-Id admin)
```

## 7. دروس ومزالق (مهمة)

1. **mongosh داخل الحاوية يتصل بقاعدة `test` الافتراضية وليست `dzhoof-iptv`!** أي تنظيف: `mongosh dzhoof-iptv`. (جولة 9: ظهرت "كل المحلات = 0" على القاعدة الخطأ — بيانات الإنتاج سليمة.)
2. **كود الخادم غير المرفوع لم يمر على CI أبدًا** — عند المزامنة من الخادم شغّل CI كاملًا قبل النشر (`reseller.js` كان سيعيد 500).
3. نماذج Mongoose: `module.exports = Model; export default Model;` | مسارات `bulk` قبل `/:id` | `PlaybackQualityData` مستوردة من `@/lib/api`.
4. `Map<string, T>` صياغة TS — لا تُستخدم في ملفات `.js` (خطأ tsc في admin.js).
5. النشر ~10 دقائق؛ انتظر `DEPLOY COMPLETE` ولا تقاطعه.
6. عند نسخ ملفات عبر scp بمسارات فيها أقواس `(dashboard)` — تحقق بالـ checksums بعد النسخ (حدثت DIFF في جولة 11 بسبب شرط معقد).
7. IP بيئة الوكيل `136.118.71.205` قد يُحظر من fail2ban — `fail2ban-client unban 136.118.71.205`.
