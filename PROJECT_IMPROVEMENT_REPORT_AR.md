# تقرير تسليم وتحسين مشروع DZ HOOT

**المستودع:** [merci1994dz/dzhoot](https://github.com/merci1994dz/dzhoot) [1]  
**التاريخ:** 12 أغسطس 2026  
**الحالة:** بوابة الجودة المحلية والبعيدة ناجحة، والفرع `main` متزامن مع GitHub.

## الملخص التنفيذي

تم تنفيذ دفعة جديدة من التحسينات على مشروع DZ HOOT، وهو منصة IPTV ذاتية الاستضافة مبنية على FireVision IPTV. ركزت هذه الدفعة على سد خلل أمني مهم في إبطال الجلسات، تقوية حدود الأجهزة، توحيد سجل التدقيق، منع تسريب بيانات المصادر في السجلات، إضافة مراقبة تشغيلية قابلة للاستخدام، وإنشاء آلية موثقة للنسخ الاحتياطي والاستعادة.

> **النتيجة المهنية:** المشروع أصبح أقرب بوضوح إلى منتج قابل للتشغيل التجاري، لكنه لا ينبغي اعتباره جاهزاً للإطلاق العام دون تنفيذ بنود الإطلاق المتبقية الواردة في هذا التقرير، وخصوصاً اختبار الاستعادة الفعلي، التخزين خارج الخادم، وتطبيق بوابة تشغيل بروابط بث opaque لا تحتوي على بيانات اعتماد.

## التغييرات المنفذة في هذه الدفعة

### 1. الصلاحيات والجلسات

تم إصلاح endpoint `POST /api/v1/auth/revoke-other-sessions` بحيث يكفيه تحقق المستخدم، فلا يحتاج المستخدم العادي إلى صلاحية Admin لإبطال جلساته الأخرى. والأهم أن العملية أصبحت مقيدة بفلتر `userId` الخاص بالمستخدم الحالي مع استثناء الجلسة الحالية؛ وهذا يمنع الخطأ السابق الذي كان يمكن أن يحذف جلسات مستخدمين آخرين عند استدعاء العملية.

أُضيف اختبار انحدار يتحقق من أمرين: بنية فلتر الملكية، وغياب middleware الخاص بـ Admin من المسار. كما بقيت عمليات إبطال الجلسة المفردة وتسجيل الخروج مقيدة بملكية الجلسة.

### 2. حدود الأجهزة

تم تقوية `registerDevice` عبر تطبيع `deviceId` والتحقق من كونه نصاً غير فارغ وبحد أقصى 200 حرف. أُضيف قفل محلي لكل مستخدم وقفل موزع اختياري عبر Redis لتقليل سباق الطلبات المتزامنة الذي كان قد يسمح بتجاوز حد الأجهزة عند وصول طلبين في الوقت نفسه. كما تمت معالجة سباق الجهاز نفسه عبر الفهرس الفريد `(userId, deviceId)`، وأصبح ازدحام القفل يعيد حالة `503` قابلة لإعادة المحاولة بدلاً من اعتباره رفض اشتراك دائم.

### 3. سجل التدقيق وحماية البيانات الحساسة

أصبحت عمليات إنشاء وتعديل وحذف واختبار ومزامنة مصادر M3U وXtream مسجلة بصورة أكثر اتساقاً، مع تسجيل نجاح وفشل المزامنة غير المتزامنة. أُضيفت دالة مركزية لتنقية رسائل الأخطاء من كلمات المرور الموجودة داخل عناوين URL، ومعلمات `username` و`password` و`token` و`api_key`، وBearer tokens. واستُخدمت التنقية أيضاً قبل حفظ `lastError` الخاص بالمصادر في قاعدة البيانات.

تم حذف طباعة رابط alternate stream الخام من سجل خدمة فحص الصحة، كما أصبح سجل الأخطاء العام أكثر انتظاماً ويتضمن correlation ID.

### 4. المراقبة التشغيلية

أصبح لكل طلب correlation ID محدود وآمن يُعاد في رأس الاستجابة `X-Request-ID` ويظهر في Morgan logs. كما أصبحت سجلات الوصول تنقح معاملات `sid` و`token` و`url` وبيانات الدخول قبل كتابتها، وهو مهم لأن بعض مسارات proxy تستقبل المصدر في query string.

أُضيفت نقاط التشغيل التالية:

| المسار | الغرض | اعتماد MongoDB/Redis |
| --- | --- | --- |
| `/health/live` | فحص أن العملية حية | لا يعتمد على أي قاعدة بيانات |
| `/health/ready` | فحص جاهزية API الأساسية | يتطلب MongoDB؛ Redis اختياري |
| `/health` | الحالة الأساسية المتوافقة مع المسار القديم | يعرض MongoDB وRedis ووقت التشغيل والإصدار |
| `/health?details=true` | تفاصيل تشغيلية غير حساسة | يعرض عدد المصادر وحالات مزامنتها، وعدد برامج EPG، وحالات المهام المجدولة |

تتضمن تفاصيل المصادر عدد المصادر الكلي والنشطة والمزامنة حالياً والتي بها خطأ، إضافة إلى آخر وقت مزامنة ورسالة خطأ منقحة. أما scheduler فيعرض أسماء المهام وحالتها وآخر تشغيل وموعد التشغيل التالي دون تسريب أسرار المصدر.

### 5. النسخ الاحتياطي والاستعادة

أُضيف السكربت التنفيذي `server/scripts/backup.sh`. ينشئ السكربت نسخة MongoDB بصيغة archive مضغوطة، يستخدم ملفاً مؤقتاً ثم نقلاً ذرياً، يتحقق من gzip، يطبق صلاحيات `0600`، وينظف النسخ الأقدم وفق `RETENTION_DAYS`. لا يطبع `MONGODB_URI` ولا يضع أي سر داخل المستودع.

أُضيف الدليل `server/docs/BACKUP_RESTORE.md`، ويتضمن المتغيرات، التشغيل اليدوي، cron، التخزين خارج الخادم، اختبار سلامة الملف، الاستعادة في بيئة اختبار، وتحذير استخدام `--drop` في الإنتاج.

## التغييرات التي كانت مكتملة قبل هذه الدفعة

ضمن دورة التطوير الأوسع، تم أيضاً تنفيذ وتحقيق العناصر التالية:

| المجال | ما تم إنجازه |
| --- | --- |
| EPG والمصادر | حماية XMLTV وM3U وXtream من SSRF مع DNS pinning وعدم السماح بإعادة التوجيه، وربط XMLTV المخصص بمصادر M3U، وإضافة مزامنة Xtream وM3U دورية قابلة للضبط |
| الكتالوج | إضافة وإحكام مسارات Movies وSeries وSeasons وEpisodes، مع pagination محدود، search آمن من regex injection، والتحقق من ObjectId وملكية الموسم للمسلسل |
| واجهة الإدارة | صفحات Movies وSeries وM3U Sources، مع labels عربية ودعم RTL وتحسينات العرض والبحث والتصفية والتصفح |
| التشغيل | إصلاح حساب محاولات failover، إضافة recovery عند buffering الطويل، وإصلاح حل قوالب alternate stream ومنع تكرار الرابط الأساسي |
| Android TV | إصلاح OptIn الخاص بـ Media3، إصلاح تحذير قاعدة البيانات في AppUpdater، جعل Firebase اختيارياً في البيئة المحلية، ونجاح بناء APK الاختباري |
| الأمن العام | تطبيق تحسينات سابقة شملت headers الأمنية، rate limiting، CSRF، حماية proxy من SSRF، وتحسينات JWT/session وOAuth بحسب ما هو موجود في تاريخ المشروع |

## نتائج بوابة الجودة

| الفحص | النتيجة |
| --- | --- |
| `npm run typecheck` للـ backend | ناجح |
| اختبارات backend | **123 اختباراً ناجحاً في 12 suite** |
| `npm run lint` للـ backend | ناجح مع **0 أخطاء** و227 تحذيراً قائماً من قاعدة `no-explicit-any` وملفات قديمة؛ التحذيرات لا تمنع البناء حالياً |
| بناء backend | ناجح |
| بناء frontend Next.js | ناجح؛ تم توليد 39 صفحة |
| اختبارات frontend | **اختباران ناجحان** |
| Android `testDebugUnitTest lintDebug assembleDebug` | ناجح في التشغيل الأخير؛ وكان رصيد اختبارات Android المتحقق خلال دورة التطوير **406 اختبارات**، مع 0 أخطاء lint ووجود تحذيرات غير مانعة كما في ملخص المرحلة السابقة |
| APK Debug | تم توليده في `android/app/build/outputs/apk/debug/app-debug.apk` بحجم يقارب 93 MB |
| اختبار `backup.sh` | ناجح باستخدام mongodump تجريبي؛ تم التحقق من gzip ومن صلاحية الملف `0600` |
| GitHub Actions CI | التشغيل رقم `31559725893` ناجح؛ وظيفتا `backend` و`frontend` ناجحتان |
| حالة Git | الشجرة نظيفة، و`origin/main` يطابق commit `948fed3` |

## سجل commits النهائي المهم

| Commit | الوصف |
| --- | --- |
| `948fed3` | تقوية الجلسات والمراقبة والنسخ الاحتياطي |
| `01112c7` | إخفاء بيانات اعتماد روابط التشغيل من استجابات الكتالوج والتفويض |
| `455eb26` | إصلاح حل قوالب alternate streams في التشغيل |
| `4a65a84` | تقوية catalog وEPG وplayback recovery |
| `a0ee8a8` | أتمتة مزامنة Xtream وM3U |
| `1f9fd6f` | إضافة إدارة Movies وSeries في لوحة الإدارة |
| `9c9ab05` | إضافة VOD وSeries API وArabic sidebar |
| `597928e` | تحسينات الأمن وRTL وإصلاحات البناء |

## العناصر المتبقية قبل الإطلاق التجاري الكامل

### أولوية P0: بوابة بث opaque بدلاً من query URL

على الرغم من أن الكتالوج وendpoint التفويض لا يعيدان رابط المصدر الخام، فإن مسار proxy الحالي ما زال يستقبل المصدر في معامل `url` داخل رابط proxy. لذلك فإن التخلص الكامل من ظهور بيانات Xtream/M3U في عنوان URL يتطلب خدمة tokens قصيرة العمر: ينشئ الخادم token opaque موقّعاً أو مشفراً، يخزن mapping قصير العمر في Redis، ثم يقبل proxy token فقط ولا يقبل URL upstream من العميل. هذه هي الخطوة الأمنية الأهم المتبقية قبل اعتبار حماية بيانات المصدر مكتملة.

### أولوية P0: اختبار استعادة فعلي ومراقبة خارجية

يجب تنفيذ استعادة فعلية من نسخة منتجة إلى بيئة منفصلة، ثم اختبار تسجيل الدخول والكتالوج وEPG والتشغيل. كما يجب إرسال النسخ إلى تخزين منفصل مشفر، وإضافة تنبيه عند فشل مهمة النسخ أو غياب نسخة حديثة؛ وجود السكربت وحده لا يضمن التعافي.

### أولوية P1: نموذج التشغيل التجاري

ما زال المشروع يحتاج إلى إكمال طبقة VOD وSeries داخل تطبيق Android TV، وإدارة reseller أو multi-tenant إن كانت ضمن نموذج العمل، ونظام دفع أو اشتراكات مرتبط ببوابة دفع مناسبة، وسياسات حصص وتدقيق مالية، وصفحة شروط وخصوصية نهائية مرتبطة ببيئة الإنتاج.

### أولوية P1: ميزات المنتج المتقدمة

لم تُنفذ بعد وظائف DVR والتسجيل، catch-up وtimeshift، المفضلة والمشاهدة الأخيرة عبر الأجهزة، إشعارات الأعطال، تحليلات المشاهدة، وأدوات إدارة المحتوى المتقدمة. هذه ليست عوائق لصحة MVP الحالي، لكنها عناصر ترفع القدرة التنافسية وتجربة المستخدم.

### أولوية P2: تقليل التحذيرات التقنية

بوابة lint ناجحة حالياً، لكن 227 تحذيراً، معظمها `no-explicit-any` في ملفات قديمة، تستحق خطة تنظيف تدريجية. ينبغي تحويل التحذيرات الأكثر حساسية في auth وbilling وsource sync إلى أنواع دقيقة، ثم رفع مستوى التحذير إلى خطأ على مراحل.

## توصية الإطلاق

أوصي بإطلاق تجريبي مغلق أولاً بعد تنفيذ tokenized proxy واختبار الاستعادة الفعلي. يبدأ الاختبار بعدد محدود من المستخدمين ومصادر يملك المشروع حقوق استخدامها، مع تفعيل HTTPS، أسرار إنتاج قوية، Redis وMongoDB مراقبين، backup خارج الخادم، alerting، وسجل تدقيق قابل للمراجعة. بعد أسبوعين من مراقبة الأخطاء والفشل في التشغيل، يمكن اتخاذ قرار التوسع التجاري بناءً على بيانات حقيقية لا على الانطباع فقط.

## المراجع

[1]: https://github.com/merci1994dz/dzhoot "مستودع DZ HOOT على GitHub"
