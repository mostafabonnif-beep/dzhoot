# تقرير تسليم — DZ HOOF IPTV (2026-08-31: مصدر Business Cloud NEO — تقوية + إصلاح صحة الـ Watchdog)

**التاريخ:** 2026-08-31
**الطلب:** إضافة البث البديل `business-cloud-neo` (tv.business-cloud-neo.com / cf.business-cloud-neo.ru) لتقوية التطبيق.
**الحالة:** ✅ منجز — الإصلاح منشور على `main` (سحق PR #130 → `8c7e310`) ونُشر ذريًا على الإنتاج (`v1.0.1-20260831T124931Z`).

---

## 1) الوضع قبل العمل (المُتحقق حيًا)

| البند | القيمة |
|---|---|
| مصدر Xtream الوحيد | **Business Cloud NEO** (`https://cf.business-cloud-neo.ru`) — `Active` + `directPlayback` + `customerVisible` |
| الكتالوج | 16,661 قناة + 67,072 فيلم + 16,928 مسلسل (كل قنوات الإنتاج تقريبًا) |
| حالة المصدر | `degraded` — بسبب خطأ فحص الـ Watchdog (انظر أدناه) |
| خرائط Failover | 263 خريطة لكن **كلها تشير لمصدر محذوف** (ottstreambox) → التبديل الاحتياطي غير فعّال فعليًا |
| الاعتمادات التي أرسلها المستخدم | `71d3195c87ae/cd479cfe1ab` — **مرفوضة من البانل (HTTP 513)** — الحساب غير صالح/منتهي |
| الاعتمادات الفعّالة في الإنتاج | `d244a7fff1ae/7998e94f9b` — تعمل (auth=1, "Welcome to World NEO 4K PRO") |

## 2) الجذر الحقيقي لمشكلة `degraded`

`source-failover-service.ts` كان يفحص **كل** روابط الـ direct-playback كـ HLS manifest مع حد استجابة 512KB.
بثّ TS (نوع `playbackFormat: ts` في بانل NEO) **لا نهائي** — فيفشل كل فحص بـ
`maxContentLength size of 524288 exceeded` ويُثبّت الـ Watchdog `verificationStatus=degraded` كل 60 ثانية،
مما كان **يعطّل المزامنة المجدولة** (تتطلب `verified`) ويُبقى الإنذارات مفعّلة بلا سبب.

## 3) الإصلاح (PR #130)

- `probeManifest` → `probePlaybackUrl`: روابط `.ts` تُفحص فحصًا خفيفًا (استجابة stream، حد 1KB، تدمير فوري، HTTP 200-399 = حيّ) بنفس نمط `stream-prober`؛ روابط HLS تحتفظ بفحص المانيفست.
- اختباران جديدان (مصدر TS سليم يبقى verified / مصدر TS ميت → degraded).
- التحقق: **Backend 453/453** ✅، tsc نظيف ✅، eslint 0 أخطاء ✅، CI أخضر (backend + CodeQL + Secret guard) ✅.

## 4) النشر والتحقق الحي بعد النشر

- نشر ذري: `APPLY=1 atomic-deploy.sh 8c7e310` → كل الحاويات صحية، health gate نجح.
- الـ Watchdog: **verified** في جولتين متتاليتين (ثبات) — `lastError: null`.
- تشخيص حي جديد: API `ok` (auth=1 Active)، بث حي **3/3 قنوات حيّة** (TS، 200 video/mp2t بعد 302 إلى CDN موقّع)، M3U export محجوب من IP الخادم (884 — مقبول بالتصميم لأن المزامنة تستخدم player_api لا get.php).

## 5) ملاحظات وقرارات (لم تُنفَّذ عمدًا)

1. **لم أُضف `tv.business-cloud-neo.com` كمصدر ثانٍ** — نفس البانل/الحساب؛ القنوات مفاتيحها per-source في MongoDB، فإضافة مصدر ثانٍ = **ازدواج كامل للكتالوج** (16.6 ألف قناة مكررة) يضعف التطبيق لا يقوّيه. النطاقان يعملان بنفس الاعتمادات الفعّالة.
2. **لم أستبدل الاعتمادات العاملة بجديدة** — الاعتمادات الجديدة (71d3195c87ae) مرفوضة 513؛ الاستبدال كان سيكسر البث. إن كانت هذه اعتمادات حساب جديد، يلزم تفعيل الحساب لدى المزود أولًا.
3. **خرائط Failover اليتيمة (263)** — تشير لمصدر ottstreambox المحذوف؛ التبديل الاحتياطي لا يعمل حاليًا. توصية: إما إعادة إضافة مصدر ottstreambox ببياناته، أو حذف الخرائط اليتيمة، أو إعادة توجيهها لمصدر احتياطي حقيقي. لم ألمسها (بيانات مستخدم).
4. **`get.php` محجوب من IP الخادم (884)** — سلوك متوقع للمزود تجاه IPs الخوادم؛ لا يؤثر على الزبائن (direct playback من شبكاتهم).

## 6) خطوات مستقبلية مقترحة

- تأكيد من المستخدم: هل اعتمادات `71d3195c87ae` حساب جديد يجب تفعيله؟ وهل يوجد حساب/بيانات لمزود ottstreambox لإعادة تفعيل الـ Failover؟
- مراقبة جولات `source-watchdog` القادمة للتأكد من بقاء المصدر `verified` لساعات.

---

## 7) جولة ثانية — «اعمل الأفضل» (2026-08-31 مساءً)

### 7.1 إعادة فحص إمكانية إحياء التبديل الاحتياطي

| الفحص | النتيجة |
|---|---|
| اعتمادات ottstreambox من تقرير 2026-08-25 (`144881537461`) | **ميت نهائيًا**: 403 Forbidden من nginx على player_api — الحساب منتهي عند المزود، لا يمكن إعادته |
| الاعتمادات الجديدة من المستخدم (`71d3195c87ae`) | ما تزال مرفوضة (513) — حساب غير مفعّل/منتهي |
| نطاق `tv.business-cloud-neo.com` كمِرآة | API + كتالوج يعملان (get_live_streams 5.9MB ✅) لكن **البث الحي منه 456 (محجوب)** من IP الخادم → ليس مِرآة كاملة، لم أُبنِ ميزة مِرآة (عائد غير مؤكد) |

**الخلاصة:** التبديل الاحتياطي الفعلي يتطلب اشتراك مزود ثانٍ — هذا الوحيد الذي لا يمكن للوكيل توفيره ذاتيًا؛ يحتاج قرار المستخدم.

### 7.2 تحقق الاستقرار بعد الإصلاح

- `source-watchdog` كل 60 ثانية: **كل الجولات `completed`** والمصدر ظل `verified` (لم يعد يتقلب) — `verifiedAt` 12:55:34، `lastError` توضيحي فقط (M3U export محجوب).
- `xtream-sync` المجدولة (12:02): **اكتملت بنجاح** (1 مصدر، 0 فشل، 229 ثانية) → `lastSyncAt` 12:05:58 — مسار المزامنة يعمل.
- لا أخطاء في حاويات API/Scheduler، النسخ الاحتياطي restic محلي + خارجي نشط (timers يومية).

### 7.3 صحة القرص (تدخل وقائي)

- القرص كان **91%** (7.1GB حر) — 82 مجلد `deploy-*` + 38 ملف مصدر متراكمة بلا احتفاظ (سكربت النشر لا ينظّف).
- نُفّذ: إبقاء أحدث 15 مجلد نشر + أحدث 10 أرشيفات مصدر، حذف الباقي → **84% (13GB حر)**.
- أُضيف `/etc/cron.d/dzhoot-backup-prune` يوميًا 04:30 — يمنع إعادة التراكم.

---

## 8) جولة ثالثة — «اعمل مِرآة: إذا توقف الأول يبحث في الثاني أتوماتيكيًا» (2026-08-31 مساءً)

### المطلوب
مِرآة تلقائية: إذا توقف النطاق الأول (cf) يتحول التطبيق تلقائيًا للثاني (tv) — دون توقف عرض القنوات.

### القيد المعماري المكتشف
- الإنتاج يمرر كل البث عبر الخادم (`ALLOW_DIRECT_PLAYBACK=false` — قرار موثّق «البروكسي إجباري — إخفاء المصادر»؛ لا يُعكس).
- نطاق `tv` يخدم API والكتالوج من الخادم (200) لكنه **يحجب البث الحي من IP الخادم (456)** — بينما `cf` يخدم البث الحي (200 video/mp2t).

### ما بُني (PR #133 → `84bca1d` → `v1.0.1-20260831T132404Z`)
1. **`XtreamSource.mirrorServerUrls`** — نطاقات بديلة لنفس الحساب (وليس مزودًا ثانيًا؛ ذلك دور خرائط الـ Failover).
2. **تتابع تلقائي في الـ API** (`apiGet` يجرب الأساسي ثم المرايا): لو انقطع cf API → المزامنة والتحقق والمسلسلات تستمر عبر tv.
3. **إعادة كتابة رابط البث للمِرآة** في تدفق توكين التشغيل عندما يعلن الـ watchdog توقف الأساسي (الفتحة 0 فقط؛ **catch-up لا يُحوَّل أبدًا**) + علامة `source:'mirror'` في الرد؛ نفس المنطق لمسارات توكين v2.
4. لوحة التحكم: `mirrorServerUrls` في إنشاء/تعديل المصدر (تحقق http(s)).

### التحقق
- Backend **460/460** (7 اختبارات مرآة جديدة)، tsc نظيف، eslint 0 أخطاء، CI أخضر.
- **إثبات حي للتتابع**: مصدر تجريبي بأساسي ميت (`127.0.0.1`) + مِرآة tv → `api.ok=true` (auth=1) عبر `server.url = tv.business-cloud-neo.com` — ثم حُذف المصدر التجريبي.
- المصدر الحقيقي: `mirrorServerUrls=["http://tv.business-cloud-neo.com"]`، `verified`، الـ watchdog مستقر.
- على الزبائن: إعادة كتابة البث تمنحهم رابط tv مباشرة (نطاقهم المفضل) عند توقف cf — تتحقق من شبكتهم لا من الخادم.

### التوصية لإكمال الحل (خطوة واحدة منك)
لتفعيل المِرآة **كاملة في وضع البروكسي** (البث عبر الخادم)، اطلب من مزوّد business-cloud-neo **إدراج IP الخادم `5.135.79.221` في القائمة البيضاء** لنطاق tv — حينها يعمل التحويل التلقائي للبث فورًا بلا أي تغيير إضافي. بديل أقوى: اشتراك مزود ثانٍ (خرائط Failover موجودة وتنتظر مصدرًا حقيقيًا).
